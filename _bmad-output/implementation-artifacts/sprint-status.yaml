# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed

# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created, ready for development
#   - in-progress: Developer actively working on implementation
#   - review: Implementation complete, ready for review
#   - done: Story completed
#   - superseded: Story shipped but its delivered behaviour was invalidated by an
#     architecture correction; a replacement story carries the corrected work.
#     See planning-artifacts/architecture/.../correction-01-native-projection.md

# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed

# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed

# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when its first story starts (via build's sprint sync)
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates the next story after the previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; the status view surfaces open ones
generated: 08-24-2026 08:00
last_updated: 08-25-2026 17:30
project: panda
project_key: NOKEY
tracking_system: file-system
story_location: C:\code\panda\_bmad-output\implementation-artifacts
development_status:
  epic-1: done  # closed with the two ROADMAP-01 kernel seams (1.6, 1.7)
  1-1-plugin-manifest-foundation: done
  1-2-injection-disposal-and-atomic-swaps: done
  1-3-scoped-event-bus-and-layered-configuration: done
  1-4-executoradapter-port-with-contract-test-harness: done
  1-5-first-execution-claude-code-driven-headlessly: done
  # ROADMAP-01 M1: kernel seams, cheapest now because the kernel has no
  # production caller yet; a breaking change once composition lands.
  1-6-kernel-owned-observability-log: done
  1-7-tool-call-interception-waterfall: done
  epic-1-retrospective: optional

  epic-2: in-progress
  # ROADMAP-01 Correction A: the capability lives in a package, the CLI is a
  # thin binding. 2-0 heads M2 so init/doctor/selection are built on the
  # pattern rather than corrected onto it.
  2-0-session-composition-through-the-kernel: done
  2-1-registry-core-with-machine-scoped-serialization: done
  # 2-2 and 2-3 shipped correct machinery writing a vocabulary no executor reads
  # (verified empirically against all three CLIs). Machinery retained; rendering
  # and delivery location replaced by 2-8/2-9/2-10 per correction-01.
  2-2-projection-engine-with-sentinel-grammar-claude-target: superseded
  2-3-codex-and-opencode-targets-via-trait-data: superseded
  2-4-toolprovider-and-skillsource-ports: done
  2-5-shipped-adapters-complete-the-set: done
  # ROADMAP-02 amendment — M4. 4.A first: Epic 4 stands on a premise M3.C found
  # broken for one of three executors, and a wrong answer there invalidates the
  # worktree stories built on it.
  m4a-the-workspace-is-a-boundary-or-panda-says-it-is-not: done
  2-6-liveness-detection-hierarchy: backlog  # re-spec onto native hook locations
  # ROADMAP-01 M2: split — 2.7 was the named home for four deferred items.
  2-7a-panda-init-and-project-init: done
  2-7b-panda-doctor: done
  2-7c-executor-selection-for-panda-run: done
  2-8-native-config-projection-with-ownership-ledger: done
  # ROADMAP-02 M3 — the container and the promise. Not an epic: these precede
  # Epic 3, because the memory provider and the methodology plugin are each a new
  # plugin kind and nothing in production mounts plugins yet.
  m3a-distribution-and-a-falsifiable-sdk-proof: done
  m3b-plugins-mount-for-real: done
  m3c-the-token-budget-stops-being-a-boolean: done
  m4b-skills-materialise-where-executors-find-them: done           # implements 2.9
  2-9-filesystem-materialisation-targets: superseded  # re-specified as m4b onto verified locations
  2-10-unprojectable-entries-reported-not-faked: done  # shipped inside 2.7b: `unprojectable` is a live DiagnosisFindingKind (environment/src/doctor.ts:88) citing correction-01 C5, with 17 assertions in doctor.test.ts and a per-entry finding printed by the binary. Its AC's worked example (a shell-command Tool) is dead vocabulary since M4.E, but the BEHAVIOUR survived onto a live case: project-scope skills, unprojectable by construction (environment/src/executors.ts:103-106). Recounted 2026-09-02 by running `panda doctor`; the row had been lying while the code was green.
  m4c-every-state-panda-reports-has-a-way-out: done  # implements 2.11 + the 2.2-era drift-resolution gap
  2-11-remediate-blocks-written-by-a-previous-build: superseded  # folded into m4c, which covers correction-01 C6 and the four terminal states
  m4d-nothing-panda-prints-is-a-command-panda-does-not-have: done            # closes FR-11's surface; story 2.1 built the store, never a verb
  m4e-the-registry-declares-only-what-panda-can-deliver: done           # removes tool; profile deferred with its evidence banked
  m4f-profile-is-a-selection-not-an-entry: done           # PRD glossary settles it: a Profile is a selection OVER entries; Bundles carry Profiles (Epic 5)
  m5a-the-methodplugin-contract-is-published-or-it-is-not: done           # implements 5.3/FR-23/RD-3; 5.4 hot swap stays out
  m5b-every-rule-the-contract-states-is-enforced: done   # closes M5.A's two prose-only rules: the artifact `path` claim and the pair rule the type never checked
  m5c-panda-writes-the-selection-it-tells-you-to-edit: done   # the persistence verb FR-28 needs; closes the hand-edit hole 2.7c left in FR-7/FR-9
  m5d-a-method-is-a-selection-panda-mounts: done   # implements 5.4/FR-28/UJ-3; a method is config, not registry vocabulary
  m7b-the-kernel-tells-an-author-everything-it-found: done   # every manifest violation, not the first; and typed absence says which of three
  m7c-a-required-field-the-kernel-actually-applies: done   # the kernel APPLIES configSchema; three plugins stop hand-rolling it
  m7a-teardown-does-what-the-kernel-says-it-does: done   # three kernel promises found unenforced by reading cordis; one is a live unhandled rejection
  m7d-the-binary-can-see-what-the-kernel-does: done   # `panda run --trace`; createLogSink gets its first production consumer, and lostRecordCount is measured UNREACHABLE from the CLI
  m7e-a-broken-vendor-file-is-refused-not-spliced: done            # panda silently splices into a RECOVERED parse tree and writes a corrupted config; refuse with the location instead
  m8a-the-environment-travels-without-the-secrets: done          # implements 5.1/FR-21/NFR-5/NFR-6; `panda export`, and an omitted entry leaves a record so FR-22 can list it
  m8b-the-environment-arrives-and-says-what-it-could-not-bring: done          # implements 5.2/FR-22/FR-25; and fixes the separator that made a Windows bundle unusable on POSIX
  m6a-parallel-work-is-infrastructure: done   # implements 4.1 + FR-18; the brief's "concurrent workspaces from day one" promise
  m10a-a-session-can-run-in-a-worktree: done   # implements 4.2/FR-19/NFR-7; the second finished-but-unreached package this project shipped, now mounted. Found and fixed a shallow `Object.assign` that silently dropped a caller`s `workspace.provider` whenever a `cwd` was also named.
  m9a-the-registry-can-fill-itself: done   # `panda ingest [--dry-run]` + the first `SkillSource`; gives `ingestProviders` its first production caller. Found and fixed the seam defect it exposed: ingest reads the roots projection WRITES into, so an ingested skill was reported `foreign-collision` at its own source and `doctor` failed on a working environment. Source == destination is now `already satisfied` -- reported as nothing, and deliberately NOT adopted into the ledger.
  m10b-a-plugin-registers-under-the-key-it-reads: done   # NOT an epic story: a bounded fix the DeepSeek Harness mining surfaced while looking for a home for its generator/--check pattern. M7.C shipped "the kernel APPLIES the configSchema every manifest must declare"; measured 2026-09-03 by wrapping each manifest's schema and driving a real kernel, that guarantee was a NO-OP for `@panda/workspace-git-worktree` -- it registered as `workspace-git-worktree` and read the config key `workspace`, so the kernel handed its schema `undefined` on every activation, with its sibling `@panda/workspace-local` (id and key both `workspace`) receiving the real subtree as the control. Invisible to 1353 green tests because both factories re-validated the subtree themselves, so no user-visible behaviour differed. The rule itself existed ONLY as a comment at `packages/adapter-cli/src/plugin.ts:183` -- grep for an assertion over `**/test/**/*.ts` returns none, control: the same greps return plenty of other hits. Fixed at the root (`WORKSPACE_PLUGIN_ID = WORKSPACE_CONFIG_KEY`, two readers, and the selection already guarantees the two providers are never registered together), the redundant factory re-validation deleted in favour of `context.settings`, and the two pre-M7.C comments claiming "the kernel never applies it" corrected -- one of which was false and one true, which is how a stale sentence survives review. Gated by `packages/session/test/plugin-config-key.test.ts`, built out of the provider catalogue's own accessors so a provider added later is covered without anyone remembering, and FALSIFIED: reverting the id turns exactly that one row red and leaves the other three green. Verified end to end by driving both providers through create/release, with `git worktree list --porcelain` naming the tree in git's own vocabulary (correction-01 C5) and a malformed `rootDir` now refused by BOTH with the same schema message, via the kernel.
  m11a-the-registry-can-see-the-servers-you-already-have: done   # gives `ToolProvider` its first PRODUCTION implementation (FR-13b ingest half). Measured 2026-09-03 at bda68a0: `registry/src/ingest.ts:334` iterates `options.toolProviders` and `environment/src/ingest.ts:103` passes only `skillSources` -- control: 23 `toolProviders` hits under `packages/*/test`, so the search sees both trees. Closes the ledger entry re-homed twice (deferred-work.md:40,130) and SUPERSEDES M9.A's D8, whose stated reason ("the port is real; its medium is not the filesystem") is false at this commit: `projection/src/formats.ts:395` already reads the user's real vendor document through `memberValue(root, traits.mcpContainerKey)`, the container key per executor is verified trait data, and `ProjectionLedgerRecord.nativeLocation` already records which MCP entries are panda's own -- its doc comment gives `mcpServers.context7` as the example. Spec: spec-m11a-the-registry-can-see-the-servers-you-already-have.md SHIPPED with a critical caught in review and fixed at the root: the first implementation compared RENDERED BYTES, so `already satisfied` fired only when the user's entry spelled the exact key set `renderMcpEntry` emits -- `{"command":"npx"}`, a missing `type`, or an `env` block all stayed a permanent `foreign-collision`, which is the defect the D4 amendment existed to remove. Two independent reviewers found it; the coordinator confirmed by execution with a control. It now compares MEANING through `readMcpEntry`: `stillPandas` still asks "are these the bytes panda WROTE?" where a hash is right, and the new branch asks "does this FOREIGN entry already deliver what the registry names?", which a hash cannot answer. Fixing the root dissolved a second finding for free -- a byte comparison made TOML key order and comments significant, a value comparison cannot. THE REUSABLE LESSON IS ABOUT THE VERIFICATION, NOT THE CODE: AC2 was reported GREEN by a real binary run, because the acceptance fixture had been generated from `renderMcpEntry`'s own output and so landed in the one shape that passed. Driving the binary is necessary and NOT sufficient -- a fixture built from the code under test measures only that the code agrees with itself. Also closed in review: three mutation survivors (`droppedNativeKeys` could return empty, the reader's unreadable branch, both source-id literals), a round-trip clause that was a TAUTOLOGY while its comment promised the two directions could not drift, and the D3 ownership wiring whose unwiring killed one report-shape assertion and left the whole CLI suite green. Gate: 1,438 tests on Node 24 AND 26, bytes/typecheck/lint 0, `proof:consumer-install` 9 passed. Four ledger entries record what was deliberately NOT built.
  m12a-ad-2-is-enforced-where-it-says-it-is: done   # NOT an epic story. Closes two claims the repo makes and does not keep. (1) AGENTS.md lists AD-2 under "Architecture -- ENFORCED" while `test/guard.test.ts` exists in only 4 of 10 packages -- `adapter-cli`, `cli`, `contracts`, `registry`, `workspace-git-worktree` and `workspace-local` have none, and AGENTS.md names only two of those six. AD-2's own text says it exists to prevent "dependency-rule-by-lint-discipline", which is exactly what six packages rely on today. (2) ARCHITECTURE-SPINE.md:66 promises third parties they can implement any port installing ONLY `@panda/contracts`; `consumer-install.proof.ts:64` installs `@panda/session` plus the five packages it pulls in, so that promise has no gate. NOT speculative: AD-2 HOLDS at 066bf3f -- a checker over every `@panda/*` import in all ten `src` trees reports zero violations across 75 files -- and the checker is FALSIFIED (a planted upward import in `packages/contracts/src` is reported as exactly one violation naming only that package). The defect is a rule listed as enforced that is not. Spec: spec-m12a-ad-2-is-enforced-where-it-says-it-is.md
  m13a-the-two-executor-lists-cannot-drift: done   # resolves 5-5. One test file, written and falsified by the coordinator directly. Falsified FOUR ways with each direction proved SEPARATELY: a renamed profile id fires "projected into but not runnable"; a REMOVED profile block fires "runnable but never projected into" (a rename fires both at once and would let one assertion hide behind the other, so the second direction needs an edit that leaves the first passing); a duplicated block is named by its own clause instead of being smoothed away by set comparison; and emptying BOTH lists reddens all three clauses -- the case set equality ALONE would have passed, which is what would have made the file a decoration. Deliberately NOT the catalogue's own fix (derive the id from the trait record): that would make @panda/environment import @panda/adapter-cli, and environment/test/guard.test.ts pins its dependency set by EXACT equality -- weakening an existing guard for a convenience is the trade M12.A refused by name one commit earlier. The upgrade path is in the test's own comment.
  m14a-memory-is-a-contract-with-two-proofs: done   # implements Epic 3 WHOLE (3-1 + 3-2) so scenario S2 passes. CARRIES A COORDINATOR CORRECTION that unblocked the epic: 3.2's "one consumer test-sequence" was read as a defect -- an AC blessing a test-only consumer -- and it is not. The PRD SPECIFIES it. FR-15's own testable consequence is "One consumer TEST-SUITE passes unchanged against both shipped providers"; S2 is "an identical consumer SEQUENCE runs unchanged on both shipped MemoryProviders"; and SM-3 (an external developer implementing one) is marked "long-term directional signal, NOT a release gate", so `private: true` does not block it either. Memory will be the FIRST port panda ships with no production caller inside its own binary -- every other one has one (ExecutorAdapter, WorkspaceProvider/M10.A, SkillSource/M9.A, ToolProvider/M11.A, MethodPlugin/M5.D) -- and that is recorded here deliberately so nobody later rediscovers it as the defect class when the PRD designs it that way. Spec: spec-m14a-memory-is-a-contract-with-two-proofs.md
  m15a-panda-reads-the-stream-and-can-finally-say-how-much-is-left: done   # implements the reopened 5-6, and carries the prerequisite 5-6 and 2-6 SHARE: panda reads Claude Code's event stream instead of its single-result object. Ships WITH its consumer, never alone -- the stream change alone is user-invisible, and a capability with no reachable consumer is the defect class this project has shipped three times. Also measured and recorded for 2-6: the same stream carries `system/init`, `assistant`, `result/success` and three `hook_*` events, so 2-6's lifecycle vocabulary is reachable from it and its AC's "injected hooks written through the projection merge path" is a mechanism the vendors made unnecessary. `--verbose` is REQUIRED (without it stream-json exits 1) and does not pollute stderr.
  m16a-a-worktree-panda-made-is-a-worktree-panda-can-take-back: done   # SHIPPED 2026-09-03: `panda workspace remove [<id>]`, the intent-before-action removal and its sweep, the unreachable-commit refusal, and the `worktree-leftover` finding with its exit. Verified by execution on Node 24.14.1 and 26.8.1 against real git 2.49.0 -- see the spec's Verification section, including the falsification that stayed green (D3's ordering) and how it was closed. Originally: implements 4-3/FR-20. Also records, for story 2-6, why it is NOT next: its acceptance is `started/working/idle/exited` plus a PTY/OSC fallback, and both presume a PERSISTENT executor. RD-2 is a resolved decision that put that out of scope -- "the Executor process is EPHEMERAL per task; the Workspace persists. Process pooling/warm workers are out of scope." An ephemeral run starts, works and exits; `idle` is a state it cannot reach, and AGENTS.md's own judgment rule is not to build a branch that cannot fire. Three of the four states ARE observable from the streams M15.A opened (claude: `system/init`, `assistant`, `result/success`), so 2-6 is feasible AFTER a re-spec onto RD-2's model -- which is what its board row has always said -- but not as written.
  epic-2-retrospective: optional

  epic-3: done
  3-1-memoryprovider-port-with-append-only-provenance: done   # in m14a, together with 3-2. Shipping the port ALONE would publish an abstraction with zero implementations, which this project has paid for three times (ingest.ts 375 lines with no caller; @panda/workspace-git-worktree finished and unreachable for two milestones; ToolProvider published in 2.4 and unimplemented until M11.A).
  3-2-filesystem-and-sqlite-providers-prove-the-swap: done   # in m14a. SQLite is `node:sqlite` -- measured working on Node 24.14.1 AND 26.8.1, the exact two CI runs -- so FR-16 costs NO new dependency, which matters because panda ships exactly ONE non-@panda runtime dependency across all ten packages (`jsonc-parser` in projection).
  epic-3-retrospective: optional

  epic-4: done
  4-1-managed-git-worktrees-with-durable-ownership: superseded  # implemented as m6a onto the measured AD-2 split
  4-2-concurrent-sessions-across-worktrees: done  # shipped as m10a. Mounts the finished-but-unreachable @panda/workspace-git-worktree: selection at `workspace.provider`, read from ONE dump entry with its layer, mirroring selectExecutor. Verified by driving the binary -- a real `git worktree list --porcelain` entry, with "no config" as its control. Its second AC clause (mixed CLI+SDK contention) was already shipped as PANDA_REGISTRY_CONTENTION; its "Registry writes remain consistent" clause is measured VACUOUS on the run path (RegistryStore: 0 files under packages/cli/src, control 8 under packages/*/src) and recorded in the ledger rather than faked. Mirror + `worktree` entry type deliberately OUT with three measured blockers.
  4-3-crash-safe-disposal: done   # shipped in m16a. Originally: in m16a. NOT blocked, and the earlier assessment that it needed a persisted session log was WRONG -- that read the DSH note's summary instead of the AC. The AC is worktree disposal: "a disposal killed mid-operation... the next startup sweep... the trash-pattern leftover is reconciled and removed". Measured at 3209fc7: panda creates a worktree on every `panda run` under the git-worktree provider and has NO way to remove one -- `panda --help` mentions workspaces only under `run`, the verb that creates them (control: the same help lists every other verb), and `WorktreeLedger` exposes no removal path. The source names this story at the exact line it declined: git-worktree-provider.ts:167-173 (`dispose()` "removes nothing... tree removal is Story 4.3") and :100-103 (`--detach`, "branch lifecycle belongs to crash-safe disposal"). FR-20's "merged branches may be deleted while unmerged preserved" has NO SUBJECT in the shipped design -- panda creates detached worktrees and no branches (verified in M10.A's own run: `detached` in `git worktree list --porcelain`) -- so that clause is closed as not-applicable with its evidence and reopens if panda ever creates a branch. Its INTENT is implemented against a hazard measured in real git that GIT ITSELF DOES NOT GUARD: a worktree with modified or untracked files is refused ("use --force to delete it"), but a detached HEAD carrying a COMMIT is removed SILENTLY and `git branch --contains` returns 0 -- the commit is reachable from nothing. Spec: spec-m16a-a-worktree-panda-made-is-a-worktree-panda-can-take-back.md
  epic-4-retrospective: optional

  epic-5: done   # RECOUNTED 2026-09-03 and the header was LYING: all six stories were already closed while the epic still read `backlog`. 5-1/5-2 superseded by m8a/m8b, 5-3 by m5a+m5b, 5-4 by m5d, 5-5 resolved by m13a, 5-6 reopened by its own stated condition and shipped by m15a. Third time this board has lied about a state its own rows contradicted (after 2-10 and the 11-vs-7 recount), and the rule remains: recount by reading the rows, not the header.
  5-1-export-bundle-with-secrets-excluded: superseded  # shipped as m8a; its three criteria met, with Profiles/SkillSources absent because panda has neither
  5-2-import-and-re-project-elsewhere: superseded  # shipped as m8b; also fixed the separator that made a Windows-written Registry unusable on POSIX
  5-3-methodplugin-contract-published: superseded  # shipped as m5a (the contract) + m5b (the two rules m5a stated but did not enforce)
  5-4-methodology-hot-swap: superseded  # re-specified as m5d onto the measured answer: a method is a selection panda mounts, not an entry it projects
  5-5-full-diagnostics: done  # RESOLVED by m13a, clause by clause rather than by building the AC literally. "one drifted target" and "direct plugin writes detected" were already shipped (DRIFT_KINDS with FINDING_EXITS as a Record over the closed union; `edited` + `foreign-collision` asserted in environment/test/doctor.test.ts). "one missing adapter" is answered by a GATE, not a finding, and the reasoning is the story: EXECUTOR_PROFILES (environment, hand-written literals, drives detection+projection) and EXECUTOR_CATALOGUE (adapter-cli, keyed from trait records, drives running) are two independent lists that agree by discipline -- exactly the shape catalogue.ts:29-31 records having already shipped a defect from ("Story 2.7a shipped an executor that was never once exercised because a parallel name list drifted from the thing it named"). Both lists are COMPILED IN, so a user cannot make them disagree; only a panda author can. A doctor finding would report panda's own build defect to someone with no way to act on it and spend a FINDING_EXITS remediation on a state with no user-side exit. "one pending secret" is closed as NO SUBSTRATE on the same ground 5-6 was: `secret` occurs in exactly three files under packages/*/src (the bundle export detector, one kernel comment, the CLI), there is no pending state, and building one invents vocabulary. Spec: spec-m13a-the-two-executor-lists-cannot-drift.md
  5-6-environment-status-with-read-only-quota-surfacing: done   # REOPENED on 2026-09-03, and by the exact condition its own DO-NOT-SCHEDULE note named: "REOPEN the day a vendor publishes a documented usage endpoint, and only then". Measured by RUNNING the binary: `claude --print --output-format stream-json --include-hook-events --verbose` emits a typed `rate_limit_event` carrying `rate_limit_info.unifiedWindows` -- `five_hour` {utilization 0.12, resetsAt} and `seven_day` {utilization 0.22, resetsAt}. The old evidence was about HTTP headers and provider ENDPOINTS (panda read none; DeepSeek Harness reads none either) and was never about the executor CLI's own output STREAM, which is where the surface turned out to be. Panda cannot see it today because `adapter-cli/src/executors/claude-code.ts:55` passes `--output-format json`, whose full top-level key list was dumped as the control and contains no `rate_limit_info`. THIS IS NOT the screen-scraping 2.6 forbids: it is a typed field in a structured event the vendor emits deliberately under a flag the vendor documents -- correction-01's NATIVE-vocabulary-at-a-NATIVE-location rule applied to reading. Third deferral this session whose REASON expired while its entry did not notice, after M9.A's D8 and the coordinator's own D4. Spec: spec-m15a-panda-reads-the-stream-and-can-finally-say-how-much-is-left.md
  # NEXT, measured 2026-09-03 at the close of m16a: the ONLY row left anywhere is
  # `2-6-liveness-detection-hierarchy`, and it is NOT buildable as written. Its
  # acceptance asks for `started/working/idle/exited` plus a PTY/OSC fallback, and
  # both presume a PERSISTENT executor. RD-2 is a resolved decision that put that
  # out of scope by name: "the Executor process is EPHEMERAL per task; the
  # Workspace persists. Process pooling/warm workers are out of scope." An
  # ephemeral run starts, works and exits -- `idle` is a state it cannot reach,
  # and AGENTS.md's own judgment rule is not to build a branch that cannot fire.
  # What IS reachable: m15a opened Claude Code's event stream, which carries
  # `system/init`, `assistant` and `result/success`, so three of the four states
  # are observable today and the AC's "injected hooks written through the
  # projection merge path" is a mechanism the vendors made unnecessary. So 2-6
  # needs a re-spec onto RD-2's model first -- which is what its row has always
  # said -- and the re-spec has to decide what an ephemeral run's fourth state is,
  # or that there are only three. That is a requirements decision, not an
  # implementation one, and it belongs to the owner.
  epic-5-retrospective: optional
