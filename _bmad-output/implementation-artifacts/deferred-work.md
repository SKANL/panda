# Deferred Work

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-plugin-manifest-foundation.md`
  summary: RESOLVED (Fase 0, post-Epic 1) — CI workflow (Node 24 + 26 canary) and aggregate `pnpm check` gate added; repo published at github.com/SKANL/panda.
  evidence: .github/workflows/ci.yml green on both matrix nodes; format tooling intentionally omitted — ESLint + editorconfig-level consistency deemed sufficient for a single-writer repo until a second contributor lands.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-plugin-manifest-foundation.md`
  summary: Kernel↔contracts error-code parity tests live in @panda/contracts with a test-only devDependency on @panda/kernel (inverted test graph).
  evidence: RESOLVED BY DECISION (Fase 0 reassessment) — the Story 1.4 contract-test harness validates adapters/providers, not kernel error codes, so it is NOT a natural relocation home; the current contracts-side pinning with a test-only devDependency is coherent, disclosed, and drift-guarded. No move needed.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-plugin-manifest-foundation.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] Packages export raw TypeScript (`./src/index.ts`) with no compile/consumption story; a build step (or bundler strategy) is needed before CLI packaging.
  evidence: Code-review finding — deliberate greenfield choice (allowImportingTsExtensions + bundler resolution), but `panda run` (Story 1.5) forces a distribution decision.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-plugin-manifest-foundation.md`
  summary: "Before any I/O" guarantee for manifest validation is scoped to kernel-owned code by documentation only; no spy-based test harness proves it mechanically.
  evidence: Code-review finding — plugin-supplied Standard Schema validators necessarily execute plugin code, so full mechanical provability needs a dedicated harness; deferred as not actionable now.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-executoradapter-port-with-contract-test-harness.md`
  summary: RESOLVED BY DECISION (Story 2.5) — executor contract clauses keep invoking adapter.run() up to four times per suite audit; no caching and no probe isolation are added.
  evidence: The suite runs against FAKE spawners exclusively (spec 2.5 Boundaries: "contract-suite runs use fake spawners exclusively — no test may execute a real binary"), so per-run cost is nil and no side effect leaves the process — the premise that made repeated runs expensive never materialized. All four adapters (claude-code, codex, opencode, trait-only stub) run the full suite through `packages/adapter-cli/test/executor-suite.ts` with `FakeSpawner`; no contract-suite clause ever starts a process. (The overhead and tree-kill suites do spawn `process.execPath`, but they measure the spawn seam, not an executor, and the only test that runs a real coding CLI is the env-gated live smoke.) Revisit only if a real-executor conformance audit is ever added.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-executoradapter-port-with-contract-test-harness.md`
  summary: Workspace contract clauses assume disk-persisted state; genericity for in-memory/remote providers needs capability-gated clause variants.
  evidence: Suite hard-couples to node:fs at rootPath.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-registry-core-with-machine-scoped-serialization.md`
  summary: Registry persistence lacks fsync-level durability — there is a power-loss window after rename where the store file may be stale or empty.
  evidence: Atomicity (temp+rename) covers process crashes, not power loss; acceptable for v1 local tool, revisit if the Registry becomes a multi-device source of truth.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-projection-engine-with-sentinel-grammar-claude-target.md`
  summary: RESOLVED (Story M4.C) — `panda remediate` is the user-facing resolution flow, and no drift verdict is terminal any more. `adopt` claims what is at panda's location, `release` drops the claim and leaves the file, `repair` rewrites panda's own ledger, `discard` removes panda's own prior output from a vendor file.
  evidence: This entry was re-homed twice (2.7, then 2.7b) and stayed open for four stories because the missing piece was never a mechanism — it was the ownership-TRANSFER decision. M4.C takes it: adoption writes a durable ledger record for what is on disk, which is AD-6's own model ("ownership is proven by durable metadata records written at creation") applied to a record written at ADOPTION instead. Two proofs, both derived rather than listed: `packages/environment/test/remediate.test.ts` asserts `Object.keys(FINDING_EXITS)` equals doctor's own `DIAGNOSIS_FINDING_KINDS`, and asserts every member of the contracts' `DRIFT_KINDS` has an exit whose `by` is `'remediation'` — so a fourth drift verdict added upstream turns the suite red until it has an exit that is not a text editor. The same file then ENTERS each of the five remediable states on a real filesystem, reports it through `panda doctor`, remediates it by name and asserts the finding is gone.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-toolprovider-and-skillsource-ports.md`
  summary: Provider ingestion holds no run-scoped lock — `store.get` reads lock-free and each `store.register` takes its own lock, so a concurrent writer can mutate an entry between the change-detection read and the write, and two interleaved ingests leave the store holding a mix of both runs.
  evidence: Code-review finding — needs `RegistryStore.withLock(scope, fn)` spanning reads and writes; acceptable for a single-writer local tool, revisit when panda runs concurrently against one machine store.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-toolprovider-and-skillsource-ports.md`
  summary: Phase 2 issues one lock acquisition and one whole-file rewrite per contribution — N locks and O(n^2) bytes for a catalog of N entries, each with its own contention deadline.
  evidence: Code-review finding — wants a `RegistryStore.registerMany(entries, scope)` batching one lock, one read and one persist; the same change shrinks the partial-write window documented in the ingest jsdoc.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-toolprovider-and-skillsource-ports.md`
  summary: `list()` on an untrusted origin has no timeout and no cancellation — a provider that never resolves wedges the whole sequential collect phase with no error path.
  evidence: Code-review finding — needs an AbortSignal/timeout policy shared across the provider ports rather than a one-off race in the driver.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-toolprovider-and-skillsource-ports.md`
  summary: [STALE-FALSE 2026-09-04 — see the RE-MEASUREMENT at the end of this file] STILL OPEN, AND ITS NAMED HOME WAS WRONG — ingestion is additive only: an entry an origin stops contributing stays registered and keeps projecting forever, with no reconciliation path.
  evidence: This entry pointed at "the drift/doctor surface (Story 2.7)" as the home for its resolution flow, and M4.C is that surface — but measured against the shipped vocabulary the two problems do not meet. `panda remediate` operates on the PROJECTION ledger: which vendor location panda owns, and what it claims is there. Reconciling the REGISTRY is the opposite direction — deciding that an entry no origin contributes any more should stop existing — and none of `adopt`/`release`/`repair`/`discard` can express it, by construction: every one of them is scoped to a finding `panda doctor` reported, and an over-registered entry is not a finding, it is a registry that projects successfully. Its exit today is `RegistryStore.remove`, which is a real exit but not a user-facing one — and that understates the hole, so it is said plainly: PANDA HAS NO USER-FACING REGISTRY MUTATION AT ALL. There is no `panda register`, no `panda remove`, no `panda list`. Two of `panda doctor`'s own exits depend on one existing — `unprojectable` ("it stops being reported when the entry leaves the registry") and the keep-it-gone half of `removed-by-user` — so both currently point a user at an SDK call from a command line that offers none. Both sentences name `RegistryStore.remove` in `@panda/environment` explicitly rather than implying a command. Re-home to a registry-lifecycle story; pruning semantics are still Ask-First in 2.4's own Boundaries.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-toolprovider-and-skillsource-ports.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] The provider ports have no production consumer — nothing outside the new tests imports `ingestProviders`, `ToolProvider` or `SkillSource`.
  evidence: Code-review finding — the CLI surface (Story 2.7 init/project-init/doctor) is the first real caller and will exercise the API shapes (outcome keys, batch write, cancellation) that no test can pressure today.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-shipped-adapters-complete-the-set.md`
  summary: The executor trait path vocabulary (`readonly string[]`) cannot address array elements, so payload shapes like `message.content[0].text` — the shape most agent CLIs and the Anthropic/OpenAI message formats actually use — are unreachable without an engine edit.
  evidence: Code-review finding — `resolvePath` rejects non-record intermediates by design; no shipped record needs an index today, so the vocabulary stays minimal until a real executor forces it. Note that the trait-only stub test proves reuse for shapes the engine already handles, not for every shape.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-shipped-adapters-complete-the-set.md`
  summary: [STALE-FALSE 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `envelope.data` is a flat string map, so trait metadata keys can collide with the engine's own `result`/truncation keys and non-string metadata is silently dropped.
  evidence: Code-review finding — the collision risk is closed defensively by trait validation (rejecting the reserved keys); nesting metadata under its own namespace and preserving non-string values is a shape change worth doing when a consumer needs the richer payload (e.g. codex token usage).
- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-shipped-adapters-complete-the-set.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `panda run` has no executor selector — codex and opencode ship as library surface only, reachable exclusively through direct construction.
  evidence: Wiring adapter selection into the CLI is explicitly Ask First in this spec's Boundaries; the natural home is the CLI surface story (2.7 init/project-init/doctor), where executor choice becomes user-visible configuration.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-kernel-owned-observability-log.md`
  summary: Records carry no kernel identity, so two kernels sharing one sink interleave into a single unattributable stream.
  evidence: Code-review finding — the closed record shape has no `kernelId`, and nothing today constructs two kernels against one sink. Belongs with composition (ROADMAP-01 M2), when a caller owns more than one container; adding the field later is a record-shape version bump, which `LOG_RECORD_VERSION` already exists to carry.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-kernel-owned-observability-log.md`
  summary: No process-exit drain — records still in flight when the process ends are lost, and only the seq gap survives to show it.
  evidence: Code-review finding — `stop()` and `dispose()` both drain, but nothing hooks process exit, and the kernel deliberately owns no process-level concern today. Belongs with composition (ROADMAP-01 M2), when there is an owner of process shutdown to register the handler.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-kernel-owned-observability-log.md`
  summary: RESOLVED (Story 1.6) — `KernelOptions.orderLog` deleted rather than deferred; the record sink subsumed it.
  evidence: Two overlapping ordering mechanisms cost real coverage: the test named "treats a second stop as a no-op with no duplicate log entries" watched `orderLog`, which never receives `kernel.stopped`, so this story's own invariant went unchecked. Migrated all 6 call sites (4 in `packages/kernel/test/lifecycle.test.ts`, 2 in `packages/registry/test/plugin.test.ts`) onto `kernel.log.records`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-tool-call-interception-waterfall.md`
  summary: Interception caps are pipeline-wide only — `ActionPolicy` has no per-action scope, so "this one action at most N times" cannot be expressed without constructing a second pipeline.
  evidence: `ponytail:` note on `createActionPipeline` in `packages/kernel/src/intercept.ts`. AD-10's three named budgets (token budgets, loop caps, fan-out limits) are all run-scoped, and nothing today registers more than a handful of actions; the upgrade path is an `ActionPolicy.perAction` map, which stays declarative data and needs no new mechanism.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-tool-call-interception-waterfall.md`
  summary: The concurrency cap counts everything in flight, so an action invoked from inside another genuinely holds two slots — depth is indistinguishable from fan-out.
  evidence: `ponytail:` note in `packages/kernel/test/intercept.test.ts` ("releases both concurrency slots the nesting held"). Each invocation is still counted exactly once, which is what the matrix's nested-invocation row asks; what is missing is a depth-aware variant that counts only siblings. Deferred until a caller actually nests under a tight `maxConcurrent`, because the fix (tracking an invocation's parent) is a real mechanism, not a tweak.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-tool-call-interception-waterfall.md`
  summary: Counters are in-process and die with the pipeline — a budget cannot span two processes or survive a restart.
  evidence: Explicitly Ask-First in this spec's Boundaries ("persisting counters across processes") and listed under Deliberately not built. Belongs with composition (ROADMAP-01 M2), when there is an owner of run identity to key a persisted counter by.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-tool-call-interception-waterfall.md`
  summary: NARROWED (Story 2.0) — `@panda/cli` no longer bypasses the waterfall. Its only route to an executor is `runSession`, which invokes the executor as an action registered on `createActionPipeline`. That is the whole of what changed; the guarantee is NOT process-wide.
  evidence: `packages/session/src/run-session.ts` returns `action.invoke()`; `packages/session/test/run-session.test.ts` pins the invocation through the RECORD STREAM (a direct `adapter.run` leaves it empty) and pins that a cap refuses the action before the adapter is called; `packages/cli/test/run.test.ts` plus the repo-wide `no-restricted-imports` rule pin that the CLI cannot construct an adapter or a provider again. The entry below carries what remains open — it was briefly recorded as fully RESOLVED, which was false.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-tool-call-interception-waterfall.md`
  summary: [STALE-FALSE 2026-09-04 — see the RE-MEASUREMENT at the end of this file] OPEN, and the "NARROWED (Story M3.B)" wording that briefly stood here is WITHDRAWN as false. The waterfall is reachable around by FIVE measured routes, not two, and one of them is the kernel's own `swap`.
  evidence: Corrected after four context-free reviewers attacked M3.B. The five, each reproduced: (1) `@panda/adapter-cli` exports SIX value factories that yield a runnable executor, and `createExecutorAdapter('codex', ...)` — the catalogue's own, not a vendor's — is sufficient alone, so the earlier "we would have to unexport the vendor factories" argument was simply wrong; (2) `createExecutorPlugin(...).factory` is a `PluginFactory`, so a holder invokes it with an `ActivationContext` of their own and gets a real adapter on their own pipeline — reproduced against the emitted `dist/`; (3) `kernel.swap(pluginId, factory)` runs a caller-supplied factory against the live registry — measured with a capped shared kernel at `maxInvocations: 0`, an honest `runSession`, a real vendor spawn and an EMPTY record stream; (4) a host that keeps the adapter it passed to `createAdapter` can invoke it after a refusal; (5) none through the service itself — extraction via `Object.keys`, own symbols, a non-enumerable prototype property and direct mutation of `run` all failed, and the service is frozen. WHAT IS TRUE of M3.B, at its real size: for the pnpm-strict consumer that installed only `@panda/session`, the surface is ZERO, because that package exports `createSessionKernel` and no factory. It briefly exported `createKernel` and both plugin factories and the surface went 0 -> 1 IN THAT STORY; that regression was found and withdrawn before ship. Closing (1) and (2) needs the plugin factory and the catalogue to stop being exported from an implementation package, which no story has proposed; closing (3) is a kernel change (`swap` is Ask-First territory).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-tool-call-interception-waterfall.md`
  summary: A wedged `around` (or a wedged operation) holds its concurrency slot forever — there is no timeout and no cancellation, so one buggy interceptor is a permanent denial of service for the fan-out budget.
  evidence: `ponytail:` note on `createActionPipeline`. The slot is deliberately tied to the OPERATION settling rather than to `around` returning (that is what makes the fan-out cap real), which means `invoke()` also stays pending. Retries, backoff and scheduling are on this spec's Deliberately-not-built list and cancellation is the same class of mechanism; it needs an `AbortSignal` policy shared with the provider ports, which `spec-2-4` already defers for the same reason.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-tool-call-interception-waterfall.md`
  summary: `createActionPipeline` is a public export, so any holder can construct a SECOND, uncapped pipeline — the no-bypass guarantee holds (its stages still run) but the budget half of AD-10 is per-pipeline, not per-process.
  evidence: Code-review finding. A registered action cannot be executed outside a waterfall, which is what the acceptance criterion claims; what a second pipeline escapes is the first one's caps. Making a run's budget singular needs an owner of run identity to hold the one pipeline, which is composition work (ROADMAP-01 M2) — the same place the cross-process counter entry above lands.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-tool-call-interception-waterfall.md`
  summary: A contained stage error has no channel to reach a caller — a throwing `post` is recorded as `action.post-failed`, but the thrown value itself (message, cause, stack) is discarded.
  evidence: The 1.6 record shape is closed on purpose (no free-form slot, so a credential has nowhere to hide), and the kernel has no other outbound channel; a growing in-memory list of contained errors would be an unbounded leak. Story 1.6 has the same gap for a hostile sink. The natural home is composition, where a process-level owner can register a handler for contained diagnostics.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-0-session-composition-through-the-kernel.md`
  summary: NARROWED (Story M3.C) — a run is now CHARGED what the executor reported spending, so `maxTotalCost` and `maxInvocations` refuse on different runs. What is still flat is the ESTIMATE a run is admitted at, and it is denominated in a different unit from the settlement.
  evidence: `ActionDefinition.settle` reconciles the estimate against the operation's own result, and the executor plugin reads the vendor figure off `envelope.data.usage`; `packages/kernel/test/settlement.test.ts` and `packages/session/test/settlement.test.ts` prove by execution that one policy refuses on COST for an expensive run and on INVOCATIONS for cheap ones. STILL OPEN, and named on `SESSION_ACTION_COST`: the estimate is 1 while the settlement is in the vendor's tokens, so an admission decision and a settled total are measured in different units. Panda may not invent a pre-run token figure (this spec's Never list), and raising the default was MEASURED to break three pre-existing suites that pin cost-1 caps (0.5 and 1.5) by refusing their FIRST run — so a host that budgets in tokens supplies its own `cost` through `createExecutorPlugin`. Upgrade path: a per-executor estimate, which is per-model weighting and Ask-First.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-0-session-composition-through-the-kernel.md`
  summary: A session caller can declare CAPS but cannot install a stage — `pre`/`guard`/`around`/`post` are not reachable through `SessionOptions`, so policy beyond the three numeric budgets has no seam.
  evidence: Deliberate. Caps are data and can be dumped and diffed; a stage is a callback that can read the prompt, which is the exact failure AD-10 names, so exposing one is a capability decision rather than a plumbing one (this spec's Ask-First list). The pipeline already accepts stages; the session simply declares none.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-0-session-composition-through-the-kernel.md`
  summary: RESOLVED (Story M3.B) — the session mounts the executor adapter and the workspace provider as kernel plugins and consumes them by name; `createKernel` has a production caller.
  evidence: `packages/adapter-cli/src/plugin.ts` and `packages/workspace-local/src/plugin.ts` each ship a manifest, a config schema over their own subtree of the kernel's layered configuration, a factory and a disposer. `packages/session/src/run-session.ts` registers them, starts the kernel and resolves `executor` and `workspace` through `getService`; there is no construction fallback, which is proven rather than asserted — a session on a kernel with nothing mounted fails `PANDA_KERNEL_SERVICE_NOT_PROVIDED` naming the service (`packages/session/test/kernel-composition.test.ts`). AD-4's ordering is now exercised and read back off the RECORD STREAM (first record at seq 1 is a `manifest.validated`, nothing dropped, nothing lost). Measured by mutation: passing `loadPlugins` a sink of its own turns that clause red.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-0-session-composition-through-the-kernel.md`
  summary: NARROWED (Story M3.B) — the pipeline belongs to the KERNEL, and a host that mounts one kernel and runs many sessions against it shares one budget. A session that builds its own kernel (the `panda run` default) still gets its own pipeline, so a PROCESS still has no singular budget.
  evidence: `SessionOptions.kernel` accepts a caller-owned kernel; `packages/session/test/kernel-composition.test.ts` runs two sessions against one kernel under `maxTotalCost: 1.5` and the second is refused `PANDA_KERNEL_COST_CAP_EXCEEDED` before the executor runs, with a two-kernel CONTROL clause beside it where both succeed. Measured by mutation: dropping `options.actionPolicy` from `createActionPipeline` in the kernel turns the shared clause red and leaves the control green. What remains open is the default: `panda run` builds a kernel per invocation, which is a process-level ownership decision no story has made.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-0-session-composition-through-the-kernel.md`
  summary: PARTLY RESOLVED (Story M3.C) — `maxTotalCost` and `maxInvocations` no longer collapse: they now refuse on DIFFERENT runs under one policy. `maxConcurrent` is still collapsed with them for a session that runs one action.
  evidence: Stated on `SESSION_ACTION_COST` and in the package README. WHAT M3.C CLOSED, proven by execution rather than asserted: under one policy of `{maxInvocations: 50, maxTotalCost: 1000}` a run that settles at 5000 tokens refuses the NEXT run with `PANDA_KERNEL_COST_CAP_EXCEEDED` while the invocation count is 1, and under `{maxInvocations: 1, maxTotalCost: 1000}` the same session code refuses with `PANDA_KERNEL_INVOCATION_CAP_EXCEEDED` while the settled cost is nowhere near its cap (`packages/session/test/settlement.test.ts`). WHAT IS STILL OPEN: `maxConcurrent`. A session registers exactly one action and awaits it, so nothing a `panda run` does can put two operations in flight — the fan-out cap is exercised only by a host that drives several sessions on one kernel concurrently, which no shipped caller does. That is composition, not accounting, and it is the same open ownership question as the per-invocation kernel below. HALF DONE (Story M3.B): the shared pipeline exists — a kernel's pipeline sees every session mounted on it — and the remaining half is the adapter-reported cost, which is M3.C. CORRECTION, carried from the review of M3.B: the old sentence here claimed a FRACTIONAL `maxTotalCost` (0.5) discriminates cost accounting from counting. It does not, and never did while cost is identically 1: `maxInvocations: 1.5` and `maxTotalCost: 1.5` admit and refuse the same runs. What actually discriminates is the cap-specific error CODE — `PANDA_KERNEL_COST_CAP_EXCEEDED` versus `PANDA_KERNEL_INVOCATION_CAP_EXCEEDED` — which is why one code per cap exists. The mechanism the old sentence named does not fire; it is deleted rather than left standing beside the true one.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-0-session-composition-through-the-kernel.md`
  summary: `runSession` creates a workspace directory per call under `<cwd>/.panda/workspaces/<uuid>` and nothing ever removes it — `release()` ends a lease and `dispose()` deliberately leaves the tree in place.
  evidence: Measured: five sessions, five directories. Retention is a product decision (work inside a workspace is meant to survive), so the session documents it rather than guessing a policy; `panda doctor` (2.7b) is the natural home for a prune command. The cheap half is done — an invalid prompt is now rejected before anything is created, so a rejected request costs no mkdir.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-0-session-composition-through-the-kernel.md`
  summary: Cleanup is contained but not bounded — `contained()` catches a throwing `release`/`dispose`, not a HANGING one, and the CLI now prints the envelope after cleanup, so a wedged provider withholds output entirely.
  evidence: The old inline CLI printed before cleanup; restoring that order would mean the CLI holding the workspace lifecycle again, which is what Story 2.0 removed, so the trade is taken and named in a comment at the `out(...)` site. The fix is a cleanup timeout in the session, which needs the same shared `AbortSignal`/timeout policy that `spec-2-4` and the wedged-`around` entry already defer.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-0-session-composition-through-the-kernel.md`
  summary: More than ~10 concurrent sessions in one process emit `MaxListenersExceededWarning` when each is given the documented per-session `onInterrupt` wiring, because every session registers its own SIGINT/SIGTERM listener.
  evidence: A per-process concern, and the session deliberately owns no process-level state (that is why `onInterrupt` has no default). The host fix is one shared registration fanned out to the live controllers, which belongs to whoever owns the process — `@panda/cli` runs exactly one session, so nothing in this repo hits it yet.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: Detection is filesystem evidence only — an executor INSTALLED but never run has no configuration yet and reads as absent, and one uninstalled after it ran reads as present.
  evidence: `ponytail:` note at the top of `packages/environment/src/executors.ts`. Probing the binary is explicitly Ask-First in this spec's Boundaries and on its Deliberately-not-built list: it is slower, has side effects, and can hang. The ceiling is not hidden — every path consulted and its verdict are in `InitResult.detected`, so a user who disagrees can see exactly what was checked (`packages/environment/test/executors.test.ts` pins that both directions are reported).
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: `panda project init` cannot bind Codex to a project — Codex reads MCP servers from `~/.codex/config.toml` alone, so a detected Codex is reported in `InitResult.skipped` and nothing is written for it.
  evidence: `ponytail:` note on the codex profile in `packages/environment/src/executors.ts`, pinned by the project-scope clause in `packages/environment/test/init.test.ts` (which also asserts `~/.codex/config.toml` is NOT created). correction-01 verified no project-scope Codex location, and inventing one would write into a file Codex never opens — this epic's signature failure. Closes only if Codex grows a per-project config panda can verify.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: The record sink carries one record per TARGET attempt (`action.invoked` then `action.completed`/`action.failed`), not one per entry written, and it names the target rather than the file.
  evidence: `ponytail:` note in `packages/environment/src/init.ts`. The Story 1.6 record shape is closed on purpose — an event and a bounded subject, no free-form slot — and a file path is unbounded and arbitrary, so it cannot become a subject without the sink rejecting it. What was written and where IS durably recorded, in the projection ledger (correction-01 C2), which is the mechanism AD-6 mandates; per-entry records in the STREAM need a record-shape change, which `LOG_RECORD_VERSION` exists to carry.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: RESOLVED (Story M4.B) — the projection outcome now HAS a reason channel. `ProjectionResult.skipped` carries `{entryId, reason}` in the target’s own words, and `@panda/environment` prefers it over its own derivation.
  evidence: This entry named Story 2.9’s materialisation work as the home, "where a target first has reasons of its own", and that is exactly what forced it: the skills target can say "this skill names a source panda cannot read", which no derivation from a registry entry could ever produce. The registry-derived sentence remains for the shapes it was always right about (a kind no target projects; an mcp-server with no command) and is now also correct about SKILLS, because a config row stops claiming an executor cannot express one once that executor has a verified skills root. Pinned by "reports a skill whose source cannot be read, per executor" and "stops reporting a skill as something the executor cannot express" in `packages/environment/test/skills.test.ts`; both were verified to fail against the previous behaviour.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: RESOLVED (Story M3.B) — the re-homing this entry recorded is complete. Plugin mounting landed in M3.B, which needed the container for its own reasons (Epic 3's memory provider and Epic 5's methodology plugin are each a new plugin kind). The un-bypassable-adapter half did NOT land with it and stays open under its own 1.7 entry above, narrowed.
  evidence: `createKernel` now has a production caller (`packages/session/src/run-session.ts`); ROADMAP-02 M3.B named this story as the home and it held.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: RESOLVED (Story M4.C) — an edited entry has two exits now, and neither is `panda init`. `panda remediate adopt` re-claims it at its current bytes, after which the ordinary `panda init` converges it to the registry; `panda remediate release` drops the claim and leaves the edit alone forever.
  evidence: The half this entry got right is that `panda init` is not the place: it converges, and converging is exactly what it must refuse to do to content whose hash it no longer recognises. What M4.C adds is the missing step BEFORE it — a decision, taken by the user, one finding at a time, which changes what panda claims and not one vendor byte. Measured consequence of that split: `adopt` on a config entry writes only `~/.panda/projection-ledger.json`, proven by a whole-scope byte snapshot in `packages/environment/test/remediate.test.ts` ("leaves every other finding and every foreign neighbour byte for byte"), whose changed-path set is exactly `['home/.panda/projection-ledger.json']`.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: NARROWED by Story M4.B — `panda init` now projects MCP servers AND materialises skills. Tools and profiles are still reported per target as unprojectable with a reason, and nothing is written for them.
  evidence: Skills were the half this entry called "a filesystem materialisation (2.9)", and M4.B shipped it: a skill in the registry lands at `<home>/.claude/skills/<id>/SKILL.md`, `<home>/.codex/skills/<id>/SKILL.md` and `<home>/.config/opencode/skills/<id>/SKILL.md`, each location verified by EXECUTION against the real binary. What stays open is narrower and unchanged in kind: `tool` has no native representation in any of the three (correction-01 C5 forbids approximating one), and `profile` is panda’s own concept with no vendor surface at all. Skills at PROJECT scope also stay unprojectable, deliberately — materialising into a project is Ask-First in M4.B’s Boundaries, so no project-scope location was verified and panda invents none.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] RE-HOMED, NOT CLOSED — "the provider ports have no production consumer" named Story 2.7 (later 2.7a) as its home. 2.7a shipped `init` and `project init` and calls neither `ingestProviders` nor any `ToolProvider`/`SkillSource`.
  evidence: 2.7a projects what the registry HOLDS and is forbidden by its own Boundaries from inventing registry content, so populating the registry from providers is a different verb on a different surface (ingestion), not a step inside init. Nothing in this story made those API shapes easier or harder to exercise. Home moves to whichever story first gives ingestion a command; it is not 2.7b (a report over existing data) either.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] RE-HOMED, NOT CLOSED — "`panda run` has no executor selector" named 2.7 (later 2.7a) as its home. 2.7a added no flag to `panda run` and no executor choice anywhere.
  evidence: This story's Deliberately-not-built list says "no new commands beyond the two", and executor selection is a `panda run` concern with no bearing on projection. ROADMAP-01 M2 already names Story 2.7c ("executor selection for `panda run`") as its home; that is where it goes. What 2.7a DID establish for it is that codex and opencode are reachable end to end from a composed path, not only as library surface.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: [STALE-FALSE 2026-09-04 — see the RE-MEASUREMENT at the end of this file] The kernel record sink (F3/NFR-4) has no PRODUCT consumer. `@panda/cli` never passes one, `@panda/environment` and `@panda/session` both build a memory sink and drop it, and `createMemoryLogSink` is the only factory that exists — so the stream is produced on every run and nothing durable ever reads it.
  evidence: Measured across both consumer packages and `packages/cli/src/run.ts`. `createLogSink(write)` already accepts any writer, so the missing half is a product decision (where records go, retention, rotation) rather than a mechanism — all three of which are Ask-First in spec 1.6. Until something reads it, "reconstructable from the records" is true of a stream nobody keeps. STILL OPEN after Story M3.B, and worth restating rather than closing: the stream now covers a whole production run in ONE place (manifest validation, activation, the invocation, disposal, `kernel.stopped` — pinned as one ordered trail in `packages/session/test/kernel-composition.test.ts`), which is strictly more than before, and nothing durable still reads it. `panda run` passes no sink.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7a-panda-init-and-project-init.md`
  summary: `@panda/environment`'s re-export closure is not total: `StaleLockBreak` (reachable through `RegistryStoreOptions.onStaleLockBreak`) and `ProjectionTarget` (returned by `ExecutorProfile.createTarget`) are not re-exported, so a consumer implementing either callback cannot name their types.
  evidence: Both are callback-only surfaces the ordinary path never touches, so the entry point is not widened on speculation; the closure comment in `packages/environment/src/index.ts` now says the closure is partial instead of overstating it. Widen when a consumer actually implements one.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-8-native-config-projection-with-ownership-ledger.md`
  summary: NARROWED — the projection ledger's read-modify-write is now serialised per LEDGER FILE across the whole process, closing the two-instance case (concurrent `initMachine()` + `initProject()` lost one claim permanently). Two panda PROCESSES can still interleave and lose a claim.
  evidence: The header comment previously claimed merging alone made concurrent runs safe; it does not, because `update` rewrites the whole document from a read taken before the other side wrote. Reproduced and pinned by "two ledgers over one file cannot lose a claim" in `packages/projection/test/engine.test.ts`, verified to fail against a per-instance queue. Cross-process needs a real lock, which cannot be borrowed from `@panda/registry` (AD-2/AD-7: that edge was removed in Story 2.8 and leaked PANDA_REGISTRY_* codes out of a projection API) — the upgrade path is a leaf lock package with its own error codes.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7b-panda-doctor.md`
  summary: `panda doctor` reports a pending projection at FILE level (`out-of-date`, naming the executor and the file) and never per entry, so a user with twelve registered servers is told the file would be rewritten without being told which entries account for it.
  evidence: `ProjectionResult` carries `written`/`byteDelta` and no channel for the entries behind the difference, and correction-01's whole point is that doctor must not compute anything the writing path did not — a per-entry answer derived here would be exactly the second classification the story forbids. Upgrade path: the engine surfaces the planned entries (it already computes `ProjectionMergeOutcome.records` and `ownedSpans`) alongside `written`, and doctor projects them. Marked `ponytail:` on `DiagnosisFindingKind['out-of-date']`.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7b-panda-doctor.md`
  summary: RESOLVED (review round 1) — `not-initialised` was keyed on panda's own DIRECTORY, which made one `panda project init` anywhere mark the MACHINE scope initialised forever, because the ledger's first write creates `<home>/.panda` on its own. The deferred note called that "a hand-made empty `.panda`"; it was the ordinary path. Now keyed on the scope's registry document.
  evidence: `RegistryStore.storePath(scope)` added as the read-only half of `ensure()` — the argument for forking the layout instead ("a second copy of a path the store owns") was already spent, since the diagnosis prints `ledgerPath` the same way. Pinned by "does not call a machine initialised because some OTHER scope created panda's directory" in `packages/environment/test/doctor.test.ts`, verified to fail against the directory check.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7b-panda-doctor.md`
  summary: RESOLVED (Story M4.C) — the ownership-transfer decision this entry was waiting for is made, and doctor is still the reporter rather than the remediator.
  evidence: The condition written here was "re-home when that decision exists". It exists: ADOPTION writes a ledger record whose `contentHash` is the hash of what is at panda's location NOW, which transfers ownership deliberately, per finding, at a user's request. 2.7b's own shape held and was not disturbed — `panda doctor` still writes nothing (its byte/mtime snapshot is unmodified and still green, including on a machine holding a legacy block, which doctor now reports through the `discard` remediation's own read-only inspection), and `panda remediate` is a separate command that calls `diagnose` for its findings, so the report and the exit cannot disagree about what state the machine is in. One thing 2.7b's design DID force: `panda remediate` refuses any state the same run did not report, which is what makes "remediating a finding panda did not report" (Ask-First in M4.C's frozen block) unreachable rather than merely discouraged.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7b-panda-doctor.md`
  summary: Doctor diagnoses exactly the scope the caller named (`panda doctor` for the machine, `panda project doctor [dir]` for one project). There is no machine-wide scan for projects panda has bound.
  evidence: Ask-First in this spec's Boundaries and on its Deliberately-not-built list. Panda keeps no index of bound projects — the ledger records target+file pairs, so a machine-wide answer would mean either walking the filesystem or introducing a project index, both of which are product decisions rather than mechanisms. Reported per scope until one is made.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7b-panda-doctor.md`
  summary: Doctor's writability check is `access(W_OK)` on the nearest existing ancestor, which is ADVISORY: on Windows it sees only the read-only attribute and not ACLs, and nothing survives another process taking the file between the check and the write. A positive answer is not a guarantee that `panda init` will succeed.
  evidence: The only stronger check is a trial write, which is exactly what a command whose defining clause is "writes nothing" may not do. Handled by wording rather than by a mechanism: the `out-of-date` resolution says panda CHECKED the location is writable and names both limits, so it stays true when the check is wrong. A negative answer IS load-bearing and reported as `not-writable` — verified against a real `chmod 0o444` on win32, where `access` reports EPERM and the rename-over that `atomicWriteText` performs fails EPERM too.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7b-panda-doctor.md`
  summary: `out-of-date` is decided by BYTE inequality between the file and what the merge produced, while ownership is compared canonically. A key reorder inside a panda-owned member therefore reports `out-of-date` (exit 1) with zero drift findings.
  evidence: Not a false statement — projecting really would rewrite those bytes, so the exit code is right — but it is a coarser question than the one the ledger asks. Closed by wording for now: the detail says "the bytes in X differ from what projecting would produce", which is exactly what was checked, instead of "does not match the registry", which was not. A canonical comparison belongs with the same engine change that would make `out-of-date` per entry, since both need the merge to report more than a single boolean.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7b-panda-doctor.md`
  summary: The `projection-warning` finding kind has no fixture, because the projection engine seeds warnings from exactly one source (the ledger) and there is no second code to force.
  evidence: That is the kind's whole purpose: `WARNING_KIND` maps the ledger code explicitly and everything else falls to `projection-warning`, so a warning source added upstream surfaces as itself instead of shipping silently wearing the ledger's resolution text. The finding-shape loop in `packages/environment/test/doctor.test.ts` judges it if it ever appears, and the kind-partition clause fails if a future kind is added without a class. Fixture arrives with the second warning source.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7b-panda-doctor.md`
  summary: SCOPE NOTE on the writes-nothing proof — the byte/mtime snapshot covers the INJECTED temp root (`homeDir` + `projectDir`) that each fixture hands `diagnose`, not the whole filesystem or the process. A write outside the scope panda was pointed at would not be seen by it.
  evidence: Filed at the reviewers' request rather than fixed. Every path doctor can reach is derived from `homeDir`/`projectDir` — the registry document, the ledger, panda's directory and every vendor config come from `EXECUTOR_PROFILES` under those two roots — so the scope of the snapshot is the scope of the code under test; a process-wide watch would add coverage for a defect that has no reachable route. The independent audit that ran a recursive `fs.watch` across the call observed zero events, which is the wider check this note stands in for.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7c-executor-selection-for-panda-run.md`
  summary: [DETAIL-DRIFT 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `panda doctor` certifies a machine that `panda run` then refuses. With `{"executor": 7}` in `~/.panda/config.json`, `panda doctor` exits 0 with empty stderr while `panda run` exits 2 with `PANDA_CONFIGURATION_UNUSABLE` — doctor does not know panda's own `config.json` exists, because executor selection is not part of what it diagnoses.
  evidence: Reproduced end to end against the real binary. `run.test.ts` already pins this exact invariant CLASS for the other pair ("never certifies an environment `panda init` then refuses"), so the gap is a real one and not a wording preference. It is NOT closed here because `diagnose` lives in `@panda/environment` and `resolveExecutor` in `@panda/session`, both CONSUMER tier: AD-2 forbids the edge in either direction, and `packages/session/test/guard.test.ts` pins @panda/session's dependency set to exactly four packages, so the dependency would fail the gate rather than ship. Duplicating the reader inside `@panda/environment` is the second-implementation failure that correction-01 forbids and that made doctor an inspection MODE rather than a second engine. Upgrade path is the SAME one recorded for the `.panda/config.json` spelling in this story's Spec Change Log: move the scope-directory and configuration-document convention down into `@panda/contracts` (shared tier) so both consumers read one spelling, after which doctor can diagnose the document without depending on the session. Re-home to whichever story makes that move.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: The interception pipeline never retires a registered action id, so a long-lived kernel running many sessions grows `registeredIds` without bound.
  evidence: `ponytail:` note on the executor service in `packages/adapter-cli/src/plugin.ts`. `ActionDefinition.run` takes no arguments and is read ONCE at registration (which is what stops a caller swapping the operation after the price was agreed), so a per-request operation cannot reuse one handle — registration per invocation is the only shape that survives the rule. CORRECTED on review: this entry first said "collisions are not the risk: workspace ids are UUIDs on the default provider", and that hedge named exactly the assumption the story's own `createProvider` seam broke — a pooled provider hands back a STABLE workspace id, therefore a stable action id, and the second run on a shared kernel failed `PANDA_KERNEL_ACTION_INVALID: 'id' is already registered`. That pair is now refused (`createProvider` beside `kernel`), so the collision is unreachable rather than unlikely; retention remains open. Upgrade path: a pipeline that can retire a handle, which is the same mechanism the post-hoc cost adjustment in M3.C needs, and which would also let the action id stay workspace-scoped for a host that genuinely pools.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: A session-owned kernel's LIFECYCLE records are unreachable — `SessionOptions.log` receives only the waterfall's `action.*` records, and the rest stay in a memory sink the session drops.
  evidence: Forced by measurement, not preference: three existing suites (`run-session.test.ts`, `consumer.test.ts`, the installed-tarball proof) assert the EXACT record list a session-supplied sink receives, and a kernel constructed with that sink writes `manifest.validated`/`plugin.activated`/`plugin.disposed`/`kernel.stopped` into it too. Redefining a published option to mean the kernel's whole stream would have been a breaking change bought for nothing. The escape hatch exists and is exercised: a caller builds `createKernel({ log })` itself and passes it as `SessionOptions.kernel`, which is where the AD-4 ordering proof reads its stream. Closes when `panda run` has a durable record destination (the sink-with-no-product-consumer entry above), at which point the CLI would supply the kernel.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: One executor per kernel. Mounting a second `createExecutorPlugin()` is refused with `PANDA_KERNEL_MANIFEST_INVALID` (duplicate plugin id `executor`) before service resolution is reached, so a host cannot mount claude-code and codex side by side and choose per session. Two DIFFERENT plugin ids providing `executor` would be `PANDA_KERNEL_SERVICE_CONFLICT`, but `createExecutorPlugin` cannot produce a second id.
  evidence: Deliberate for this story: the selection is a CONFIGURATION decision resolved at activation from one composed document, which is what makes `.panda/config.json` the single place a user changes their agent. Mounting several would need either service names keyed by executor id (which makes the consumer name the vendor, the coupling AD-1 forbids) or a multi-provider service kind the kernel does not have. A host that genuinely needs two runs two kernels today. Re-home to whichever story first needs per-session executor choice inside one process.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: [STALE-FALSE 2026-09-04 — see the RE-MEASUREMENT at the end of this file] Plugin mounting is STATIC — `runSession` and any host register factories they imported. Nothing reads plugin code from disk, and AD-3's "installing one is executing it" surface still does not exist.
  evidence: Named on this spec's own "What this is not" and Ask-First in its Boundaries ("dynamic import of plugin code from disk, and any plugin discovery mechanism"). AD-3 is no longer the only architecture decision with no adjacent story — the container has a production caller and manifests, service resolution and containment are all exercised — but its TRUST half needs a decision about what installing a third-party plugin is allowed to do, which no artifact has made. Re-home to whichever story first accepts a plugin panda did not ship.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `PluginManifest.configSchema` is decorative — the kernel PROBES the field for shape (Standard Schema v1, synchronous, non-throwing) and never applies it to the plugin's own subtree of the layered configuration. Every plugin enforces its own schema by calling it from its factory, so a manifest whose schema accepts anything is indistinguishable from a correct one.
  evidence: Measured on review of M3.B: replacing BOTH new plugins' schemas with a no-op that accepts anything leaves 103, 23, 84 and 53 tests green, tsc 0 and eslint 0. Inherited from `@panda/registry`'s plugin (Story 1.2), not introduced here. NOT fixed in M3.B by deliberate call: applying it is a kernel semantic change that touches an existing plugin and is not on that spec's Ask-First list. PROPOSED FIX, with this measurement as its evidence: `createKernel` validates each manifest's `configSchema` against `config.resolve()[<plugin subtree>]` before calling the factory and converts issues into the same contained `PANDA_KERNEL_PLUGIN_START_FAILED` the factory's own rejection produces — which would also settle what "its own subtree" means, currently a per-plugin convention rather than a kernel rule. Until then, the enforcement point is pinned by a clause in `packages/workspace-local/test/plugin.test.ts` and stated on the schema constant itself.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: A ~3000-level nested configuration document still fails with an UNCODED `RangeError: Maximum call stack size exceeded` naming no file. The overflow is in `LayeredConfig.dump()` inside `selectExecutor`, OUTSIDE the `setLayer` call that `seedExecutorConfig` wraps; at ~4000 levels the wrap does fire and the error is coded and names the file.
  evidence: Pre-existing (Story 2.7c), and re-measured on review of M3.B because that story's Spec Change Log #5 implied the whole depth band was covered by the wrap. It is not — the wrap covers `setLayer`, and there is a band between the two thresholds where the composed-view walk overflows first. Fixing it means an explicit depth bound in the kernel's config walk (or an iterative `dump()`), which is a kernel change; wording corrected in the spec rather than papered over.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: [DETAIL-DRIFT 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `ARCHITECTURE-SPINE.md`'s AD-2 diagram disagrees with the code. It draws implementation packages depending on `@panda/contracts` alone, while `@panda/registry` has depended on `@panda/kernel` since Story 1.2 and `@panda/adapter-cli` and `@panda/workspace-local` joined it in M3.B; separately and pre-dating all three, `@panda/environment` depends on BOTH `@panda/projection` and `@panda/registry`, which the diagram does not express at all.
  evidence: M3.B's Spec Change Log #9 argued the precedent rather than proposing an amendment, which leaves a spine document that four reviewers can each independently read as violated. The dependency is real and correct — a plugin's manifest and factory are kernel types — so the fix is an amendment to AD-2 stating that implementations may depend on the kernel for its plugin TYPES (all three edges are type-only and erase from `dist`), plus a consumer-tier row for `@panda/environment`. Documentation debt, not code debt; it needs an artifact owner rather than a story.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: The `executor` configuration subtree is a bare STRING (`{"executor": "codex"}`), so it has nowhere to carry per-executor options. A binary path or a model selection is reachable only through mount-time `adapterOptions`, which no document can supply.
  evidence: Deliberate in M3.B: the key is the one panda's own `.panda/config.json` already spells, and inventing an object namespace beside it would have been the second spelling this repository keeps getting burned by. The ceiling is that widening it later — `{"executor": {"id": "codex", "command": "..."}}` — is a break to a user-facing document. Natural home is M4's `profile` entry kind, which is where the PRD already puts per-executor model and effort selection; a document migration belongs with it rather than ahead of it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: A throwing `createAdapter` seam now surfaces as `PANDA_KERNEL_PLUGIN_START_FAILED ... executor: <original message>` rather than the bare error the caller threw.
  evidence: A consequence of mounting: the kernel contains a throwing factory and reports it naming the plugin, which is AD-5 working as specified. Not argv-reachable (`createAdapter` has no CLI spelling) and the pre-existing clause in `packages/session/test/executors.test.ts` survives on `toContain`, so nothing is broken — but a host that matched on the error identity would now see a wrapper. Recorded rather than fixed: unwrapping it would mean the session re-throwing a plugin's `cause`, which loses which plugin failed, and that is the half AD-5 says must be reported.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3b-plugins-mount-for-real.md`
  summary: The SDK's documented two-call shape still builds TWO layered configurations, and the kernel's one receives no user document. `panda run` builds and seeds exactly ONE (verified with an instance counter), but `resolveExecutor(...)` then `runSession({ executorId })` — the shape in `consumer.test.ts` and in `consumer-install.proof.ts` — leaves the kernel's config holding `[defaults, invocation]` only, so no mounted plugin sees `.panda/config.json` on the FR-29 surface.
  evidence: Measured on review of M3.B. Not closable inside that story: `runSession` may not read files (pinned by "reads no configuration of its own" in `packages/session/test/executors.test.ts`), `ExecutorSelection` may not grow a field (pinned by `toEqual` in `consumer.test.ts`), and both call sites live in pre-existing test files the story was forbidden to edit. What changed instead is the DOCUMENTED path — `@panda/session`'s README now shows `readExecutorConfigLayers` -> `runSession({ configLayers })`, and `packages/session/test/kernel-composition.test.ts` proves a document's `workspace.rootDir` reaches a mounted plugin through it. Closes when the two-call consumer proofs may be updated, which is a test-edit permission rather than a design problem.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: A settled cost is what the VENDOR said it spent. Panda checks that a number is a usable number and never that the tool told the truth about its own usage.
  evidence: Stated on `ActionDefinition.settle`. The kernel rejects a non-number, a NaN, an infinity, a negative and anything past `Number.MAX_SAFE_INTEGER`, and the adapter voids a figure whose billed records cannot all be read — but a vendor that UNDER-reports its own tokens is charged what it claimed. NARROWED on review: a settlement is now floored at the declared estimate, so under-reporting can never take a run below what it was admitted at, which is what stops an all-zero usage object (what `claude` prints on a logged-out machine) from making runs free. Above the estimate the figure is still taken on trust; there is no second source to reconcile against short of panda counting tokens itself, which is this spec's Never list. The remaining mitigation is the invocation cap, which no vendor figure can influence.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: The settled total can overshoot `maxTotalCost` by `N x (settled - estimate)` — settlement is post-hoc, so the cap refuses the NEXT admission and never interrupts a run already in flight, and with `maxConcurrent` unset N runs are admitted on estimates before any of them settles.
  evidence: Deliberate, and stated on `createActionPipeline`: no honest token figure exists before a run, so the only refusal a budget can make about work that already happened is to refuse the next one. CORRECTED on review: this entry said "a SINGLE run", which understates it — the bound is per run only when concurrency is capped at 1, and `maxConcurrent` is unset by default, so every run admitted while the first is in flight adds its own overshoot. `maxConcurrent` is the lever that bounds N today. A settlement re-entered from inside `settle` is a different case and is now refused outright (`PANDA_KERNEL_SETTLEMENT_IN_PROGRESS`): there the figure is known and merely unapplied, so nobody can price that admission. Enforcing mid-run needs a vendor-side token limit passed into the executor (claude's `--max-turns` is the nearest lever, and it bounds turns rather than tokens), which is per-executor argv policy nobody has specified. The up-front refusal on the settled total is proven by execution; the mid-run one does not exist.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: RESOLVED (Story M3.C, review round 1) — the record stream carries the whole accounting whenever a policy is configured, and the total reconstructs from it exactly. On an UNBUDGETED pipeline the settlement records are deliberately silent.
  evidence: This entry first recorded the absence as open, which was the wrong shape. The settlement records are gated on `policyConfigured` rather than on a figure having arrived: with a budget set, every admitted settleable invocation emits `action.estimated` at ADMISSION and every reconciled one an `action.settled`, so a reader taking the settled figure where it exists and the estimate where it does not gets the total exactly (pinned by a clause that does the arithmetic). MEASURED before the gate: reconstruction across three actions gave 59845 against an actual 59852, the gap being exactly the estimate of the action nothing observed. What is deliberately NOT recorded: an unbudgeted pipeline emits nothing extra, because the frozen block promises a run with no policy set behaves exactly as it did before, and three pre-existing clauses pin that exact event sequence.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: Vendors report MONEY as well as tokens and panda drops it — claude's `total_cost_usd` and per-model `costUSD`, opencode's `cost`.
  evidence: Read off real runs while writing the story (claude reported `total_cost_usd: 0.430985` for a one-word task) and deliberately not shipped: a monetary figure is on this spec's Ask-First list, because money is a different promise from tokens and a budget denominated in it means something a user will hold panda to. The channel would carry it unchanged — `usagePaths` sums numbers and says nothing about their unit.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: `LogEntry` gained an optional `cost` and `LOG_RECORD_VERSION` stayed at 1.
  evidence: Deliberate, argued on the constant itself. `cost` only ever appears on `action.estimated` and `action.settled`, two events a v1 reader did not know and had to skip anyway, so every correct v1 parse still reconstructs everything v1 promised; bumping would tell that reader its parse had become invalid, which is false. Also load-bearing: `packages/kernel/test/log.test.ts:47` pins `version === 1` and the frozen block requires it to pass unmodified. A bump is owed the day an EXISTING event's record changes shape.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: The per-vendor live verification costs one real API call per vendor on every `pnpm check` where the binaries are installed.
  evidence: `packages/adapter-cli/test/usage-live.test.ts`. Two one-word tasks and two runs of a deliberately MULTI-STEP opencode task, because a single-step run cannot tell summing the steps apart from taking the last one — which is how a per-step billing defect shipped in the first place. It is the only executable form of "the figure panda charges is the one THIS binary printed in THIS field", which correction-01 requires of anything that reads a vendor's output, and a replayed fixture cannot notice a vendor renaming a field. `PANDA_LIVE_USAGE=0` disables it and a missing binary skips visibly. Cheaper alternatives were considered and rejected: a recorded fixture proves nothing about the current binary, and a `--version`-only check proves nothing about the payload.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: Two pre-existing test files were edited to land this story: `packages/adapter-cli/test/executor-suite.ts` gained an optional `expectedUsage`, and `packages/adapter-cli/test/executors.test.ts` gained `expectedUsage: 33` for codex.
  evidence: Not avoidable, and the reason is evidence rather than convenience: that fixture already contained codex's real `{"type":"turn.completed","usage":{"input_tokens":11,"output_tokens":22}}` event, so once codex reads its own usage field the fixture legitimately produces `data.usage === 33` and the harness asserts `envelope.data` with `toEqual`. The alternatives were to ship codex with no usage trait (refusing to read a field the vendor demonstrably prints — the inertness correction-01 exists to prevent) or to contrive codex's path set so the fixture would not match (a guessed field in a disguise). Both edits ADD an expectation; no existing assertion was weakened. Every other test file in the repository is untouched.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: `panda run` cannot set a budget at all — there is no flag and no configuration key, so `actionPolicy` has no production caller outside `run-session.ts`. This story is true for an SDK host and vacuous for the shipped binary.
  evidence: Pre-existing (the option landed in Story 2.0 and M3.B moved the pipeline to the kernel), but this is the story that made caps mean something, so it is the story that has to say the only shipped entry point cannot use one. Deliberately NOT built here: a user-facing budget is a promise about money and time and deserves its own frozen block — what unit a cap is denominated in, what happens mid-run, and what the CLI prints when one fires. The layered configuration already has the shape for it (`actionPolicy` beside `executor` in the kernel's composed document), so the next story is a surface, not a mechanism.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] A green `pnpm check` is not reproducible from a fresh Windows checkout — `packages/projection/test/vendor-conformance.test.ts` fails four clauses on a clone that has not normalised line endings.
  evidence: Diagnosed during this story's review and NOT caused by it. `.gitattributes` protects `packages/projection/test/goldens/**` with `-text`, so those survive checkout untouched, but `packages/projection/test/vendor-schemas/*.md` are not covered and check out CRLF under git's autocrlf on Windows. The extractor regex that pulls the fenced schema blocks out of those documents matches zero blocks against CRLF, so the clauses fail with an empty corpus rather than a wrong one. The fix is one `.gitattributes` line covering the vendor-schema documents, or a CRLF-tolerant extractor; it belongs to whoever owns the projection suite, not to this patch.
- source_spec: `_bmad-output/implementation-artifacts/spec-m3c-the-token-budget-stops-being-a-boolean.md`
  summary: RESOLVED by Story M4.A — `opencode` wrote files OUTSIDE the workspace directory panda spawned it in, into the directory the test process itself was launched from. Mechanism found (`PWD`, not `INIT_CWD`), cause fixed in panda, fix proven by the same measurement.
  evidence: Found because the first version of the live multi-step check asked opencode to create two files and left `a.txt` and `b.txt` behind in `packages/adapter-cli` — twice, reproducibly — while the child had been spawned with `cwd` set to a fresh `mkdtemp` outside the repository. Ruled out as a panda bug: a control child (`node -e "console.log(process.cwd())"`) through the same spawner reports the temp directory exactly, and the same opencode prompt run directly from a shell inside the repo wrote into its own cwd. So the spawn seam honours `cwd` and opencode does not bind its file tools to it; the mechanism is unproven, and an inherited environment hint (`INIT_CWD` is set by the package manager and inherited by the test runner) is the obvious suspect. WHY IT MATTERS beyond a dirty checkout: `WorkspaceHandle.rootPath` is delivered to an executor as the child's cwd and nothing else, so for opencode the workspace is a suggestion rather than a boundary. Deliberately NOT fixed here — a real boundary is a sandbox decision, not a patch to a token-accounting story. The live check now uses a read-only task, which is the right shape for a verification suite regardless. CLOSED BY M4.A, and the original evidence was re-checked before it was: the escape reproduces, twice, and the mechanism is `PWD` -- but the LOCATION recorded above is wrong, and the same finding is what corrects it. `git log --diff-filter=A` puts `a.txt` and `b.txt` at the repository ROOT, not in `packages/adapter-cli`. That is what `PWD` predicts and cwd does not: vitest's cwd was the package directory while the `PWD` inherited from `pnpm` named the root, and the files followed `PWD`. opencode resolves its file tools against `$PWD` rather than against its cwd — with `PWD` aimed at a decoy directory its own `write` tool call carried the decoy's absolute path — while `INIT_CWD`, the suspect this entry named, was RULED OUT by pointing it at a SECOND decoy that stayed empty through every run. `packages/adapter-cli/src/node-child-spawner.ts` now hands every child a `PWD` equal to the cwd it is given, and the same measurement then shows opencode confined, twice. `packages/adapter-cli/test/confinement-live.test.ts` keeps it true and was shown to go red with the correction removed. The two residue files this entry describes were still committed at the repository ROOT (`a.txt` = `1`, `b.txt` = `2`, from `cfad464`); they are deleted.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4a-the-workspace-is-a-boundary-or-panda-says-it-is-not.md`
  summary: NARROWED, not closed — panda makes the workspace TRUE for an executor, it does not ENFORCE it. Every measured executor confines, and none of them is prevented from writing an absolute path outside the workspace if it decides to.
  evidence: What M4.A established is that each of the three shipped executors resolves a RELATIVE write against a location panda now controls: claude against its cwd, opencode against the `PWD` panda sets to match it, codex not at all (it ships read-only). The absolute-path case is no longer unmeasured: told to create a file at an ABSOLUTE path outside its workspace, `claude` DID, without hesitating -- panda runs it with `--dangerously-skip-permissions` and spawns an ordinary child with the user's own privileges, and nothing sits between them. `codex` under `-s workspace-write` refused (it answered that it cannot write outside the permitted workspace), but that is codex's own sandbox rather than panda's, and it is off in the mode panda ships. The spec's Ask-First list puts OS-level sandboxing (containers, jails, seccomp, restricted tokens, filesystem namespaces) behind a human decision, so the sentence FR-19's concurrency stories must be written against is "isolated for a workspace-relative write" -- not "isolated". Stated on a surface as well as here: `packages/adapter-cli/README.md`.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4a-the-workspace-is-a-boundary-or-panda-says-it-is-not.md`
  summary: panda ships `codex` in its default `read-only` sandbox, so the shipped codex adapter cannot write a file at all — including into the workspace it was handed.
  evidence: Measured against codex-cli 0.149.1 with panda's own argv (`exec --json --skip-git-repo-check`): asked to create a file, codex answered "Unable to create the file because filesystem write access is denied" and produced nothing, twice. Adding `-s workspace-write` — which the confinement measurement does and panda does not — it wrote the file into the workspace through `apply_patch`. So the confinement answer for codex is real but the SHIPPED capability is narrower than the other two, and `panda run --executor codex` is a coding agent that cannot edit code. Deliberately not changed here: the shipped sandbox mode is vendor configuration, which this story's Ask-First list reserves for a human, and it is a user-facing promise about what an executor may do rather than a patch to a confinement measurement. But it is no longer only a ledger note: `packages/adapter-cli/README.md` states it in a per-executor table, and `test/confinement-live.test.ts` GUARDS it -- the codex case runs the shipped argv and asserts the workspace comes back empty after a completed turn, so the day codex ships a writable default the case goes red and the verdict gets re-measured.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4a-the-workspace-is-a-boundary-or-panda-says-it-is-not.md`
  summary: codex's shell tool cannot launch the Store-installed `pwsh.exe` on the Windows machine this was measured on. Its shell works otherwise; an earlier version of this entry said the shell could not run at all, which is false.
  evidence: `codex_core::tools::router: exec_command failed ... CreateProcessAsUserW failed: 5 (Acceso denegado) | cmd="C:\Program Files\WindowsApps\Microsoft.PowerShell_7.6.5.0_x64__8wekyb3d8bbwe\pwsh.exe"`. codex sandboxes a shell command by spawning it under a restricted token, and the Store-installed `pwsh.exe` execution alias refuses to launch under one. Not panda's defect and not codex's confinement: codex picked the cwd correctly (the error prints `cwd=<workspace>`). What fails is only that ONE interpreter -- codex runs `cmd.exe` commands on the same machine, and across runs it chose `cmd.exe` sometimes and `apply_patch` other times, so which tool a given run uses is a model choice rather than machine behaviour. Worth knowing because a codex run here silently loses its PowerShell path.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4a-the-workspace-is-a-boundary-or-panda-says-it-is-not.md`
  summary: Two isolated workspaces are not two isolated executors -- `HOME` passes through untouched, so per-user executor state is SHARED by every concurrent session. This is the FR-19 collision that happens on every run, not only a hostile one.
  evidence: Measured while confining the filesystem side, on the machine this story ran on: `~/.local/share/opencode/opencode.db` is ONE 522 MB SQLite file per user (plus `-shm`/`-wal`), and `~/.claude/projects` holds 292 directories whose most recent entries are named after the temp workspaces this story created and deleted -- `C--Users-angua-AppData-Local-Temp-panda-confinement-<id>-ws-claude-code-<id>` -- so the state outlives the workspace it describes. `~/.codex/sessions` keeps rollouts the same way. panda changes exactly one environment variable and `HOME` is deliberately not it: scrubbing `HOME` would break all three executors, and this story's Never list forbids scrubbing a variable an executor legitimately needs. NOT caused here -- `panda run` does the same, and so does every live suite in this package -- but it is the sentence Epic 4 has to design against: two concurrent sessions are isolated in their FILES and share their executor state, and unlike the absolute-path case this happens on every ordinary run. This suite cannot see it either: `escapeSites()` probes `homedir()` for the marker file at its top level only.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4a-the-workspace-is-a-boundary-or-panda-says-it-is-not.md`
  summary: A UNC workspace reached through the win32 `.cmd` reroute would INVERT the fix's own invariant -- `cmd.exe` refuses a UNC cwd and falls back to `C:\Windows` while `PWD` still names the workspace.
  evidence: Not reachable today and recorded rather than fixed: all three shipped binaries resolve to `.exe` on this machine, so `routesThroughCmdShim` is false for every one of them. It becomes reachable the moment someone installs `opencode` through npm (`opencode.cmd`) AND puts a workspace on a network share -- which Epic 4's managed worktrees make less exotic than it sounds. The failure is quiet in the wrong direction: an executor honouring `process.cwd()` would write into `C:\Windows` while `PWD` said otherwise, i.e. exactly the disagreement `resolve(options.cwd)` exists to prevent, arriving from the other side. A guard belongs with whoever makes worktrees, not in a story about environment variables.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4a-the-workspace-is-a-boundary-or-panda-says-it-is-not.md`
  summary: `test/confinement-live.test.ts` is green on CI while measuring zero executors, and always will be -- none of the three binaries exists on `ubuntu-latest`.
  evidence: Simulated: all four live cases skip on `ENOENT`, the deterministic ones pass, exit 0. The skip reasons print only under `--reporter=verbose`, and inside `pnpm -r test` the skips blend into the package's existing ones. Mitigated rather than solved: the file's last case PRINTS `[M4.A confinement] measured 0 of 3 executors -- NOTHING about executor confinement was verified by this run` with each skip reason, which the default reporter shows. Deliberately a printed line and not an assertion, because failing CI for the absence of three proprietary CLIs would make the gate unrunnable there. The real fix is a job that has the binaries and credentials; until then the honest statement is that panda's half of the finding is defended everywhere and the executors' half only on a developer machine with three authenticated CLIs.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: Materialisation is transactional for WRITES and not for REMOVALS. Every write records the bytes it displaced, so a failure halfway through a tree rolls back exactly; the removals that follow are not undone.
  evidence: Marked `ponytail:` on `land()` in `packages/projection/src/materialise.ts`, and it fails in the safe direction: removals run last, the target fails, no ledger write happens, so the entries panda did not finish removing stay CLAIMED and the next run completes the job. Proven for the write half by "leaves no partial tree behind when a write fails halfway through one" in `test/materialise.test.ts`, verified red with the rollback removed. Upgrade path: a staging directory per target, which buys real transactionality at the price of copying every tree twice — worth it only if a partial removal ever becomes observable.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: A whole skill tree is read into memory before it is written: `materialiseTarget` holds every source file of one entry at once so that an unreadable source is reported with nothing partially materialised.
  evidence: Marked as the deliberate ordering in `packages/projection/src/materialise.ts`. Measured shape of the input: a skill is a `SKILL.md` plus references, kilobytes in practice; the developer’s own 85 skills are all in that range. The ceiling is a skill carrying a large binary asset. Upgrade path: stream per file and accept a narrower guarantee (a partially written ENTRY becomes possible), or hash-then-copy in two passes — neither is worth it until a skill exists that needs it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: [DETAIL-DRIFT 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `panda doctor` reports a skills root as `out-of-date` at ROOT level, naming the executor and the directory, never which skills account for it.
  evidence: The same shape, and the same cause, as the existing file-level `out-of-date` entry for config targets: `ProjectionResult` carries `written` and `byteDelta` and no channel for the entries behind the difference, and correction-01 forbids doctor computing anything the writing path did not. It is narrower here than for a config file, because a skills root can be `out-of-date` for a REMOVAL as well as a write and the wording does not distinguish them. Closes with the same engine change: the merge surfaces its planned entries alongside `written`, and doctor projects them.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: A skills root panda materialises into is also read by the executor from OTHER roots, and two of the three executors ignore an injected home for some of them. CORRECTED after review round 2: the counts and one of the locations were wrong.
  evidence: Re-measured. Under a fresh injected home `opencode debug skill` listed 39 skills — 26 from the real `~/.agents/skills` and 12 from the real `~/.claude/skills`; with `CODEX_HOME` injected and cwd off the home, `codex debug prompt-input` listed 32 — 5 from the injected home (its own `.system` bundle, which it MATERIALISES there) and 27 from the real `~/.agents/skills`. The round-1 note said codex read "one bundled `.system` skill" from `~/.codex`; both halves were wrong. Claude honours the redirect for skills. These are READS, and `~/.agents/skills` is now covered by `test/real-skills-roots.ts` alongside the three panda writes to, so a live check that started touching it would be caught. It still matters for the product rather than the suite: a user whose skill id exists in two roots gets a resolution panda does not control, which is stated in `packages/projection/README.md`.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: `@panda/environment`’s re-export closure is still not total, and now misses one more callback type: `ExecutorProfile.createSkillsTarget` returns a `ProjectionMaterialiseTarget`, beside the existing `StaleLockBreak` and `ProjectionConfigTarget` gaps.
  evidence: Narrows the identical 2.7a entry above rather than replacing it: the reason is unchanged (all three are reachable only by a consumer implementing one of those callbacks; the ordinary path needs none of them) and the comment in `packages/environment/src/index.ts` now names all three instead of two. Widen when a consumer actually implements one.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: The real-roots snapshot is scoped to the SKILLS roots, and the vendor binaries write elsewhere under their own homes on every live run.
  evidence: Measured for codex, reproducibly and despite an injected home: `cache/codex_apps_tools`, `cache/codex_apps_server_info`, `tmp/arg0` and `models_cache.json` under the real `~/.codex`. The skills root itself is untouched and this story's Never clause holds, but `snapshotRealSkillsRoots` is structurally blind to it. NOT widened to those directories, deliberately: they change between runs for reasons that are not panda's doing, so asserting on them would make the gate fail for something no code here controls. What WAS widened is `~/.agents/skills`, which two of the three executors read and nothing writes. The honest statement is in the helper's own header. A real fix is a per-run filesystem watch over the executor homes, which belongs to whoever owns the live-check harness.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: RESOLVED (Story M4.C), and the first closure of it was WRONG in a way execution refuted. `panda remediate adopt` claims panda's own orphaned tree back — including a tree that is only PARTLY there, which the first shipped shape refused.
  evidence: CORRECTED after review. The first closure said "the crash window is unchanged and is no longer terminal", which was false as generalised: `land()` writes file by file and rolls back only inside a `catch`, so a process KILLED mid-write leaves a partial tree that no rollback runs for — and the first `adopt` refused to claim a partial tree, so that shape had no exit at all. Three routes reached it (a `release` on an `edited` tree, a kill inside `land()`, a user deleting one file from a skill folder), and in every one `adopt` refused, `release` was spent or refused, `repair` reported nothing, `discard` did not apply and `panda init` refused — `rm -rf` by hand, rebuilt out of the new vocabulary. What is true now: a partial tree is claimed as the SUBSET that exists, so the record reads `intact` and the ordinary run writes the missing files back; and an entry that has left the registry is claimed from the ledger's own record, after which the ordinary run REMOVES the tree. All three routes are walked end to end in `packages/projection/test/remediate.test.ts` ("a tree that is only partly there is left, not trapped"). What remains open and is NOT closed by this: `land()` is still not crash-atomic — it is transactional for writes only through its own `catch`, which a `SIGKILL` does not run. That half stays under the removal-transactionality entry above, whose upgrade path (a staging directory per target) is the same one.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: Panda's registry id is not the id two of the three executors show. Claude Code names a skill by its DIRECTORY; codex and opencode name it by the source's frontmatter `name:`.
  evidence: Measured against all three binaries. A registry entry whose id differs from its source's frontmatter therefore projects a skill the executor calls something else, while panda's ledger, `init` and `doctor` all name the registry id — so a user reading both sees two names for one thing. The live fixture hard-codes them equal, so the suite cannot surface it; that is recorded rather than hidden. Deciding it is a design call with three options that are not equivalent (rewrite the frontmatter on copy, which breaks "panda copies and never authors"; reject a mismatch at registration; or report both names), so it is not taken here. Stated in `packages/projection/README.md` so a user meets it before a support thread does.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: Two panda PROCESSES materialising into the SAME root can still lose a claim, and the in-process serialisation that makes distinct scopes safe does not help.
  evidence: Narrows the Story 2.8 cross-process entry rather than repeating it: `LEDGER_QUEUES` keys on the resolved ledger path, which was verified this round to hold across 8 parallel runs over distinct scopes with no claim lost, and a cross-process lock is still the missing mechanism. What materialisation adds is that the lost claim is now a lost DELETE authority as well as a lost write authority — the safe direction (panda refuses to remove what it cannot prove it owns), but it means a raced removal is silently deferred to a later run rather than reported. Same upgrade path: a leaf lock package with its own error codes.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: Atomic writes leave `.tmp` files behind when a write is interrupted, and nothing sweeps them; inside a materialised tree one is enough to keep a directory alive after a removal.
  evidence: `atomicWriteText`/`atomicWriteBytes` create `<name>.<uuid>.tmp` in the target directory and unlink it on a failed write, but a killed process leaves it. In a config file that is litter; in a skills root it changes an outcome, because `rmdir` correctly refuses the directory and panda reports the tree as partially removed rather than gone. Deliberately not swept: a janitor that deletes files matching a pattern panda chose is a delete path with no ledger behind it, which is exactly what this story exists to avoid. The upgrade path is a temp directory panda owns outright, per target, which is the same mechanism the removal-transactionality entry above needs.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: A zero-byte `SKILL.md` is accepted and materialised, and the `PANDA_PROJECTION_LEDGER_UNAVAILABLE` code is used for a dropped record as well as for an unreadable ledger.
  evidence: Two small honesty gaps found in review round 2 and left as they are. The first: panda validates that a source tree CONTAINS a `SKILL.md`, never that it contains anything — validating skill content would mean panda deciding what a valid skill is, which its own Boundaries reserve. The executor is the authority and reports it as an empty skill. The second: the warning code is right about the class (panda's ownership record is not fully usable) and coarse about the degree, so a user cannot tell "one record was dropped" from "the whole file is unreadable" by code alone — the detail text says which, and a new code is a contracts change that should come with the other ledger vocabulary work rather than alone.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md`
  summary: [DETAIL-DRIFT 2026-09-04 — see the RE-MEASUREMENT at the end of this file] On Windows the live suites cannot always delete their own sandbox, because the vendor binaries still hold handles when the test finishes.
  evidence: Contained rather than fixed: every `afterAll` uses `rm(..., { force: true, maxRetries: 3 })` and swallows the failure, so a locked temp directory never fails a suite that already produced its answer — the same treatment `codex-strict-config.live.test.ts` has needed since Story 2.8. The residue is under the OS temp directory, never in the repository or the user's home, and the real-roots snapshot covers the directories that would matter. A deterministic fix needs the binaries to exit their child processes, which is not panda's to arrange.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: There is no `restore` verb. "Take panda's version back" is `panda remediate adopt` followed by `panda init` — two commands, with a window in between where panda's ledger claims bytes panda did not write.
  evidence: Deliberate, and the alternative was rejected on evidence rather than taste. A verb that rendered one registry entry straight into a vendor file would be a SECOND renderer beside `mergeNative`, which is the exact divergence correction-01 and `panda doctor` exist to remove — and scoping the ordinary projection to one entry is not possible without wrapping a target, which is the same second path wearing a different hat. Measured consequence of the shipped shape: after `adopt` on an `edited` entry, `stillPandas` matches and the next `panda init` upserts the registry's rendering (pinned by "re-claims an entry the user edited, so the next projection takes panda's version back"). The residual cost is the intermediate state, which the adopt description names out loud. Upgrade path: a verb only becomes worth it if the two-step is measured to be a real product problem.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: RESOLVED (review round 2) — `discard` has the same read-write race check the engine's config path has. The argument for omitting it was false on BOTH halves and is recorded here rather than quietly dropped.
  evidence: The omission was justified as "the failing case cannot be BUILT, because the read and the write are inside one call and nothing deterministic can interleave". A reviewer built it: after the read, `atomicWriteText` performs six further filesystem round-trips (`lstat`, `mkdir`, `stat`, `writeFile`, `chmod`, `rename`), and a competing write anywhere in that window is clobbered — reproduced with `applied: true`, no refusal, and the competing save gone. The scope half was wrong too: Claude Code writes `~/.claude/settings.json` itself when settings change, so one of the three files IS vendor-rewritten. The guard is back, and its case is in `packages/projection/test/remediate.test.ts` ("loses no competing write") — the competing write is chained off a read of the same file so it is issued inside the window, and the attempt is repeated because the scheduler owns the interleaving; the LOOP is what makes the assertion deterministic rather than the race. STANDING LESSON, which is why this entry is kept rather than deleted: a declared omission is better than a decorative guard, and an omission justified by a premise nobody tried to falsify is worse than either.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: `repair` on a WHOLLY unreadable ledger keeps nothing — it replaces the document with an empty one, and every entry panda ever wrote then reports as a foreign collision that has to be adopted back one at a time.
  evidence: Deliberate and stated in the preview before it runs ("panda cannot read any of this file and will REPLACE it ... every entry it has written anywhere reports as a foreign collision"), which is the whole reason the description is computed by the code that acts. There is no salvage pass over a document panda cannot parse, because a partial parse of an ownership record is exactly the authority M4.B's review refused to hand the delete path. What IS salvaged is the readable case: a document that parses with N malformed RECORDS keeps every record panda can read and drops only those. Upgrade path: a recovery that reconstructs claims by hashing what is at each known native location — which is `adopt` run in bulk, and bulk is Ask-First in this story's frozen block.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: The correction-01 C6 scan recognises exactly the two shapes correction-01 names — a `$.panda` root key in the JSON family and a `# BEGIN panda-managed` block in TOML — at exactly three machine-scope locations, and nothing else.
  evidence: Bounded on purpose. Those three files and those two shapes are what the invalidated builds actually wrote (`~/.claude/settings.json`, `~/.codex/config.toml`, `~/.config/opencode/opencode.json`), and a scan that guessed more broadly would be panda deciding that a key it did not write is its own — which is the same failure as adopting foreign content, on the one verb that DELETES from a vendor file. No project-scope location is scanned, and that is a measurement rather than an omission: `panda project init` arrives in Story 2.7a, after correction-01, so no project file can hold one. A block panda finds and cannot bound (an unbalanced marker pair, a `panda` key declared twice) is REPORTED with the reason and left exactly where it is.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: NOT A GAP — measured. A legacy block is only reported for a DETECTED executor, and for all three executors the legacy file's own existence already satisfies detection, so the case this entry worried about cannot occur.
  evidence: Corrected after review, which caught it as vacuous. Checked against the shipped profiles: codex's legacy file IS `~/.codex/config.toml`, an evidence path; opencode's IS `~/.config/opencode/opencode.json`, an evidence path; and claude's `~/.claude/settings.json` cannot exist without `~/.claude`, which is an evidence path. A machine holding a legacy block is therefore always a machine where that executor is detected. What DOES stay unreported, deliberately, is a legacy file panda cannot READ: its only resolution would be a removal panda could not then perform, which is `panda doctor`'s own Never clause, and two of the three files already report the read failure as `target-failed`.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: `ProjectionLedgerRecord.contentHash` no longer means only "the text panda WROTE"; after an adoption it means "the text panda OWNS". The field's own comment still says the stricter thing for the write path.
  evidence: This is the ownership-transfer decision, said plainly rather than hidden in a hash. Nothing downstream distinguishes the two, and nothing should: every predicate that consumes the field asks "is this still the content panda is responsible for", which is true of both. What the widening costs is that a reader of the ledger can no longer infer that panda authored a region — an inference nothing in the codebase makes today. `panda remediate adopt` is the only writer of an adopted record, it is explicit, per finding and user-named, and the claim it writes is limited to the paths the TARGET would itself write. Upgrade path if the distinction is ever needed: an `adopted: true` flag on the record, which is additive in the same way `ownedPaths` was.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: A remediation's PREVIEW and its ACT are two separate invocations with no handle between them, so what a user consented to is not bound to what runs.
  evidence: `expectPreviewEqualsAct` proves the two MODES agree on a static filesystem; it cannot prove the described act is the act performed, because `panda remediate adopt` and `panda remediate adopt --apply` are two processes. Measured consequences, both real: a second user-written file entering the tree between the two makes the act claim a path the description never named; and a ledger that becomes unreadable between them turns "drop 1 record" into "orphan every claim". Contained rather than closed — the ACT always reports what it really did, in its own `changes`, and the repair case's own row pins that the two details differ rather than the act staying quiet ("reports the destructive branch in its OWN output when the ledger moved between calls"). The in-call window IS closed: `repair` reads inside the ledger queue, and `adopt`/`release` write one entry through `updateEntry` instead of replacing a scope from an earlier read. Upgrade path: a receipt on the preview — a hash of the described changes that the act must match or refuse — which is one mechanism serving all four verbs.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: `Diagnosis` gained a top-level `legacy: []`, present on every run including a clean machine.
  evidence: Flagged rather than left for a consumer to discover. It is always present, like `targets` and `skills`, because a conditional key reads to a JSON consumer as a field panda decided to say nothing about — the reasoning `TargetProjection.error` already follows in the other direction. Any script parsing `panda doctor`'s JSON sees a new field; nothing existing changed shape or order. The CLI's `remediate` payload deliberately does NOT carry the diagnosis, so the field appears on one command only.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: `panda doctor` now READS `~/.claude/settings.json`, a file panda had never opened before, on every machine-scope run where claude-code is detected.
  evidence: Recorded because it widens what a read-only command touches, which is the kind of thing a user of a read-only command has a right to know. It is a read: the writes-nothing byte/mtime snapshot covers the whole scope and is unmodified and still green, including on a fixture holding a legacy block. The file is opened through the `discard` remediation's own inspection mode, so doctor gains no second code path for it, and a file panda cannot read is not reported at all.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: An `outside-panda` exit is a SENTENCE, and nothing asserts the sentence is true, reachable, or backed by anything panda ships.
  evidence: Narrowed, not closed. Two cheap clauses landed: a `command` exit may only name a command from a pinned set (`panda frobnicate` used to pass), and a detail must be more than whitespace. What stays open is the shape: any future hard state can satisfy the totality proof by being classified `outside-panda` with a plausible sentence, and no mechanism would catch it. A mechanism that proved an English sentence true is not something worth building; what the proof rests on instead is that `DRIFT_KINDS` may never be `outside-panda`, which IS asserted and derived. Revisit if a second kind is ever reclassified INTO `outside-panda`, which is the move this would have to catch.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4c-every-state-panda-reports-has-a-way-out.md`
  summary: `WARNING_KIND` is a `Partial<Record<PandaErrorCode, …>>`, so a projection warning from a new source silently acquires the generic `projection-warning` kind — whose exit is an `outside-panda` shrug.
  evidence: Deliberate and unchanged from Story 2.7b, where the fallback was introduced precisely so a new warning source could not ship wearing the ledger's resolution text. What M4.C adds is that the fallback now also carries an EXIT, and the exit says panda resolves none of it — accurate for a code panda does not recognise, and the honest answer for one it does not. The gap is that nothing signals when the fallback is taken. Closes with the second warning source, which is also what the existing "no fixture for `projection-warning`" entry waits for.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: `--ext <json>` for the `extensions` namespace is deliberately omitted from `panda add`, so a provider payload can only reach the registry through the SDK.
  evidence: Named in the story's own Design Notes and unchanged by implementation: no consumer reads a CLI-supplied extension payload yet, so the flag would ship a surface with nothing behind it. Measured consequence, which is why it is recorded rather than assumed harmless: `extensions` is the one root key the contract accepts for every entry type, so the only entry shape the binding cannot express is the one a vendor would use — a third party wiring `RegistryStore.register` through `@panda/environment` still can. Upgrade path: the flag arrives with the first consumer that reads an extension payload, and its value has to be parsed and rejected coded rather than passed through as text.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: `REGISTRY_PATH_FIELDS` now carries TWO meanings — which envelope fields belong to each entry type, and which of those are normalized as paths — and `registryEntryIssues` rejects a well-formed field belonging to another type.
  evidence: The widening that makes matrix row 5 true without a per-type table in the CLI (the story's frozen Ask-First clause forbids the table by name; see Spec Change Log 0). It is safe TODAY because every optional root key panda has is a path field for some type, so the two readings coincide exactly: tool->command, skill->entryPath, mcp-server->command/args, profile->none. It fails closed rather than open — an envelope field added with no path meaning is rejected for every type until it is listed, which is a red suite and not a silent acceptance — and that ceiling is stated in the record's own header. Two measured consequences: a persisted store document holding an ill-fitting entry from an earlier build now fails to READ coded instead of being served (no such entry exists in any fixture in this repository, checked before the change), and `@panda/registry`'s ingest path tightened by the same rule, so a tool provider handing back a `tool` carrying an `entryPath` is now refused at ingest. Upgrade path: a separate `REGISTRY_TYPE_FIELDS` record the moment a non-path envelope field exists.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: [STALE-FALSE 2026-09-04 — see the RE-MEASUREMENT at the end of this file] The printed-command invariant cannot see a command whose VERB is interpolated, and would read an all-lowercase prose sentence written inside backticks as a command.
  evidence: Both ceilings are stated in `packages/cli/test/printed-commands.test.ts`'s own header rather than discovered later, and both fail in the safe direction. A `` `panda ${verb} ...` `` holds a value, not a string in the source, so nothing static can dispatch it — it is skipped, and the one place the codebase writes one is a usage-error sentence whose verb came from argv the dispatcher had already accepted. The second is a FALSE FAILURE risk, not a hole: a sentence backticked as though it were a command is itself the defect the story is about, so being forced to rephrase it is the right outcome. What is genuinely NOT covered is `FINDING_EXITS[kind].command`, whose value is a plain quoted string — that stays pinned by a hand-written set in `packages/environment/test/remediate.test.ts`, because `@panda/environment` may not import the CLI (its own guard test) and so cannot dispatch anything. Upgrade path: none worth building; the exit DETAIL beside every such command is backticked and is dispatched, which is where a fabrication would land.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: `RegistryStore.list()` gained an optional scope argument, so the public store surface now has two list modes as well as two get modes.
  evidence: Additive and mirrored on the `get(type, id, scope?)` shape that already existed, for the same reason: the merged view keeps one row per `type:id` and therefore drops the scope that produced it, which is exactly the fact `panda list` has to print. Every existing caller passes no argument and is unchanged (`runScope` reads the merged view, which is right for a projection). What is NOT closed is that `panda list` at the machine grammar can only see the global scope, because the machine store has no project directory — the two-scope listing is `panda project list [directory]`, and going looking for other projects panda has bound is something `panda doctor` already refuses to do. Recorded as an amendment to matrix row 11 rather than a gap.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: A registry document holding ONE entry that violates the canonical envelope makes the WHOLE store unreadable, and the only exit is hand-editing `~/.panda/registry.json`.
  evidence: Determined by execution, as the open question Amendment 4 recorded against the per-type field-fit change. With one `{type:'tool',id:'legacy',entryPath:'./x'}` beside two healthy entries, `panda list`, `panda remove tool legacy`, `panda add` and `panda init` all exit 2 with PANDA_REGISTRY_STORE_UNAVAILABLE -- including, pointedly, the command whose job is removing an entry. `panda doctor` is the one command that still answers, reporting `registry-unreadable` with the `outside-panda` exit "Repair or remove that document". NOT INTRODUCED BY M4.D, and that was checked rather than assumed: a violation that predates it (`id: 42`) fails identically, because the whole-store refusal is `RegistryStore#readStore`'s generic behaviour for any envelope violation and is deliberate -- serving a partially-parsed registry would delete every entry panda could not read from every vendor file. What M4.D changed is only which documents can reach it. So this is a pre-existing state with no in-product exit, which is M4.C's whole subject, and it is REPORTED here rather than fixed because building a remediation was explicitly held for a decision. The shape it would take is `repair`'s, which already exists for the ledger: describe the entries it would drop, drop only those, touch no vendor file. Removing the entry by hand restores the store (verified).
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: [DETAIL-DRIFT 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `panda add`'s reported next step is derived from the planner; `panda remove`'s is still a sentence written beside the command.
  evidence: Deliberate scope line, named so the asymmetry is not mistaken for an oversight. `add` now composes its next step through `deliveryFor`, which asks every planned target whether it would take the entry, so it can say NOTHING TAKES IT HERE and name the scope that does -- the dead end Amendment 4 found. `remove` still prints "`panda init` takes it out of every executor panda wrote it into", which is weaker than derived: on a machine where nothing ever projected the entry, no executor is taken out of anything and the sentence is vacuous rather than false. It was left alone because the correction for a removal is a different question -- what panda OWNS at each location, which is the ledger's answer and not the planner's -- and widening the change to reach it was out of the scope this round was given. Upgrade path: `deliveryFor` gains a ledger-backed sibling, or `remove` reports the ledger records that would be released.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: `deliveryFor` asks a materialise target to `plan`, which reads the skill's SOURCE tree, so `panda add skill` now performs a filesystem read it did not before.
  evidence: The cost of making the message derived rather than authored, stated rather than hidden. `plan` never touches the projection DESTINATION -- that is the materialise contract's whole safety split -- but it does `stat`, and for a directory source `readdir`, the path the user just named in `--entry-path`. Two measured consequences, both judged acceptable: `panda add skill x --entry-path ./typo` now reports that nothing would be materialised, at the moment the entry is created rather than at the next `panda init`, which is more honest and not less; and a large skill tree is walked once for a message. Config targets add no read at all -- they are asked to merge into `nativeText: ''`, which the three shipped ones do without importing `node:fs`. Upgrade path if the walk ever matters: the materialise contract grows a cheaper "would you answer for this entry" question that stops before enumerating files.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: One full-gate run failed a single unnamed assertion in `@panda/registry` and could not be reproduced.
  evidence: Kept rather than dropped now that it was chased. `pnpm --filter @panda/registry test` was run 10 times in a row: 10/10 green, 61 passed each time. The original failure's log was lost, so the case cannot be named; every full-gate run before and after it is green. The package's timing-sensitive files (`lock.test.ts`, `contention.test.ts`) run alongside `@panda/adapter-cli`'s twelve parallel files under `pnpm -r`, which is the only load difference between the full gate and the loop. Left as an unidentified transient with the loop recorded, rather than as four green runs.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: [DETAIL-DRIFT 2026-09-04 — see the RE-MEASUREMENT at the end of this file] CORRECTION of an earlier entry in this ledger: the interpolated-verb ceiling of the printed-command invariant was NOT harmless, and calling it harmless is what let three bypasses through.
  evidence: The earlier entry said a command whose verb is interpolated 'is skipped; the verb it holds is a value, not a string in the source' and that the ceiling 'fails closed'. Refuted by execution. The mechanism did not skip only interpolated verbs: an unrecognised TOKEN ANYWHERE made the classifier continue past the whole string, verb included, so a single-quoted argument -- this repository's dominant style -- hid a fabricated verb, and 43 backticked strings were dropped in silence while a '> 20' corpus count went on passing. Three bypasses were built, run, and left all 84 tests green: a fabricated `panda purge 'demo'` planted in `panda add`'s own success output; `panda remove --all`, whose verb dispatches and whose flag the binary rejects; and `packages/cli/bin/panda.ts`, which ships, prints, and was never scanned. What replaced it inverts the rule: every backticked `panda ...` string is a COMMAND and is dispatched -- verb AND flags -- unless it is listed by hand as prose, and a listed string that stops appearing fails too. Six interpolated sites remain, five of them touched by this story; they are now LISTED rather than silently dropped, which is the difference. Their verbs are still unassertable when the verb itself is the interpolation, and that is the residual ceiling -- stated in the test's own header, and no longer described as harmless.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: A `SKILL.md` with no YAML frontmatter materialises and reports success, and codex silently ignores it.
  evidence: Verified in both directions by the spec author against `codex-cli 0.149.1`: `panda init` exits 0 with `"written": true`, the file lands at the root codex reads, and codex does not list the skill. Panda's own validation is deliberately shallow -- the skills target checks that a tree CONTAINS a `SKILL.md`, never that it contains anything, because validating skill content would mean panda deciding what a valid skill is, which its Boundaries reserve to the executor. That reasoning still holds; what it did not anticipate is that the executor's rejection is SILENT, so 'panda reports success' and 'the executor uses it' come apart with nothing in between. Same class as this story's own defect -- a promise kept syntactically and broken in substance -- one layer down. NOT FIXED HERE: what materialisation writes is M4.B's, and the shape of a fix (parse the frontmatter and report a skill no executor will load) is a validation decision that belongs with that story rather than being smuggled in through the CLI.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] A permanently inert project-scope entry is warned about ONCE, at creation, and is then invisible to every diagnostic.
  evidence: Measured: `panda add skill x` at project scope prints NOTHING TAKES IT HERE, and after that neither `panda doctor` nor `panda project doctor` reports it, while the identical entry at machine scope IS an `unprojectable` finding. The asymmetry has a cause: `unprojectable` findings are derived from a projection run's `skippedEntryIds`, and at project scope no skills target is planned at all, so no target answers for the entry and nothing is skipped -- there is no row for doctor to phrase. The warning at creation is therefore the ONLY time a user is told, and a user who registers the entry from the SDK is never told. Not fixed here because the fix belongs to whichever story decides the open T4 question (give codex a verified project-scope skills root, or have `panda project init` report skills as unprojectable the way the machine scope does); patching doctor alone would add a third answer to a question two code paths already disagree about.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: [DETAIL-DRIFT 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `panda add` silently overwrites an existing entry AND strips its `panda.source` stamp, after which that provider's whole catalog stops updating.
  evidence: `RegistryStore.register` is an upsert by `type:id` and the CLI passes the entry straight through, so `panda add tool rg --command mine` replaces a `tool` an ingest provider owns and drops the `extensions` namespace that carried its origin. The consequence is not local: `ingestProviders` refuses to touch an entry whose origin does not match, so the next run of that provider fails `PANDA_REGISTRY_ORIGIN_CONFLICT` and its ENTIRE catalog stops updating -- one hand edit, silently, from a command whose output says 'registered'. Recorded rather than fixed because the right shape is a decision, not a patch: refuse an overwrite unless a flag says so, preserve `extensions` across an overwrite, or report the replacement and what it displaced. The first changes an existing store API's contract, the second silently keeps a stamp the user did not ask for, and the third is the only one that is purely additive.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: `add` accepts an `mcp-server` with no `command`, and the only exit it names is `panda remove`.
  evidence: Such an entry is permanently unprojectable -- every config target reports it in `skippedEntryIds` because there is nothing to render -- and the derived message says NOTHING TAKES IT HERE with 'no target said why', because `skippedEntryIds` carries ids alone and no target supplies a reason. So the user is told the entry reaches nobody and is offered removal, never the one thing that would fix it: supply `--command`. The CLI cannot say it without holding a per-type table of required fields, which is the frozen Ask First; the honest place for it is the contract, beside the per-type field FIT rule this story already added there. Related and left alone for the same reason: a skill whose `entryPath` names a nonexistent file is severity `info`, so `panda doctor` exits 0 over an entry that can never materialise.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: Four low-severity mutation survivors in the new CLI surface, left as they are.
  evidence: Reported by the reviewers and judged not worth a test each: `remove` silently accepting field options (the guard exists and refuses, but nothing asserts the refusal, so deleting it would go unnoticed); the `--flag=value` spelling of `--command`/`--entry-path`/`--arg` (implemented and unasserted, so the inline branch could be deleted silently); the CLI's own entry-type check passing for the wrong reason (a mutation that always returned the first known type still satisfied the rows, because every row that names a type names a valid one); and matrix row 1's weak oracle (it asserts the stderr CONTAINS the scope, the type and the id rather than the sentence, so a reordering would pass). Each is a coverage gap rather than a defect, none of them can produce a wrong write, and the surface they cover is asserted end to end by the rows that do exist. Upgrade path: one row per item, cheapest first, if this surface grows.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: One full-gate run failed a single live confinement assertion in `@panda/adapter-cli`, and it did not reproduce.
  evidence: Recorded alongside the earlier unidentified `@panda/registry` transient rather than folded into it, because this one WAS captured: `test/confinement-live.test.ts > keeps two concurrent opencode sessions in two workspaces apart` failed because the real opencode agent asked its write tool for a POSIX-spelled `/tmp/panda-confinement-.../ws-concurrent-a-.../...txt` while its workspace is the Windows path `C:\Users\...\Temp\panda-confinement-...`, and opencode then auto-rejected its own request as `external_directory`. The sibling session in the same run used the Windows spelling and passed. So the variable is the live model's choice of path spelling, not panda's confinement: the assertion compares the path the AGENT names against the workspace prefix. Re-running the package immediately was green (148 passed, 6 skipped), as was the next full gate. Nothing in this story touches `@panda/adapter-cli`. Upgrade path, for whoever owns the live harness: compare the RESOLVED path rather than the string the agent typed.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: RESOLVED, and the resolution refutes the reasoning in the entry it replaces: the HIGH-5 race test never had to be a race, and the loop that was called deterministic was a wager.
  evidence: The M4.C entry that restored this guard said its case 'is in `packages/projection/test/remediate.test.ts` ("loses no competing write") -- the competing write is chained off a read of the same file so it is issued inside the window, and the attempt is repeated because the scheduler owns the interleaving; the LOOP is what makes the assertion deterministic rather than the race.' That last clause is false, and CI proved it: `84c62e2` went red on the Node 26 job with 'the race was never won in 25 attempts'. Measured before changing anything -- `0144745` green on the same canary, `84c62e2` red twice, the test green 12/12 isolated on Node 24 and 5/5 on Node 26, and a probe throwing from `occupiedByContent` failing eight remediate tests without this one among them -- so the cause was scheduling moved by M4.D's two new `materialise.test.ts` rows, not a source change on that path. The deeper fact the original construction missed: between `statSnapshot` and the guard, `discardLegacy` performs NO await, so the window was one microtask against two filesystem round-trips. It replaces with `test/remediate-race.test.ts`, which fires the competing write FROM the remediation's own snapshot `stat` through a file-scoped `node:fs/promises` wrapper -- no production seam, the guard unmocked and unbypassed, the refusal and the survival of the competing bytes both still required. STANDING LESSON, which is why this is kept rather than deleted: a test that must win a race to pass is a test that will fail on some machine someday, and 'the loop makes it deterministic' is the sentence that hides it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4d-nothing-panda-prints-is-a-command-panda-does-not-have.md`
  summary: The window between `readIfPresent` and `statSnapshot` is NOT covered by the read-write guard, and the old racing test could silently absorb a real loss in it.
  evidence: Found while replacing that test, and left as it is. `discardLegacy` reads the file, THEN snapshots it; a competing write landing between those two calls is captured by the snapshot, so `hasFileChangedSince` correctly reports no change -- while `text`, and therefore the computed `next`, are stale. Panda then writes the old content minus the block over the competing write. The guard is not wrong: it defends the window it was built for (snapshot to write), which is the one the reviewer built the failing case in. What is newly visible is that the old test could land in this EARLIER window, lose the competing write for real, count the attempt as 'not refused' and simply retry -- so a genuine loss was indistinguishable from a lost race. The new test cannot hide it, because its precondition is forced and asserted. NOT FIXED here: closing it means snapshotting before the read and comparing after, or reading and stat-ing under one handle, and both change the shape of a function this task was told not to redesign. Upgrade path: take the snapshot BEFORE `readIfPresent` and re-check it after, which makes the guarded window cover the read as well.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4e-the-registry-declares-only-what-panda-can-deliver.md`
  summary: `panda init` neither reports nor counts a RETIRED entry; only `panda doctor` and `panda list` show one.
  evidence: `runScope` now returns the retired rows and `diagnose` turns each into a `retired-type` finding, but `toInitResult` does not carry them, so `panda init` runs clean over a registry holding a word panda no longer has and says nothing -- exactly the asymmetry the M4.D entry above records for a project-scope skill. `entryCount` counts them (it is `entries.length`, the entries the diagnosis READ), while `targets[].unprojectable` does not (they never reach a target), so the two numbers in one payload answer for different populations. Left alone deliberately: `panda init` is the converging command and a retired entry is not something it can converge -- panda will not delete a registry entry by itself -- so the honest fix is either a new reporting field on `InitResult` (a payload contract change, for a state `doctor` already reports in full) or teaching `init` to print a doctor-shaped warning, which is a second code path answering a question `diagnose` already answers. Upgrade path: add `retired` to `InitResult` alongside `skipped`, phrased from the same `ScopeReport.retired` this story added, whenever a caller needs it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4e-the-registry-declares-only-what-panda-can-deliver.md`
  summary: The retired vocabulary is a permanent one-way ratchet with no expiry, and nothing measures when a retired word is safe to delete outright.
  evidence: `RETIRED_PATH_FIELDS` is the one place a retired word and its old fields live, and every reader derives from it -- but nothing ever REMOVES an entry from it, so the read path will admit `tool` forever. That is correct today (a registry written by any shipped build may still hold one) and it is not free later: each retired word is a permanent branch in the envelope, in `pathFieldsFor`, in `panda remove`'s accepted list and in the synopsis `panda remove <...|tool> <id>` a user reads. Not solved here because the deciding fact does not exist yet: panda has no store-format version bump, no telemetry and no migration record, so there is no evidence for when the last `tool` entry in the world is gone. Upgrade path: when the store document gains a version bump that rewrites entries, a retired word can be dropped from `RETIRED_PATH_FIELDS` in the same change that proves no readable document can still carry it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4e-the-registry-declares-only-what-panda-can-deliver.md`
  summary: [STALE-FALSE 2026-09-04 — see the RE-MEASUREMENT at the end of this file] The `retired-type` finding is still the longest thing `panda doctor` prints, and it restates itself.
  evidence: Measured at review: 907 of 925 characters against 724 for `edited`, the next longest. This story's item-2 fix cut it — the concrete command moved out of the detail and into the exit sentence, so the rendered line now states the fact once and the command once instead of interpolating the same `panda remove tool rg` twice — but the shape that makes it long is structural and untouched: every finding renders `detail - RESOLUTION[kind] - exitSentence(kind)`, and all three of those legitimately have something to say about a retired type. Two residual costs are recorded rather than fixed. The line still opens with an apparent self-contradiction ("panda no longer declares that type" ... "To leave this state: `panda remove tool rg`") that is only reconciled by the exit's own detail, several hundred characters later; and the actionable command still lands past the first 150 characters, after the fact and the resolution. NOT fixed here because the fix is not local to this kind: shortening one kind's three slots while ten others keep the same shape trades a long line for an inconsistent report, and the honest change is a rendering decision for `panda doctor` as a whole -- put every finding's exit FIRST, or render `detail` and `resolution` on separate lines. Upgrade path: change `finding()`'s composition once, for every kind, and re-measure all eleven.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4e-the-registry-declares-only-what-panda-can-deliver.md`
  summary: `panda list` renders a retired entry identically to a live one, though the product already has a vocabulary for inert.
  evidence: `describeEntry` prints `global - tool - rg (command rg)`, which is the same shape as `global - mcp-server - ctx (...)`, so the one row in the list that no command will ever deliver is indistinguishable from the ones that will. `panda add` already says NOTHING TAKES IT HERE for an entry no target would take, and `panda doctor` reports the retired row as a problem -- so the concept and the words for it both exist, and `list` alone is silent. Left as it is deliberately: `list` is the inventory command and its contract is one line per stored row, so annotating a subset means deciding what `list` is allowed to editorialise about -- which also reaches the `mcp-server` with no command and the `skill` whose `entryPath` does not exist, both equally undeliverable and both printed plain today. Upgrade path: one derived suffix on rows the planner reports as undeliverable, applied to all three cases at once rather than to the retired type alone.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4e-the-registry-declares-only-what-panda-can-deliver.md`
  summary: [DETAIL-DRIFT 2026-09-04 — see the RE-MEASUREMENT at the end of this file] The store's whole-store rejection names three valid words in a document whose retired row it just accepted.
  evidence: `registryEntryIssues(value, admitRetired)` widens the accepted vocabulary but keeps one message: `'type' must be one of: skill, mcp-server, profile`. So a document holding a genuinely unknown type beside an accepted `tool` row is refused with a sentence that lists neither the word that was accepted nor the fact that a retired one would have been. It is not wrong -- those are the types panda declares, and no user should be told to write `tool` -- but it is incomplete exactly where a reader is trying to work out why one retired row parsed and their hand-edit did not. Not fixed because the alternative is a message whose content depends on `admitRetired`, i.e. two sentences for one rule, and the read path's message is consumed inside a `PANDA_REGISTRY_STORE_UNAVAILABLE` detail that already names the offending index. Upgrade path: append `(plus the retired types this build still reads: ...)` under `admitRetired` only, derived from `RETIRED_ENTRY_TYPES`.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4e-the-registry-declares-only-what-panda-can-deliver.md`
  summary: [DETAIL-DRIFT 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `--help` shows different type lists for `add` and `remove` and never says which word is the retired one.
  evidence: The synopsis reads `panda add <skill|mcp-server|profile>` and `panda remove <skill|mcp-server|profile|tool>`. The asymmetry is deliberate and is now pinned by a test, but nothing in the help text explains it, so a reader's most available conclusion is that one of the two lines is stale -- the precise misreading Spec Change Log 1 records as a real defect in the other direction. The `remove` paragraph says a retired type is accepted; it does not name `tool` as that type, because naming it there would be a second place the retired vocabulary is written down. Not fixed because the honest version interpolates `RETIRED_ENTRY_TYPES` into the prose, which is cheap but adds a fourth derived string to a block that is read as a whole; recorded so the next reader of that synopsis knows the asymmetry is intended. Upgrade path: one derived clause on the `remove` paragraph naming the retired words.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4e-the-registry-declares-only-what-panda-can-deliver.md`
  summary: A live test that could not measure anything FAILS the gate, which is the typed-absence rule panda enforces on `doctor` and not on its own suite.
  evidence: Observed during this milestone: `opencode` rate-limited its provider, and two `packages/adapter-cli` confinement rows failed with their own message saying `this run measured nothing`. The suite could not distinguish that from a confinement regression, so the gate went red for an outcome that carries no information about panda. This is the same distinction AD-5 makes and `panda doctor` is built around -- `permitsWrite` returns three values precisely so `panda could not determine` is never reported as `panda cannot write` -- applied to everything except the tests that assert it. Recorded, not built: a third test outcome is a harness design decision (vitest has `skip` but a row that skips itself on a provider error can also skip a real failure, which is strictly worse than a red one), and it reaches every live suite in the repository rather than anything this story touched. Upgrade path: decide once whether an unavailable live dependency skips with a recorded reason or fails, then apply that ruling to every `*.live.test.ts` at the same time.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4e-the-registry-declares-only-what-panda-can-deliver.md`
  summary: Cross-package source mutations are invisible inside a git worktree, so mutation results taken there can be false negatives.
  evidence: Reported by the reviewers and reproduced during this review round: `packages/*/node_modules` hold junctions into the main checkout with RELATIVE `@panda/*` links, so a worktree's own `packages/contracts/src` is not what a worktree's `packages/cli` suite resolves. A mutation planted in one package therefore reads as `not a derivation` in another package's suite for a reason that has nothing to do with the code. One reviewer worked around it with a worktree-local `resolve.alias` in the four downstream `vitest.config.ts` files and re-baselined. NOT applied to the shipped configs here: the aliases are correct only inside a worktree, the main checkout resolves correctly through the existing `panda-source` condition (measured in this round -- a probe importing `REGISTRY_ENTRY_TYPES` through `@panda/environment`, through `@panda/contracts` and through `USAGE` returned the same reordered value from all three), and committing a workaround for a harness the repository does not itself create would make every future reader wonder which resolution is real. Upgrade path, for whoever owns the review harness: make the worktree setup script write the alias, or stop using junctions for `@panda/*`.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4f-profile-is-a-selection-not-an-entry.md`
  summary: The `no native representation for a <type> entry` sentence is now reachable at PROJECT scope only, and nothing says so where it is written.
  evidence: `reasonUnprojectable` in `packages/environment/src/init.ts` produces that sentence for any entry a target was handed and could not render. Before this story its machine-scope witness was `profile`, which no executor expresses. After the retirement the only DECLARED kind a config target skips is `skill`, and at machine scope every shipped executor has a verified skills root, so `unprojectableFor` drops those ids into the skills row instead (the KNOWN LOSS OF GRANULARITY the function's own comment records). The branch is therefore live at project scope alone, where panda has no verified project-scope skills location. Three test rows moved to `initProject` for that reason. Not a defect and not fixed: the code is correct at both scopes and the machine-scope path is a deliberate hand-off, not dead code. Recorded because a reader auditing `reasonUnprojectable` at machine scope will now find no case that reaches its second branch, and the honest conclusion from that alone is that the branch is dead. Upgrade path: none needed while an executor without a verified skills root can still be added; the branch becomes machine-reachable again the moment one is.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4f-profile-is-a-selection-not-an-entry.md`
  summary: `REGISTRY_PATH_FIELDS` no longer has any type with an empty field list, so `fieldsSentence`'s fieldless wording is reachable only through a RETIRED word.
  evidence: `fieldsSentence` renders "a '<type>' entry carries no field beyond 'type' and 'id'" when `pathFieldsFor` returns `[]`. `profile` was the only declared type that did; both survivors carry fields. The branch stays live because `RETIRED_PATH_FIELDS.profile` is `[]` and the READ path validates a retired entry with the retired type's own allowlist, so `{type:'profile', id:'p', command:'x'}` still gets that exact sentence -- asserted in `packages/contracts/test/registry.test.ts`. Left alone: deleting the branch would be wrong (a retired fieldless word reaches it), and it costs nothing. Recorded so nobody prunes it as unreachable after grepping only the declared vocabulary.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4f-profile-is-a-selection-not-an-entry.md`
  summary: The retired half of the vocabulary now equals the declared half, and `panda remove`'s synopsis is the first place a user sees it.
  evidence: `panda remove <skill|mcp-server|tool|profile> <id>` -- two words the product declares and two it only reads, in one derived list, with nothing in the help text saying which is which. M4.E already recorded that asymmetry as unexplained prose; this story is the first time the retired side is not a single word, so the reading 'four entry types' is now the most available one and it is wrong. Also newly load-bearing: M4.E's ledger entry calling `RETIRED_PATH_FIELDS` a permanent one-way ratchet with no expiry now carries weight rather than describing a single member. Not fixed here for M4.E's own reason -- naming the retired words in the prose puts the retired vocabulary in a second place -- and because the deciding fact for dropping a word (a store-format version bump that proves no readable document still carries it) still does not exist. Upgrade path unchanged: one derived clause on the `remove` paragraph, interpolating `RETIRED_ENTRY_TYPES`.
- source_spec: `_bmad-output/implementation-artifacts/spec-m4f-profile-is-a-selection-not-an-entry.md`
  summary: The full gate could not run to completion: `@panda/adapter-cli`'s live opencode rows fail on a PROVIDER refusal, reproducibly.
  evidence: Two rows of `packages/adapter-cli/test/confinement-live.test.ts` fail with the opencode API answering HTTP 403 `{"type":"DataPolicyError","message":"This model collects data used to improve its quality and requires explicit opt in: https://opencode.ai/workspace/<id>/go"}` from `https://opencode.ai/zen/go/v1/responses`; the concurrent row additionally reports `database is locked` on its second session. Reproduced on a second isolated run of that file alone (2 failed | 8 passed). Not this story's: `@panda/adapter-cli` imports nothing from `@panda/contracts`' registry module -- its only occurrences of the word `registry` are two prose comments -- and every other package is green on Node 24.14.1 and Node 26.8.1. It is the same class as the M4.E ledger entry 'a live test that could not measure anything FAILS the gate': the row's own message says it measured nothing, and the suite cannot tell that from a confinement regression. Recorded as an occurrence, not a new problem; the upgrade path is M4.E's, unchanged. Consequence for this story's evidence: `pnpm check` aborts at that package, so the remaining suites were run individually as the repo's own instructions prescribe.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5a-the-methodplugin-contract-is-published-or-it-is-not.md`
  summary: [EXPIRED-REASON 2026-09-04 — see the RE-MEASUREMENT at the end of this file] The two MethodPlugin lifecycle hooks take NO argument, so a method cannot reach panda from inside its own activation.
  evidence: `MethodActivateHook` and `MethodDeactivateHook` are `() => void | Promise<void>`. A methodology author can therefore do its own I/O on mount and nothing else -- no kernel, no registry, no config, no workspace handle. That is deliberate and it is the only choice this story could make honestly: whatever panda hands a method on activation IS the swap surface, and FR-28 / Story 5.4 owns it. Designing a `MethodContext` here would decide 5.4's central question inside the story explicitly told not to pre-empt it, and a context object shipped in a published contract is the hardest kind to change afterwards (NFR-8 versions every Contract together, so widening it later is a major bump of all seven). Recorded rather than built. Upgrade path: 5.4 adds one parameter to both hook types in the same change that decides what a swap passes; adding a parameter to a zero-arg function type leaves every existing implementation valid, which is precisely why starting at zero is cheaper than starting at a guess.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5a-the-methodplugin-contract-is-published-or-it-is-not.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `PANDA_METHOD_HOOK_FAILED` is ONE code for both hooks, and Story 5.4's swap is the caller most likely to need them apart.
  evidence: The kernel splits its three budget codes because a log record's shape is closed and has nowhere to carry which cap fired. That reasoning does not reach here: a thrown `PandaError` carries `cause` and a message naming both the method and the hook (`method 'tdd' failed in 'onActivate': ...`), so a consumer can already tell mount from unmount without a second constant, and one code is the smaller published surface. What is foreseeable and not yet needed: an ordered swap has to branch on WHICH half failed -- an outgoing `onDeactivate` that throws leaves the previous method half-unmounted while the incoming one has not started, and an incoming `onActivate` that throws leaves nothing mounted at all, and those are different recoveries. Deciding that now would be designing 5.4's failure policy from outside it. Upgrade path: split into `methodActivationFailed` / `methodDeactivationFailed` in the same change that writes the swap's rollback, or keep one code and branch on the hook name the message already carries.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5a-the-methodplugin-contract-is-published-or-it-is-not.md`
  summary: Collection items inside a MethodPlugin reject unknown keys with NO `extensions` hatch of their own -- one level stricter than the registry envelope this contract copied.
  evidence: The matrix's row 3 names the manifest ROOT, and `registry.ts` guards only its root because a registry entry has no nested objects to guard. A MethodPlugin has three collections, and `{ id: 'red', descripton: 'typo' }` on a phase is the exact "kept syntactically, broken in substance" failure this milestone keeps shipping -- so `phases[i]`, `artifacts[i]` and `commands[i]` are strict too, and a vendor payload has exactly one home, the root's reserved `extensions`. The cost, recorded because it is a real one: a third party who wants to annotate a single command cannot do it in place; the whole annotation set has to live at the root, keyed back to the command id by hand. That may prove awkward the first time a real methodology needs per-command vendor data. Upgrade path: give collection items their own `extensions` namespace, which is purely additive (a key that was rejected becomes accepted) and can be done at any time without a major bump.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5a-the-methodplugin-contract-is-published-or-it-is-not.md`
  summary: The semver pattern now exists TWICE -- `packages/kernel/src/manifest.ts` and `packages/contracts/src/method.ts` -- and only a test stops the copies drifting.
  evidence: AD-1 forbids the kernel a runtime dependency on anything, `@panda/contracts` included, so `PluginManifest.version` and `MethodPlugin.version` cannot share one predicate. Both files carry the recommended semver.org regular expression verbatim and both say so in a comment naming the other. The guard is `packages/contracts/test/method.test.ts` -> "agrees with the kernel copy of the pattern on every string in the corpus", which runs an 18-string corpus (`1.2`, `v1.0.0`, `latest`, `01.0.0`, `^1.0.0`, `1.0.0.0`, `1.0.0-`, prerelease and build spellings, ...) through `isSemver` and through the kernel's `validateManifest` and requires the same verdict from both. That test lives in `@panda/contracts`, which already devDepends on `@panda/kernel` for the error-code parity suite, so the direction of the dependency is the one AD-1 permits. Not a defect and nothing to fix; recorded so nobody "deduplicates" it by making the kernel import the contract, which is the one change AD-1 forbids outright. Upgrade path: none while AD-1 stands. If a third consumer ever needs the same rule, it imports `isSemver` / `SEMVER_PATTERN` from `@panda/contracts` rather than adding a fourth copy.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5a-the-methodplugin-contract-is-published-or-it-is-not.md`
  summary: M4.D's "nothing panda prints is a command panda does not have" guard scans `.ts` only, so documentation can name a verb the binary does not have.
  evidence: Found by the guard doing its job: the first full run went red with "'panda swap method' in packages/contracts/src/method.ts names 'panda swap'" -- a doc comment explaining why the hooks take no argument had spelled Story 5.4's verb in backticks, in shipped source. Fixed by naming the story instead. What the failure also revealed is the boundary: `shippedFiles` in `packages/cli/test/printed-commands.test.ts` recurses `src/` and `bin/` collecting `entry.name.endsWith('.ts')`, so `packages/contracts/METHOD-PLUGIN.md` names the swap verb twice with nothing to stop it, and so could any README. Today that is correct in substance -- both mentions are explicitly scoped as "not in this contract, it is FR-28 / Story 5.4" -- and it is exactly the kind of promise that rots into a lie when the story slips or the verb is spelled differently. NOT fixed here: widening the scan to `.md` is a decision about what a doc is allowed to say about the future, and it would immediately have to classify every `panda ...` string in seven READMEs, which is a change to that test's contract rather than to this story's. Upgrade path: extend `shippedFiles` to `.md` under a second NOT_A_COMMAND-style list for documented-but-unbuilt verbs, all seven READMEs classified in one pass.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5a-the-methodplugin-contract-is-published-or-it-is-not.md`
  summary: Another occurrence of the `@panda/adapter-cli` live opencode failure that stops `pnpm check` reaching the rest of the repository.
  evidence: Same shape as the M4.E and M4.F ledger entries, same two rows of `packages/adapter-cli/test/confinement-live.test.ts`, and this time the refusal is a data-policy consent rather than a rate limit: HTTP 403 `{"type":"DataPolicyError","message":"This model collects data used to improve its quality and requires explicit opt in: https://opencode.ai/workspace/<id>/go"}` from `https://opencode.ai/zen/go/v1/responses`. One row then asserts "neither concurrent opencode session asked to write anything, so this run measured nothing about isolation" -- the suite's own words for an outcome that carries no information about panda, failing the gate as if it were a confinement regression. Nothing in this story touches `@panda/adapter-cli`; excluding the live file that package is 136 passed / 8 skipped on Node 26.8.1, and every other package is green on both Node 24.14.1 and Node 26.8.1. Recorded as an occurrence, not a new problem. Upgrade path unchanged from M4.E: decide once whether an unavailable live dependency skips with a recorded reason or fails, then apply that ruling to every `*.live.test.ts` at the same time.
- source_spec: `_bmad-output/implementation-artifacts/spec-m6a-parallel-work-is-infrastructure.md`
  summary: The Registry mirror half of Story 4.1's acceptance does NOT ship with the provider; the on-disk record ships alone as the creation-of-record.
  evidence: The acceptance says the on-disk ownership record and the Registry mirror "commit in one serialized transaction". AD-2 forbids that edge: measured from the manifests, `@panda/workspace-local` depends on exactly `@panda/contracts` + `@panda/kernel`, and `@panda/registry` depends on the same two -- they are siblings, and the topology is strictly downward (IMPL -> CONTRACTS), so a workspace provider may not import the registry in either direction. This is the same wall the doctor/`resolveExecutor` entry above records. FR-18's own wording settles which half is which: the on-disk record is the CREATION-OF-RECORD and the Registry entry is a MIRROR, i.e. derived state, which AD-4 assigns to its owning component. So the provider owns and ships the record, and the mirror belongs to a consumer-tier caller holding both. Not a gap in the provider: `acquire()` answers from the record alone and a directory without one is classified external, which is the substance of the acceptance. Upgrade path: either the consumer that mounts this provider (Story 4.2, which is where "which provider does a session use" is decided) writes the mirror inside its own serialized write, or -- if the transaction must be one -- `@panda/contracts` publishes a narrow mirror port the provider calls and the consumer implements, which is downward and therefore legal. Do NOT add the port before a consumer exists; it would be an interface with no implementation.
- source_spec: `_bmad-output/implementation-artifacts/spec-m6a-parallel-work-is-infrastructure.md`
  summary: The ordinal reservation queue is in-process only, so two panda PROCESSES over one state directory can still read the same `nextOrdinal`.
  evidence: `LEDGER_QUEUES` in `packages/workspace-git-worktree/src/ledger.ts` is module-level and keyed by resolved state directory -- deliberately, mirroring `ProjectionLedger`, because an instance-level queue would let two providers over one directory both reserve the same ordinal. That case IS closed and pinned by "gives concurrent creates on separate instances distinct names", which was verified to fail against an instance-level queue. What stays open is the cross-process case. Measured consequence, and it is bounded and non-destructive: both processes read the same ordinal, both compute the same path, and the second `git worktree add` fails on an existing directory, so the outcome is a coded `PANDA_CONTRACT_WORKSPACE_UNAVAILABLE` rather than two trees sharing a name -- git itself is the backstop. Upgrade path is the one already recorded for the projection ledger, unchanged: a leaf lock package with its own error codes, which both ledgers then use.
- source_spec: `_bmad-output/implementation-artifacts/spec-m6a-parallel-work-is-infrastructure.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] The frozen spec's task T7 (a kernel plugin for this provider) was dropped during implementation rather than built.
  evidence: `packages/workspace-local/src/plugin.ts` provides the service `workspace` and reads the configuration key `workspace`. A second plugin providing the same service is `PANDA_KERNEL_SERVICE_CONFLICT`, so the two providers are ALTERNATIVES that cannot be co-mounted, and shipping a plugin here would mean deciding -- inside a story about ownership records -- how a user selects between them and what configuration key expresses that choice. Nothing mounts this provider today: `@panda/session` depends on `@panda/workspace-local`, and the story that introduces a second workspace consumer is 4.2. Panda ships as an SDK first, so an exported provider class is a usable deliverable on its own. Not a shortcut and not an omission from the acceptance -- Story 4.1's criteria name creation, ownership records, name retirement and external classification, none of which need a plugin. Upgrade path: Story 4.2 adds the plugin together with the selection it implies, in the story that actually has to answer it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m6a-parallel-work-is-infrastructure.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] NEGATIVE RESULT, recorded because it is a real finding about a package this story did not touch -- `LocalWorkspaceProvider.acquire` does not guard against a non-string id.
  evidence: `acquire(id: string)` is typed, but a JavaScript caller can pass anything, and `WORKSPACE_ID_PATTERN.test(id)` COERCES: `test(null)` tests the string `"null"`, which matches, so the call proceeds to `join(rootDir, 'null')` and lstats a path the caller never named. `GitWorktreeWorkspaceProvider` guards it (`typeof id !== 'string'` before the pattern), which means the two providers of one port now differ on this point. Deliberately NOT fixed here: `@panda/workspace-local` is outside this story's frozen scope and its provider is shipped and reviewed. Consequence is small -- a coded `PANDA_CONTRACT_WORKSPACE_UNKNOWN_ID` in almost every case, because no such directory exists -- but the divergence is the part worth recording, since a reader comparing the two providers will otherwise read it as an accident. Upgrade path: move the id guard into a shared helper in `@panda/contracts` (downward, legal for both) so one spelling serves every provider of the port, or add the same two-token guard to `workspace-local`.
- source_spec: `_bmad-output/implementation-artifacts/spec-m6a-parallel-work-is-infrastructure.md`
  summary: `WorktreeRecord.version` is written and never checked on read.
  evidence: `isRecordShape` in `packages/workspace-git-worktree/src/ledger.ts` validates `id`, `ordinal`, `path`, `repoPath` and `createdAt`, and ignores `version`. A record written by some future version 2 of this provider therefore reads as valid here rather than as something this build cannot interpret. Recorded rather than fixed because the honest guard is not `version === 1`: refusing every future record makes the field a downgrade trap, and the useful rule (which shape changes are compatible) cannot be written before a second shape exists. The field is present so that rule CAN be written later, which is the whole reason to emit it now. Upgrade path: decide the compatibility rule when the second record shape is introduced, the way `LOG_RECORD_VERSION` carries the kernel's.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5b-every-rule-the-contract-states-is-enforced.md`
  summary: `MethodArtifact.path` rejects a backslash and a leading `<letter>:` outright, so two characters that are legal in a POSIX filename cannot be declared.
  evidence: `isProjectRelativePath` in `packages/contracts/src/method.ts` rejects any `path` containing `\`, and METHOD-PLUGIN.md states the cost in the author's own terms. The reason is that `docs\plan.md` is a nested file on Windows and a single flat filename on POSIX -- one manifest, two meanings, decided by whoever reads it, which is the "kept syntactically, broken in substance" failure this contract exists to prevent. Translating the separator instead was rejected: panda validates `path` and stores it verbatim, and rewriting an author's declared value is a materialisation decision that no materialiser exists to make yet. The cost is real and bounded: a backslash is a legal character in a POSIX filename, and a methodology that genuinely produces one cannot declare it. Measured likelihood: no such artifact exists in this repository or in any of the three executors' own layouts. The SAME cost applies to the `/^[A-Za-z]:/` rule, which rejects `a:b/c.md` -- a colon is legal in a POSIX filename, but a single letter followed by a colon is indistinguishable from a Windows drive spec, and the ambiguity is what is being refused, not the character. Both are one decision seen twice. Upgrade path: if a real methodology ever needs either, add an escape (a doubled backslash meaning one literal backslash inside a segment) rather than accepting a bare one, which keeps the single-meaning property.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5b-every-rule-the-contract-states-is-enforced.md`
  summary: NEGATIVE RESULT -- `MethodPlugin.version` is deliberately NOT constrained at the type level, and the measurement that disqualified it is recorded so nobody "improves" it later.
  evidence: The obvious spelling is a template literal type, and it is wrong in BOTH directions. Measured with `tsc --noEmit --strict` against a nine-string corpus with `'1.0.0'` as the control: `` `${number}.${number}.${number}` `` MATCHES `01.0.0`, `-1.0.0` and `1e3.0.0`, all of which real semver rejects, and REJECTS `1.0.0-rc.1` and `1.0.0+build.5`, both of which real semver accepts. The second direction is the disqualifying one -- it would break the build of an author publishing a legitimate release candidate, and a contract that punishes correct usage is worse than one that catches nothing. A recursive template type handling prerelease and build identifiers could close the second direction but not the first: `${number}` cannot express "no leading zeros, no sign, no exponent". So semver stays a runtime rule, `isSemver` is exported so an author can check early, and METHOD-PLUGIN.md carries a compiler-versus-validator table saying which rules fall on which side rather than leaving the author to discover the line. Nothing to fix. Upgrade path: none that is an improvement; if TypeScript ever gains a pattern-constrained string type, revisit with the same corpus.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5b-every-rule-the-contract-states-is-enforced.md`
  summary: OCCURRENCE, in a package this story did not touch -- `packages/projection/test/skills-discovery.live.test.ts` fails on Windows at HEAD, and its own accounting invariant is what breaks.
  evidence: Measured at `087e357` with this story STASHED, so it is not caused by M5.B: 2 failed / 246 passed / 3 skipped, identical before and after. The aggregate line it prints is `[M4.B skills] measured 2 of 3 executors -- the remaining cases did not report a reason`, and the failing assertion is `expect(measured.length + notMeasured.length).toBe(3)`. That is the interesting half: the suite was built so a run that measured nothing says so, and here one executor fell into NEITHER bucket -- it neither answered nor recorded a reason, which is the "a zero without a control" hole the mechanism exists to close, inside the mechanism. CI is green on both jobs at this SHA, so the divergence is local/Windows, not universal. Same family as the M4.E / M4.F / M5.A occurrences, different file and different shape: those were a provider REFUSING with a reason, this one is a case that vanished silently. Not fixed here -- `@panda/projection` is outside this story's frozen scope. Upgrade path: give the third bucket a name, so an executor that neither answers nor refuses is reported as unaccounted rather than subtracted from the total; then apply the M4.E ruling (unavailable live dependency skips with a recorded reason) to every `*.live.test.ts` at once.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5c-panda-writes-the-selection-it-tells-you-to-edit.md`
  summary: The config writer lives in `@panda/projection` rather than beside the config READER in `@panda/session` or in the `@panda/environment` facade that publishes it, and none of the three is an obviously right home.
  evidence: The frozen spec put it in `@panda/environment`, reasoning from the manifest: environment declares `@panda/projection`, so AD-2 permits the import. `packages/environment/test/guard.test.ts` refused it, with two clauses the spec's measurement never read -- environment may import only `access`, `constants`, `mkdir` and `stat` from the filesystem, and its source may not contain the string `atomicWriteText` at all. That second clause exists because a reviewer already reached the writer exactly that way, and the fix was to un-export `atomicWriteText` from the projection index; the spec's plan would have undone it. So the writer moved INTO `@panda/projection`, beside the primitive, and `@panda/environment` re-exports it as the CLI's facade -- the shape it already uses for `createMemoryLogSink`. That needs no new package edge and both clauses pass. What stays unsatisfying: `@panda/projection` is the vendor-file-and-ownership-ledger package, and panda's own configuration document is neither; the reader of that same file lives in `@panda/session`, which cannot reach the symlink-safe writer without a new edge. Upgrade path: a leaf package owning the symlink-resolving atomic write with its own neutral error codes, which `@panda/projection`, `@panda/session` and this writer would all use -- worth doing when a THIRD caller appears, not for two.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5c-panda-writes-the-selection-it-tells-you-to-edit.md`
  summary: NEGATIVE RESULT about a package this story did not change -- `~/.panda/registry.json` has the same dotfile-symlink exposure as `config.json`, and `@panda/registry`'s writer does not resolve links.
  evidence: `packages/session/src/executors.ts` documents that "every dotfile manager (stow, chezmoi, dotbot) materialises panda's config as a symlink", and `~/.panda/registry.json` sits in the same directory under the same scope model (`<home>/.panda/` and `<project>/.panda/`). Measured in `packages/registry/src/store.ts`: it imports `mkdir, readFile, rename, stat, unlink, writeFile` and NOT `lstat` or `realpath`, and its persistence is a plain temp-file-then-rename. So a user who symlinks `registry.json` into a dotfiles repo has the link destroyed and the source orphaned by the first `panda add`, every later edit in the dotfiles repo goes nowhere, and panda exits 0 -- the exact failure `atomic-write.ts` was written to prevent for vendor files. NOT fixed here: `@panda/registry` is outside this story's frozen scope, it is a shipped and reviewed write path with a lock protocol this story has not measured, and the fix is the same one the entry above defers. Upgrade path: the leaf writer package; until then, adding `lstat`/`realpath` resolution to `RegistryStore`'s persist step is a small, self-contained change with its own story.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5c-panda-writes-the-selection-it-tells-you-to-edit.md`
  summary: [EXPIRED-REASON 2026-09-04 — see the RE-MEASUREMENT at the end of this file] NEGATIVE RESULT -- coding the write failure inside `atomicWriteText` looked like a root fix and was reverted, because the root was already handled one level up.
  evidence: A 0o444 target makes `rename` fail on Windows (measured: `EPERM: operation not permitted, rename`, while the temp write succeeds), and the bare errno escaped `atomicWriteText`. Raising a coded `PandaError` there turned `packages/environment/test/doctor.test.ts` red: `toTargetFailure` in `packages/projection/src/engine.ts` wraps a RAW error as `PANDA_PROJECTION_TARGET_FAILED` and passes an existing `PandaError` through unchanged, and `doctor` classifies its `not-writable` finding from that code. So every caller reaching the writer through the projection engine already received a coded failure and the premise behind the "root fix" was false. Reverted; the one caller that does not go through the engine (this story's config writer) codes it at its own boundary as `PANDA_CONFIGURATION_UNUSABLE`, which is also the better vocabulary for a configuration verb. Recorded so the same "fix" is not attempted again: the writer was measured and its caller was not. Nothing to do. Upgrade path: none -- if a second non-engine caller of `atomicWriteText` ever appears, it codes its own boundary the same way.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5c-panda-writes-the-selection-it-tells-you-to-edit.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] BLOCKER FOR STORY 5.4, measured while scoping it -- panda cannot load a MethodPlugin from anywhere, so 5.4's precondition "given two installed methods" is unreachable today.
  evidence: Measured at `3e6f85c`. `panda --help` lists 11 verbs and `swap` was not among them. Dynamic imports across `packages/*/src` -- `await import(`, `import(`, `createRequire`, `require(` -- are **zero**; control: `export function` over the same glob is 81 occurrences in 38 files, so the query works. `createSessionKernel` mounts exactly two plugins, both constructed in process (`createExecutorPlugin`, `createWorkspacePlugin`). `REGISTRY_ENTRY_TYPES` is `skill` and `mcp-server`, and a registry entry is JSON, which cannot carry the `onActivate`/`onDeactivate` FUNCTIONS a `MethodPlugin` is defined by. The PRD is not in conflict with itself by accident: §6.1 puts `swap` inside v1's CLI while §6.2 puts "official methodology plugins (immediately post-v1)" outside it, and §6.1 names the seventh contract as the "MethodPlugin **definition**". Story 5.4 also asks for two things one CLI process cannot honestly do at once -- "outgoing onDeactivate runs fully before incoming onActivate" needs methods MOUNTED (functions in memory) while "the selection persists across processes" crosses a process boundary with nothing mounted. This story ships the persistence half as a general capability. Upgrade path: 5.4 decides where a method comes from BEFORE it decides anything else -- a host-supplied set through the SDK, built-in methods shipped inside panda, or a loader panda does not have -- and its ordering guarantee belongs to the capability package, with the CLI verb persisting a selection and saying so honestly.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5d-a-method-is-a-selection-panda-mounts.md`
  summary: FR-28's "an invalid id exits non-zero listing available methods" CANNOT be met in v1, and the refusal names the load failure instead.
  evidence: There is no installed-methods list and there cannot be one. PRD §6.2 places official methodology plugins post-v1, and D1 removed the store that would have held them -- a method is a SELECTION in `<scope>/.panda/config.json`, not a registry entry, because `packages/contracts/src/registry.ts` defines the declared vocabulary as "every word here reaches an executor ... exactly the two kinds the projection layer renders" and a method reaches none. So `panda swap method <specifier>` LOADS the specifier as its check and refuses with the reason it could not be imported or validated. Measured as strictly more actionable for the audience that exists: `PANDA_METHOD_INVALID_PLUGIN: invalid method plugin: 'version' must be a semver version ...; 'onActivate' is declared without 'onDeactivate'` tells an author which rules they broke, where a list of ids would tell them nothing they did not already know. Filed as a deliberate deviation from the acceptance criterion rather than silently narrowed. Upgrade path: if a methods store ever exists (post-v1, when methodologies ship), the criterion becomes meetable and the refusal gains the list; nothing in this design blocks that -- a store would sit beside the selection, exactly as the registry sits beside the executor selection today.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5d-a-method-is-a-selection-panda-mounts.md`
  summary: v1 "hot swap" is mount/unmount plus a persisted selection, NOT live reloading of changed module code.
  evidence: Story 5.4 is titled "methodology hot-swap" and the phrase invites the module-reload reading. Measured: the only shipped implementation of that is `@cordisjs/plugin-hmr`, which reaches `internal/modules/esm/loader` via `getOrInitializeCascadedLoader()`, gated on `--expose-internals` with a native addon as fallback -- control: without the flag, `Cannot find module 'internal/modules/esm/loader'`. Requiring users to launch panda with a Node flag is the owner's §10 principle inverted, and cordis already maintains `ModuleLoaderV1` and `V2` side by side because Node 24 removed `getModuleJobForImport` and made `resolve` private, so the surface breaks again. NEGATIVE RESULT worth recording: those internals ARE present on Node 24.14.1 and 26.8.1 alike, so the obvious "it will break on our canary" objection was wrong -- the flag is the reason, not the version. A method changed on disk therefore takes effect on the next session, which the help text says in the product rather than only here. Upgrade path: none that is an improvement while the only mechanism needs a process flag. If Node ever exposes a supported module-reload API, the ordering machinery this story shipped (`swapMethod`) is already the half that would consume it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5d-a-method-is-a-selection-panda-mounts.md`
  summary: A BARE package specifier resolves through `createRequire(baseDir)` and falls back to a plain `import()`, which an ESM-only package with no CJS-resolvable entry can still fail.
  evidence: `resolveFrom` in `packages/session/src/methods.ts` resolves a relative specifier against the user's directory (the defect below) and a bare one through `createRequire(join(baseDir, 'panda.method.js')).resolve(specifier)`, so a method installed in the USER's project is found there rather than in panda's own `node_modules`. `require.resolve` honours `exports` maps in modern Node but cannot resolve a package that publishes only an `import` condition; the `catch` falls back to passing the bare specifier through, which then resolves from panda's own location -- correct for a globally-installed method, wrong for a project-local ESM-only one. Not closed here and not tested, because NO methodology is published as a package in v1 (PRD §6.2) so the case has no user: writing a test for it would be pinning a guess. The relative case, which is the one v1 has, is covered and falsified. Upgrade path: when the first packaged methodology exists, replace the fallback with a real ESM resolution -- `import.meta.resolve` gained no parent parameter, so this means either a resolver dependency or the Node internals D4 refuses; decide it against a real package rather than in the abstract.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5d-a-method-is-a-selection-panda-mounts.md`
  summary: REFUSED, with its reasoning, so it is a decision rather than an omission -- panda's kernel will NOT gain a unified `effect()` primitive modelled on cordis's `Fiber.effect()`.
  evidence: The cordis research note found that panda re-derives collect-and-dispose at every call site where cordis has one named primitive that takes a disposer (or an iterable, or an async iterable of them), disposes in reverse, epoch-guards double dispose and builds a nested `EffectMeta` tree. Unifying panda's version is genuinely attractive and was considered as the next story. Refused on the ponytail ladder's first rung: panda's kernel ALREADY delivers every guarantee that primitive delivers -- reverse-order disposal, double-dispose as a no-op, a throwing disposer contained -- so the change buys a NAME, not a behaviour, and it is a refactor of the one package every other package stands on, with zero user-facing change, while eleven stories are open and v1 is unshipped. Upgrade path: revisit when the kernel is next opened for a behavioural reason, so the unification rides along with work that had to touch it anyway rather than being its own risk.
- source_spec: `_bmad-output/implementation-artifacts/spec-m5d-a-method-is-a-selection-panda-mounts.md`
  summary: THE DEFECT THIS STORY SHIPPED AND CAUGHT -- `await import()` resolved against panda's own source directory, so no relative method specifier could ever work, with the whole suite green.
  evidence: `import(specifier)` resolves relative to the module that CALLS it. `resolveMethod` lived in `packages/session/src/methods.ts`, so `panda swap method ./tdd.mjs` searched `C:\code\panda\packages\session\src\tdd.mjs` -- and a relative path is the ordinary way a user names a local method, so the feature was entirely broken for its primary case. Nine tests passed throughout, because every one of them handed `resolveMethod` a `pathToFileURL()` href, which sidesteps module resolution completely. This is the SECOND time in three stories that a harness supplying what the real caller does not has hidden a total failure (M5.C: every test passed `runPanda` a `cwd` the binary never passes), and both were found the same way -- by running the binary as a user. Fixed by `resolveFrom(specifier, baseDir)` and pinned by a test that passes a plain `./local.mjs`. Recorded not as an open item but as evidence for a process rule: a test that constructs its input in a form the product never produces is testing a caller that does not exist, and the only lens that sees it is driving the real thing.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7a-teardown-does-what-the-kernel-says-it-does.md`
  summary: `swap()` is synchronous and does NOT await an async previous disposer; the rejection is contained but the teardown is not waited for.
  evidence: Disposal became awaitable in M7.A (`stop()` and `dispose()` are already async and now `await plugin.disposer?.()`), but `PandaKernel.swap(pluginId, factory): SwapResult` is synchronous. Making it async is a breaking change to a published signature, and it was measured to have ZERO production callers -- `grep -n "\.swap(" packages/*/src` returns nothing, every hit in the repository is inside `packages/kernel/test/lifecycle.test.ts`. So a superseded implementation whose teardown is asynchronous is started and not awaited: the swap commits, the new implementation serves, and the old one may still be flushing. What IS closed is the crash: a thenable previous disposer gets a `.catch()` that records `plugin.disposal-failed`, so a rejection can no longer become an unhandled one. Upgrade path: when the first production caller of `swap()` appears, decide there whether it needs the await -- either `swap()` becomes async, or `SwapResult` carries the settling promise so a caller that cares can await it while one that does not is unaffected. Do not choose in the abstract; the shape depends on what the caller is doing.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7a-teardown-does-what-the-kernel-says-it-does.md`
  summary: The plugin disposer is typed `() => unknown` rather than `() => void | Promise<void>`, deliberately, and the reason is an ergonomics measurement rather than laziness.
  evidence: The precise type was written first and it BROKE existing valid code: `dispose: () => count++` and `dispose: () => map.delete(k)` are legal against `() => void` through TypeScript's void-return bivariance, and stop being legal the moment the return type becomes a union. Two of panda's own tests failed to compile on that change, and a third party's would too -- a breaking change to a published surface that buys nothing, because the kernel `await`s the result either way and awaiting a non-thenable is a no-op tick. `unknown` keeps the bivariance ergonomics, accepts a promise, and the docblock carries the fact that teardown MAY be asynchronous and IS awaited. The cost, stated: an author who returns a value by accident gets no signal -- which is exactly what `() => void` already permitted, so nothing was lost. Upgrade path: none wanted. If TypeScript ever gains a way to say "void-like, or a promise" without losing bivariance, revisit.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7a-teardown-does-what-the-kernel-says-it-does.md`
  summary: NEGATIVE RESULT, recorded so it is not re-derived -- an `unready` plugin never re-activates on a later `start()`, and that is a DELIBERATE decision rather than the gap it looks like.
  evidence: A subagent reading cordis reported this as panda's biggest reactivity gap: cordis's `reflect.ts:207-229` re-checks every fiber when a service arrives and reloads the ones that were blocked, while `packages/kernel/src/lifecycle.ts:338` (`if (runtime.has(id)) continue`) skips the plugin parked as `'unready'` and discards the loader's freshly recomputed readiness verdict. The mechanism is described correctly. The conclusion is wrong: the line directly above it reads "A plugin activates at most once; its failure (if any) is reported once ever", the unready record carries its own comment ("recording it twice would make the stream claim the plugin failed twice"), and `packages/kernel/test/lifecycle.test.ts:412-431` pins the semantics with `second.started` equal to `[]`. Nothing in panda registers a provider after `start()` -- `createSessionKernel` registers both plugins and starts once -- so re-activation has no consumer today and would be built on speculation. Upgrade path: if an SDK host ever needs late registration, the change is ~15 lines in `start()` (skip unless `state === 'unready'`, delete the entry, re-run `runCandidate`) plus a decision about whether the audit stream may report the same plugin's failure twice, which is the actual reason it was written this way.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7a-teardown-does-what-the-kernel-says-it-does.md`
  summary: NEGATIVE RESULT -- panda's `intercept.ts` waterfall and cordis's `waterfall` event mode share a word and nothing else, so neither is a candidate to replace the other.
  evidence: Measured on both sides. Cordis's `waterfall` (`packages/core/src/events.ts:117-132`) composes N INDEPENDENTLY-REGISTERED listeners on one event name into a middleware chain, each receiving `next`, any of them able to short-circuit; registration is open, so any plugin can insert itself into another plugin's chain. Panda's `intercept.ts` is a single-owner, fixed-arity slot machine: `register()` reads `pre/guard/around/post` ONCE off one definition object, `ACTION_STAGES` is a closed vocabulary of four named roles rather than a list of participants, and `registeredIds` forbids a second registration of the same id. Control: chain vocabulary (`next(`, `chain`, `interceptors`, `middlewar`, `.push(`) in `intercept.ts` -> 0 hits, against `invoke` -> 8. The consequence worth knowing: in panda a second plugin CANNOT intercept an action a first plugin declared, and that is a deliberate security posture stated in the `settle` docblock, not an oversight. Multi-party interception was considered and rejected: it reopens the double-`proceed` and slot-ownership problems the pipeline exists to close. Upgrade path: if cross-plugin policy is ever needed, a bus query (a `bail`-style dispatch where a listener can answer) is a twentieth of the cost and does not touch the budget seam.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7b-the-kernel-tells-an-author-everything-it-found.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] REVERSAL of a queued plan -- the Standard Schema issue `path` is still declared away, deliberately, because nothing in panda reads a foreign schema's issues yet.
  evidence: The cordis research note queued "carry the issue `path`" beside M7.B, on the argument that `StandardSchemaIssue` is `{ readonly message: string }` in BOTH copies (`packages/contracts/src/standard-schema.ts` and `packages/kernel/src/manifest.ts`) while a third party plugging Zod or Valibot into `configSchema` produces issues whose `path` is populated -- and panda drops it. The mechanism is correct. The timing is not: NOTHING in panda consumes a third party's schema issues today. The kernel only PROBES `configSchema` with a `CONFIG_PROBE` symbol and never validates real configuration against it, and every panda-authored schema goes through `defineStandardSchema` with the coordinate baked into the message text (`artifacts[0]`, `consumes[2]`), so it produces no `path` to carry. Adding the field now is a widened published type with no reader -- the exact defect already recorded against four kernel exports nothing consumes, which the handoff carries as an open item. Upgrade path: it belongs to M7.C, the story that makes the kernel APPLY `configSchema` and thereby creates the first consumer of a foreign schema's issues. Add the optional `path` and the renderer in that change, not before.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7b-the-kernel-tells-an-author-everything-it-found.md`
  summary: `validateManifest` accumulates issues in a MODULE-SCOPED buffer, which is safe only while it stays synchronous and single-threaded.
  evidence: `collected` is a module-level `string[]`, reset at the top of every `validateManifest` call rather than threaded through `readField`, `collectDuplicateServices`, `collect` and `fail`. That is safe for two reasons that are both currently pinned: the function is synchronous by contract (`is synchronous and never returns a promise` is an existing test) so no two calls interleave, and the kernel validates manifests one at a time. It is recorded because the safety is CONDITIONAL on facts elsewhere: the moment `validateManifest` gains an await, or the kernel validates two manifests concurrently, a shared buffer silently reports one manifest's mistakes on another. The mutation harness confirms the reset is load-bearing -- removing `collected = []` kills 10 tests, including several that pass a VALID manifest and would otherwise inherit a previous rejection's issues. Upgrade path: thread an `issues: string[]` parameter through the five helpers. Not done now because it is five extra parameters for a hazard that two pinned invariants currently exclude, and the note on the constant says exactly when to do it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7b-the-kernel-tells-an-author-everything-it-found.md`
  summary: [STALE-FALSE 2026-09-04 — see the RE-MEASUREMENT at the end of this file] NEGATIVE RESULT about my own tooling -- the exclusion glob `**/*.live.test.ts` does not match `confinement-live.test.ts`, so several sessions of "live excluded" runs were not excluding it.
  evidence: Panda has TWO naming styles for live suites: `packages/projection/test/skills-discovery.live.test.ts` (a dot) and `packages/adapter-cli/test/confinement-live.test.ts` (a dash). Every gate run in this and previous sessions used `--exclude "**/*.live.test.ts"`, which matches only the first, so the adapter-cli live suite ran every time and its documented flake was being read as a real failure needing investigation. Measured: with the corrected glob `**/*live.test.ts`, adapter-cli is 132 passed / 6 skipped; with the live suite included it is 148 passed on one run and 147 passed / 1 failed on another, non-deterministically, with the failure being the recorded "neither concurrent opencode session asked to write anything, so this run measured nothing about isolation". Nothing to fix in the product. Upgrade path: rename one of the two files so a single glob covers both, OR record the corrected glob where the gate is documented -- the second is done (this entry and the M7.B spec's verification section); the first is a rename nobody has asked for.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7c-a-required-field-the-kernel-actually-applies.md`
  summary: RESOLVED (M7.C) -- the M3.B entry above recording `PluginManifest.configSchema` as decorative is closed. The kernel now APPLIES each plugin's schema to `config.resolve()[manifest.id]` before the factory runs, and hands the factory the validated `value` as `ActivationContext.settings`.
  evidence: The proposed fix that entry recorded is what shipped, with two things it left open settled by measurement rather than by choice. "A plugin's own subtree" is `manifest.id`: all three shipped plugins already spelled it that way (`workspace`/`workspace`, `registry`/`registry`, `executor`/`executor`), so the kernel states a convention that existed rather than inventing one, and no `configKey` manifest field was added. And the feared semantic change is narrower than recorded: each plugin's strictness lives in its OWN schema, so the kernel calling that schema preserves that plugin's policy -- the kernel imposes none of its own. The three plugins lost the duplicated ceremony (read `config.resolve()`, pick the subtree, validate, check for a promise, map issues, reject); `registry` and `workspace-local` keep their own merge-then-validate because only a merged value can be checked once it exists.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7c-a-required-field-the-kernel-actually-applies.md`
  summary: A registry subtree that violates its schema is now rejected even when explicit factory options would have overridden the offending key.
  evidence: `createRegistryPlugin(options)` merges explicit options over the config subtree and validates the MERGED candidate, so before M7.C a document with e.g. `registry.lockTimeoutMs: NaN` activated fine whenever the host passed a good `lockTimeoutMs`. Since M7.C the kernel validates the raw subtree first and rejects it, before the merge happens. Deliberate, and the reasoning is panda's own: the document IS invalid either way, and accepting it because a host happened to override that key means the next run WITHOUT those options fails mysteriously with no hint that the document was the problem all along. Measured as theoretical rather than observed: no test covers the combination, and the option path exists for an embedding host rather than for the CLI. Upgrade path: if a real host needs the old tolerance, the honest form is for the registry schema to accept the raw subtree leniently and for the plugin to keep enforcing strictness on the merge -- the same shape `workspace-local` now has -- rather than for the kernel to special-case it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m7c-a-required-field-the-kernel-actually-applies.md`
  summary: NEGATIVE RESULT about my own frozen measurement -- "each plugin's strictness lives in its own schema" was INCOMPLETE for `workspace-local`, and applying it naively would have turned a warning into a failure to start.
  evidence: The spec's measurement 4 checked what `WORKSPACE_CONFIG_SCHEMA` returns and concluded the kernel applying it preserves behaviour. It read the schema and not what value the FACTORY feeds it. The schema returned an issue for a non-record subtree, and the factory never handed it one -- it warned on the bus and passed `{}` instead -- so that strict branch was unreachable dead code. The moment the kernel applies the schema to the RAW subtree, unreachable strictness becomes a plugin that refuses to start where it used to warn, which is exactly the regression `run-session.ts:148` records (one forward-looking key in `~/.panda/config.json` failing every run on the machine). Fixed by making the schema state the plugin's real policy: a non-record subtree yields `{ value: {} }` and the warning stays in the factory, off the raw document. That is a better state than before, because the schema now describes what the code does instead of declaring a rule the code never reached. Caught by driving the binary and confirmed by a stashed BASELINE comparison producing byte-identical output. The generalisable rule: when a validator's caller pre-filters its input, checking the validator alone tells you nothing about what happens once someone else calls it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m8a-the-environment-travels-without-the-secrets.md`
  summary: NEGATIVE RESULT about my own frozen measurement -- "the store on disk is already portable, so a bundle carries those bytes forward" was true about the WRITER and false about what the caller receives.
  evidence: `RegistryStore.register` normalizes machine paths at write time (`normalizeRegistryEntryPaths`, labelled NFR-6 in `packages/contracts/src/registry.ts:339`), and the store document on disk does hold `~/skills/...`. But `list()` maps `expandRegistryEntryPaths` over everything it returns (`packages/registry/src/store.ts:253,267`), so what `createBundle` was handed was `C:\Users\...` again. The first artifact this story produced carried a fully machine-specific absolute path while its spec asserted NFR-6 was already satisfied. Caught by DRIVING THE BINARY and reading the file, not by re-reading either function. Fixed by normalizing inside `createBundle` against the SAME `homeDir` the store was bound to. Same shape as M7.C's `workspace-local` finding one story earlier: reading what a function does is not reading what its caller gets.
- source_spec: `_bmad-output/implementation-artifacts/spec-m8a-the-environment-travels-without-the-secrets.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] A normalized path keeps the platform separator of the machine that wrote it, so a bundle written on Windows carries `~/skills\name.ts` and a Linux import would read the backslash as part of the filename. PRE-EXISTING, not introduced here.
  evidence: Measured in a real store written by the real binary: `.panda/registry.json` holds `"entryPath": "~/skills\commit-lint.ts"` before any export runs. `normalizeRegistryEntryPaths` replaces the home PREFIX and leaves the remainder verbatim by design -- its own comment says only the declared path fields are transformed and the rest stays as written -- and its round trip is lossless ON THE SAME PLATFORM, which is what NFR-6 asks and what `packages/contracts/test/registry.test.ts:195` pins. This story does not touch it: the bundle carries what the store holds, and translating separators inside an export would make the artifact disagree with the registry it came from. The clause added here asserts NFR-6 as it is written ("no machine-specific absolute path persists") rather than pinning one platform's spelling. Upgrade path: Story 5.2 is where it bites, because import is the half that has to place the path on a different platform -- the honest fix is for the normalizer to emit `/` and for the expander to map to the host separator, which is a change to a contract function with a pinned round trip and belongs in the story that needs it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m8a-the-environment-travels-without-the-secrets.md`
  summary: [STALE-FIXED 2026-09-04 — see the RE-MEASUREMENT at the end of this file] The secret detector treats a value of EXACTLY 40 or 64 lowercase hex characters as not-a-credential, so a token of exactly those lengths and that alphabet would travel in a bundle.
  evidence: A git object name is 40 lowercase hex and a sha256 is 64, both plausible in an `mcp-server` argv (`--ref`, `--digest`), and both matched the generic opaque-token rule before they were excluded by exact shape. The trade was measured rather than assumed: with them included the corpus had two false positives (a UUID and a git sha) out of seventeen legitimate values, and a false positive drops a user's entry. A 32-hex token -- the realistic raw-token length -- is still detected, and the boundary is pinned in both directions by the clause "draws its one admitted blind spot where the spec says it does". Upgrade path: none that is an improvement without a second signal; entropy scoring separates neither of these two pairs, and the omission record already makes any decision the detector makes visible to the user rather than silent.
- source_spec: `_bmad-output/implementation-artifacts/spec-m8b-the-environment-arrives-and-says-what-it-could-not-bring.md`
  summary: RESOLVED (M8.B) -- the M8.A entry recording that a normalized path keeps the separator of the machine that wrote it is closed. `normalizePathValue` now emits `/` in the remainder it keeps, so a Registry written on Windows can be read on POSIX.
  evidence: The fix the M8.A entry proposed is what shipped, and the risk it named was measured rather than accepted. On win32, `join('C:\Users\dev', 'skills/name.ts')` returns `'C:\Users\dev\skills\name.ts'`, IDENTICAL to `join('C:\Users\dev', 'skills', 'name.ts')` -- so emitting a forward slash round-trips losslessly on both platforms and the round trip pinned at `packages/contracts/test/registry.test.ts` still holds. It is also backward compatible: a store written by an older build holds `~/skills\name.ts`, and `join` DOES treat a backslash as a separator on the platform that wrote it, so nothing needs migrating. Two clauses that pinned the OLD spelling were updated deliberately (they asserted `join('config', 'server.json')` inside the normalized value, which is the defect written down as an expectation), and one new clause asserts the portable property instead of one platform's. Verified end to end by driving the binary: a bundle exported on Windows now carries `~/skills/commit-lint.ts`.
- source_spec: `_bmad-output/implementation-artifacts/spec-m8b-the-environment-arrives-and-says-what-it-could-not-bring.md`
  summary: A real Windows-to-POSIX import is not tested by anything; what is tested is that the ARTIFACT is separator-neutral.
  evidence: The normalize half is asserted platform-independently (the emitted value contains no backslash, with a control proving the input carried this platform's separator). The expand half is `join`, which is correct by construction on whichever platform runs it, but no test crosses machines -- CI runs Linux only and development is on Windows, so the two halves are each exercised on one platform and never in sequence across two. Not a gap in the code so much as a gap in what a single-platform suite can prove. Upgrade path: a fixture bundle checked into the repo carrying a Windows-written path, imported by the Linux CI job, which would exercise exactly the crossing that matters -- worth doing when a second platform's user actually appears, and cheap when it does.
- source_spec: `_bmad-output/implementation-artifacts/spec-m8b-the-environment-arrives-and-says-what-it-could-not-bring.md`
  summary: `panda import` onto a machine with NO executor installed exits 2, sharing `panda init`'s meaning of that code rather than reporting the install it did perform.
  evidence: Measured on two throwaway homes: `init` and `import` both exit 2 with `no executor configuration was found under any of: ...`, which is `reportInitOutcome` -- one implementation, shared, so the two verbs cannot disagree about one outcome. The entries ARE installed (verified by reading the destination `registry.json`), so a script that reads the exit alone learns "the environment has nothing to project into" and not "the import failed". Deliberate: the alternative is a second exit-code mapping for the same result object, which is the drift M4.D's printed-command invariant exists to prevent in a different form. What is genuinely unsatisfying is that the JSON envelope carries both halves and the exit code carries only the second; a caller that wants the install outcome reads `imported` and `replaced`. Upgrade path: none proposed -- a third exit code for "installed but nothing to project into" would need every existing consumer to learn it, for a machine state `panda doctor` already describes.
- source_spec: `_bmad-output/planning-artifacts/research/deepseek-harness-the-product-layer-2026-09-02/research.md`
  summary: CORRECTED 2026-09-04 -- this said a packed tarball carries the `license` field "but not the license TEXT". That is FALSE at HEAD and appears never to have been driven: npm/pnpm HOIST the workspace-root `LICENSE` into every tarball regardless of `files`. Measured by packing `contracts`, `kernel` and `registry` and listing the archives -- each carries exactly one `package/LICENSE` entry -- with the control in both directions: `ls packages/*/LICENSE` returns nothing, so no package owns one, and the root `LICENSE` exists. The original concern was real and is CLOSED, not open.
  evidence: Measured before adding it: `find . -maxdepth 2 -iname "LICENSE*"` returned nothing and `rg -c '"license"'` over all 11 manifests exited 1, with `AGENTS.md` as the control that the search path was live -- a public repository granting no rights to anyone, including on the clone-and-pack path the FR-29 proof exercises. Fixed at the field level: a root `LICENSE` plus `"license": "MIT"` in the root manifest and all ten packages (verified by parsing each file, 11 of 11). What is NOT fixed is the tarball: `files` paths are relative to the package directory, so a root LICENSE is unreachable from `pnpm pack`, and every packed archive would ship the SPDX identifier with no accompanying text. That is legally thin for a published artifact and irrelevant for an unpublished one, and all ten packages are still `private: true` at `0.0.0`. Deliberately NOT fixed by copying the file into ten directories, which is duplication that drifts and which nothing would check. Upgrade path, and it belongs to whichever story first removes `private: true`: either a pack-time copy step, or a `LICENSE` entry in each package's `files` fed by a build step that places one -- and in the same story, an assertion in `packages/session/test/consumer-install.proof.ts` that every packed archive contains `package/LICENSE`, so the guarantee fails rather than being stated. The proof already walks each archive's entries, so the clause is a few lines on machinery that exists.
- source_spec: `_bmad-output/implementation-artifacts/spec-m10a-a-session-can-run-in-a-worktree.md`
  summary: The Registry mirror half of Story 4.1/4.2's acceptance still does not ship, and the reason is now an ADOPTED-versus-ADOPTED conflict between AD-6 and AD-2 that no story can resolve on its own — it needs an architecture amendment, not an implementation.
  evidence: This story supplied the consumer the M6.A entry above named as missing ("the consumer that mounts this provider (Story 4.2 ...) writes the mirror inside its own serialized write") and the mirror STILL cannot be written, so that entry's precondition is necessary but not sufficient and this one supersedes its upgrade path. Three independent blockers, each measured at `2daa657`. (a) THE VOCABULARY. `RegistryEntryType` is `'skill' | 'mcp-server'` and the comment above it in `packages/contracts/src/registry.ts:7-9` is a rule, not a description: "every word here reaches an executor ... Both, and only both". A `worktree` word reaches no executor and renders through no projection target, which is exactly why M4.E retired `tool`. Adding it would make the type a bag of nouns again. (b) THERE IS NO CROSS-PACKAGE SERIALIZER. AD-6 (`ARCHITECTURE-SPINE.md:89`, `[ADOPTED + tightened H2]`) requires the mirror "within the same serialized transaction as record creation". The Registry's serializer is `acquireLock` over `<store>.lock` (`packages/registry/src/store.ts:362`); the ledger's is `LEDGER_QUEUES` (`packages/workspace-git-worktree/src/ledger.ts:55`). Two locks are not one transaction, and the shared leaf lock package that would make them one is recorded unbuilt twice already (this file, projection-ledger and ordinal-queue entries). (c) THERE IS NO TRANSACTION TO JOIN, ON EITHER SIDE. `#queued` wraps only `reserveOrdinal` (`ledger.ts:76-83`); `writeRecord` (`ledger.ts:85-87`) calls `#writeJson` directly, OUTSIDE the queue. So AD-6's clause is unsatisfiable by the worktree ledger today even if the Registry edge were legal, which AD-2 says it is not (`@panda/workspace-*` and `@panda/registry` are siblings; the topology is strictly downward). Upgrade path, and it has an OWNER because no story can take it alone: the architecture spine is amended — by whoever holds AD-6 — to say which of the two adopted rules yields for derived state. Two candidate amendments, both writable today: (1) AD-6's "same serialized transaction" is narrowed to "same serialized transaction as the record's own writer", making the mirror an eventually-consistent projection a consumer-tier caller reconciles, which is what AD-4 already says about derived state; or (2) `@panda/contracts` publishes a narrow mirror port the provider calls and a consumer implements — downward, therefore legal — and the leaf lock package is built first so one lock spans both writes. Until that amendment exists, do NOT add a `worktree` entry type, a mirror port, or the import: this story reached that clause and filed instead of implementing past it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m10a-a-session-can-run-in-a-worktree.md`
  summary: NEGATIVE RESULT — Story 4.2's acceptance clause "Registry writes remain consistent" is VACUOUS on the run path, and this story records that rather than writing a test that pretends to prove it.
  evidence: `panda run` never opens the Registry. Measured: `RegistryStore` occurs in ZERO files under `packages/cli/src`; the control is the same query over `packages/*/src`, which returns eight files, so the zero is an absence and not a failed search. `runSession` reaches exactly two services, `executor` and `workspace` (`packages/session/src/run-session.ts`), and neither touches `@panda/registry` — AD-2 forbids the workspace half from doing so at all. A test asserting "the Registry stayed consistent across two concurrent sessions" would therefore assert that a file nothing wrote was not corrupted, which passes for a reason that has nothing to do with the code under test and would keep passing if the workspace mount were deleted. What the acceptance's other clause DOES have is a real proof, and it already exists: `PANDA_REGISTRY_CONTENTION` (`packages/contracts/src/errors.ts:49`) is raised from `packages/registry/src/lock.ts:131-144` in both shapes, proved cross-process at `packages/registry/test/contention.test.ts:49-82` (the loser wrote nothing — `ENOENT` on the store) and at the binary at `packages/cli/test/registry-commands.test.ts:147-165`. Upgrade path: the clause becomes non-vacuous only when a session actually writes to the Registry — the worktree mirror of the entry above is the first thing that would — so it is re-examined in the story that lands that amendment, not before.
- source_spec: `_bmad-output/implementation-artifacts/spec-m10a-a-session-can-run-in-a-worktree.md`
  summary: RESOLVED (Story 4.2) — the `LocalWorkspaceProvider.acquire` non-string-id divergence recorded as a NEGATIVE RESULT under `spec-m6a-parallel-work-is-infrastructure.md` is closed, and it is closed by a SHARED clause rather than by the two-token guard.
  evidence: The M6.A entry offered two upgrade paths and this story took neither literally: a shared helper in `@panda/contracts` would have been one more spelling both providers could stop calling, and the two-token guard alone would have left the next provider of the port free to diverge again. What shipped instead is `acquire-non-string-id-rejected` in `packages/contracts/src/contract-suite/workspace-clauses.ts`, so the rule is enforced against every implementation of the port that runs the suite, plus the guard at `packages/workspace-local/src/local-workspace-provider.ts`. The clause was added FIRST and observed red: `acquire() of a non-string id rejected with non-coded error: The "path" argument must be of type string. Received null, expected PANDA_CONTRACT_WORKSPACE_UNKNOWN_ID` for `@panda/workspace-local`, green for `@panda/workspace-git-worktree` in the same run — which is the divergence the M6.A entry described, reproduced before it was fixed.
- source_spec: `_bmad-output/planning-artifacts/research/deepseek-harness-the-product-layer-2026-09-02/research.md`
  summary: The plugin-id-is-the-config-key gate covers only the plugins `run-session` MOUNTS, so `@panda/registry`'s plugin is conformant and unasserted.
  evidence: `packages/session/test/plugin-config-key.test.ts` is built out of `availableWorkspaceProviderIds()` plus the executor plugin, which is exactly what `packages/session/src/run-session.ts:417-418` registers -- two `kernel.register` calls, control: the same query over `packages/session/src` returns those two and nothing else. `@panda/registry`'s plugin declares `id: 'registry'` (`packages/registry/src/plugin.ts:75`) and reads `context.settings`, so it satisfies the rule today; it is simply out of the gate's reach because `@panda/session` does not depend on `@panda/registry` and AD-2 forbids adding that edge to host a test. Deliberately NOT fixed by exporting a shared assertion helper from `@panda/contracts`: that is an abstraction with one consumer, and the plugin it would cover has no production mount to break. Upgrade path, and it belongs to whichever story first gives the registry plugin a production caller: the mount site gets the same behavioural probe, built from that site's own registration list rather than from a hand-written package list, so a plugin cannot be mounted without being covered.
- source_spec: `_bmad-output/planning-artifacts/research/deepseek-harness-the-product-layer-2026-09-02/research.md`
  summary: NEGATIVE RESULT -- the research note's proposal to adopt DSH's discovery completeness bit (`{candidates, complete}`) was measured in panda and REFUSED: panda already carries the capability, spelled three different ways, and adding the field would be a fourth.
  evidence: The note reported that "today panda's discovery cannot say which it means", written from DSH's side and never measured here. Measured on 2026-09-03, in the two places panda discovers anything. (a) EXECUTOR DETECTION: `EvidencePath.exists` is already THREE-valued -- `true` present, `false` definitively absent, `undefined` could not determine -- with the errno kept alongside (`packages/environment/src/executors.ts:72-78`), `detectExecutors` returns the FULL catalogue including misses, and BOTH commands that can report "nothing found" print the undetermined paths on their own line (`packages/cli/src/run.ts:884-892`, called from the doctor path at `:560` and the init path at `:959`). (b) SKILLS DISCOVERY: `readRoot` does not return a partial list at all -- an absent root contributes nothing and every other failure THROWS a coded `registryProviderRejected` naming the path (`packages/registry/src/skills-source.ts:111-132`), so the list is complete or the run fails, and what was skipped is reported separately as `warnings` and `excluded` (`skills-source.ts:34-40,62-72`). (c) INGEST: `IngestWarning`'s one kind is `empty-source`, whose own comment states the distinction verbatim -- "the caller can tell a working-but-empty source apart from one that was never consulted" (`packages/contracts/src/providers.ts:101-106`). The one residual gap is narrow and deliberately left: `Diagnosis`'s `no-executor` FINDING message is not itself qualified by the undetermined evidence, though the same JSON envelope carries `detected` with all three values, so a consumer routing on findings alone sees an unqualified claim. Upgrade path if a consumer ever routes on the finding rather than reading the envelope: qualify that one message from `undeterminedEvidence`, which already exists and is already called on both paths -- not a new field on a port.
- source_spec: `_bmad-output/implementation-artifacts/spec-m11a-the-registry-can-see-the-servers-you-already-have.md`
  summary: After `panda remediate release`, panda's own unedited bytes in a vendor config become permanently invisible, and `panda remove` + `panda init` can then orphan the server with no report.
  evidence: M11.A's D4 case (ii) makes an unclaimed entry whose read-back content matches the registry `already satisfied` -- reported as nothing. That is right for the case it was built for (a server the USER wrote). It also silently covers a case it was not: after an explicit `release`, panda's own bytes remain at the location with no ledger record, so the merge takes the same `continue` and says nothing forever; a later `panda remove <id>` then leaves the server live in the vendor file, because the removal path needs a ledger record to reach it. Before M11.A that state reported `foreign-collision`, which was also wrong -- the user had just ASKED panda to stop claiming it -- so this is a bad report replaced by no report rather than a regression from a good one. NOT fixed here because the honest fix is a fourth verdict (`unclaimed, content-identical`) at info severity that does not move the exit code, and adding a reportable state is a `DiagnosisFindingKind` change with a `FINDING_EXITS` row, which is vocabulary this story has no mandate to invent. Upgrade path, and it belongs to whichever story next touches `REMEDIATION_KINDS` or `DRIFT_KINDS`: add the info-severity verdict, and assert in the same story that `remove` + `init` after a `release` says out loud what it is leaving behind.
- source_spec: `_bmad-output/implementation-artifacts/spec-m11a-the-registry-can-see-the-servers-you-already-have.md`
  summary: `readNativeMcpEntries` has no size cap and follows symlinks with no timeout, and a directory in place of a vendor config reports as ABSENT to `ingest` while `init` fails on the same path.
  evidence: The reader does a plain `readFile(filePath, 'utf8')` against `~/.claude.json`, which is Claude Code's own state file and routinely tens of megabytes, then parses it -- in addition to the merge's own read of the same file. A symlink is followed silently with no loop bound beyond the OS's `ELOOP`, so a symlink to a fifo hangs the run with no timeout. And `EISDIR` is in the absence list, so a directory where the config should be makes `panda ingest` say nothing while `panda init` fails on that exact path -- two commands disagreeing about one machine, which is the shape `noExecutorsDetected` exists to prevent elsewhere. All three are hardening rather than defects at the sizes and shapes measured, and none has produced a failure here. Upgrade path: a size cap that REFUSES coded rather than truncating (a truncated config read as complete is the silent-corruption direction M7.E closed for the writer), and moving `EISDIR` out of the absence list into the same coded refusal the writer already gives it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m11a-the-registry-can-see-the-servers-you-already-have.md`
  summary: NEGATIVE RESULT, and it is about the VERIFICATION rather than the code -- driving the binary reported AC2 green while the feature was broken for three of four real-world entry shapes, because the fixture was generated from the code under test.
  evidence: The acceptance sandbox wrote one MCP entry per vendor in each vendor's own file, and each fixture was built from the shape `renderMcpEntry` emits. The first implementation compared RENDERED BYTES, so that corpus was the one input guaranteed to pass. Measured after the fact with a control that proves the harness was sound: the rendered shape exits 0, while `{"command":"npx"}` (no `type`, no `args`), `type` absent, and an entry carrying an `env` block all exited 1 -- and per vendor, each on its own minimal shape, all three collided. This repository had already recorded the lesson in these words -- "the plant landed in the one shape the extractor accepted, and three other shapes sailed through green" -- and it recurred anyway, in the acceptance evidence of the story that cites it. The reusable rule: DRIVING THE BINARY IS NECESSARY AND NOT SUFFICIENT. A fixture generated from the code under test measures that the code agrees with itself. Where a feature reads input someone else wrote, the corpus must come from the vendor's documented shapes and must include the MINIMAL legal one, which is the shape real users actually have.
- source_spec: `_bmad-output/implementation-artifacts/spec-m11a-the-registry-can-see-the-servers-you-already-have.md`
  summary: One M11.A clause -- the reader classifying a non-absence errno -- is `describe.skipIf(process.platform === 'win32')` and therefore proves nothing on the development machine.
  evidence: Measured rather than assumed before accepting it: `chmod 0o000` on win32 sets the read-only attribute and does NOT deny reads, and every other candidate for a portable non-absence errno (a wildcard, `?`, `|`, a 400-character name, a mid-path colon) returns `ENOENT` on this machine, while a directory returns `EISDIR`, which is in the absence list. The only portable alternative was mocking `readFile`, which this repository does not do anywhere. CI runs Linux, so the clause does execute there. What runs on EVERY platform is the behaviour that depends on it: the source-level clause injects `unreadableFile: 'EACCES'` and asserts a per-origin warning with the other executor still contributing (`packages/registry/test/mcp-source.test.ts`), so "an unreadable config is a warning, not a run-wide refusal and not silence" is gated everywhere. Recorded because a `skipIf` that nobody wrote down becomes a clause everyone believes runs.
- source_spec: `_bmad-output/implementation-artifacts/spec-m14a-memory-is-a-contract-with-two-proofs.md`
  summary: `FilesystemMemoryProvider` loads its whole log into memory at `open()` and every read answers from that array; a store larger than the process, or a second writer against the same directory, is out of reach.
  evidence: Deliberate and marked `ponytail:` at `packages/memory-filesystem/src/filesystem-memory-provider.ts`. The alternative measured against it was re-reading `entries.ndjson` on every operation, which needs no cache and cannot diverge from the medium -- and which would have made the acceptance falsification WEAKER, because the reopen plant (deleting the append) would then have reddened every clause instead of exactly one, and the clause array could not have shown that persistence is a separate property from correctness. The cache is what makes `state-survives-reopen` the only thing that fails when the write never lands, which is the property FR-15's reopen seam exists to isolate. RD-1's single-writer doctrine is what makes the multi-instance case out of scope rather than unhandled. Upgrade path when a real store outgrows memory: keep a byte offset from the last read and tail the file on each operation -- the same append-only shape with a cursor, and no change to the port.
- source_spec: `_bmad-output/implementation-artifacts/spec-m14a-memory-is-a-contract-with-two-proofs.md`
  summary: `@panda/memory-sqlite` confines Node 24's `ExperimentalWarning: SQLite is an experimental feature` by patching `process.emitWarning` for the duration of ONE memoised dynamic import.
  evidence: Both halves were measured on Node 24.14.1 before being written. The warning fires when `node:sqlite` LOADS, not when a database opens -- so a static import puts it on stderr for a consumer that never touches a store, and stderr is a contract surface for `panda run`. The emission lands during the awaited import (Node schedules it on `process.nextTick`, which drains before the continuation), so a patch installed around the import and removed after it catches exactly that warning. The filter is narrow, on the warning TYPE and its TEXT, and `packages/memory-sqlite/test/load-sqlite.test.ts` proves it in child processes with two controls: a plain `import('node:sqlite')` that MUST show the warning, and an unrelated `ExperimentalWarning` that MUST survive the confinement. What remains a ceiling, and is marked `ponytail:` at `packages/memory-sqlite/src/load-sqlite.ts`: the patch is a global mutation, and a concurrent patcher replacing `process.emitWarning` inside that window would have its replacement restored away -- which is why the restore checks the function it is replacing is still ours. Upgrade path: delete the confinement down to a plain `import('node:sqlite')` in the first Node release where SQLite is no longer experimental.
- source_spec: `_bmad-output/implementation-artifacts/spec-m14a-memory-is-a-contract-with-two-proofs.md`
  summary: NEGATIVE RESULT -- E11 (a store path that cannot be created or opened) is NOT a shared conformance clause and is proved per package instead, deliberately.
  evidence: The shared suite takes two seams already (`reopen()` and `openDivergentFormatVersion()`), and both earn their place: neither property is reachable from a live provider instance, and no ordering trick produces them. An `openUnopenable()` seam would have been a third, and what it would test is medium-specific by construction -- a file where a directory belongs, for the filesystem; a path under a directory SQLite cannot create, for the database. It is proved in both packages instead, in the same shape and asserting the same code (`PANDA_CONTRACT_MEMORY_STORE_UNAVAILABLE`) and the same requirement (the message names the path), at `packages/memory-filesystem/test/provider.test.ts` and `packages/memory-sqlite/test/provider.test.ts`, each paired in one test with the AD-5 half that is the whole point of the distinction: an ABSENT store opens EMPTY. Deliberately NOT a clause that silently passes when a seam is absent, which is prose wearing a gate's clothes. Upgrade path if a third provider ever arrives: promote it to a seam then, when there is a second implementation to keep honest rather than a hypothetical one.
- source_spec: `_bmad-output/implementation-artifacts/spec-m14a-memory-is-a-contract-with-two-proofs.md`
  summary: NEGATIVE RESULT -- `MemoryProvider` ships with NO production caller inside panda's binary, and that is the PRD's design rather than the defect class it resembles.
  evidence: Recorded here so nobody rediscovers it as a defect. Every other port panda ships has a production caller -- `ExecutorAdapter` (adapter-cli plugin), `WorkspaceProvider` (M10.A), `SkillSource` (M9.A), `ToolProvider` (M11.A), `MethodPlugin` (M5.D) -- and this repository has paid three times for shipping an abstraction with no consumer. Memory is different because the PRD SPECIFIES the consumer: FR-15's own testable consequence is "One consumer TEST-SUITE passes unchanged against both shipped providers", scenario S2 is "an identical consumer SEQUENCE runs unchanged on both shipped MemoryProviders", and SM-3 (an external developer implementing one) is marked "long-term directional signal, NOT a release gate". The spec's D8 forbids a `panda memory` verb by name, on the grounds that inventing a requirement is the mistake 5-6 and the Profiles item were closed on. What DID ship to keep the port from being unreachable: two implementations, both driven by the same clause array, in two packages the topology gate and the FR-29 consumer-install proof both now enumerate. Upgrade path: the first story with a REAL consumer -- a memory verb the PRD asks for, or a session that persists what it learned -- takes the port as it stands and adds the caller; nothing here needs redesigning for that, and nothing here should be built ahead of it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m15a-panda-reads-the-stream-and-can-finally-say-how-much-is-left.md`
  summary: `recordUsageObservation` is a read-modify-write with no atomicity and no lock, so two runs finishing in the same instant can lose one of the two readings.
  evidence: Marked `ponytail:` at `packages/session/src/usage.ts`. The document is a CACHE of numbers a run can take again -- losing one costs a stale row until the next run, and `panda status` prints `observedAt` beside every reading precisely so a stale one is visible rather than silently believed. Deliberately not fixed with `@panda/projection`'s atomic writer, which `@panda/session` does not and per AD-2 may not depend on, nor with a new leaf lock package, which this file already records unbuilt three times over for cases with much more at stake than a cached utilisation. Upgrade path when concurrent `panda run` invocations become normal: write to a sibling temp file and rename, which is atomic on both platforms panda targets and needs no lock for a last-writer-wins document.
- source_spec: `_bmad-output/implementation-artifacts/spec-m15a-panda-reads-the-stream-and-can-finally-say-how-much-is-left.md`
  summary: Only `panda run` records a reading. A host that calls `runSession` from the SDK gets the observation on its callback and nothing writes it down, so `panda status` on that machine keeps saying not-observed.
  evidence: Deliberate, and the alternative was measured against it. The adapter reports through `CliExecutorAdapterOptions.onUsageObservation` and the CLI is what turns that into a file, because the adapter has no business knowing where panda's home directory is and `SessionOptions` carries no `homeDir` -- `readExecutorConfigLayers` is called by the CLI and hands the session already-read LAYERS, which is the shape M3.B chose so the kernel decides. Making the session record would mean giving `runSession` a home directory it has no other use for. The SDK half is not silent: `recordUsageObservation` is exported from `@panda/session`, so a host that wants the record calls it with the report it was handed. Upgrade path if a host ever needs it automatically: `SessionOptions` gains an optional recorder seam rather than a path, so the session still owns no scope.
- source_spec: `_bmad-output/implementation-artifacts/spec-m15a-panda-reads-the-stream-and-can-finally-say-how-much-is-left.md`
  summary: NEGATIVE RESULT -- `panda status` documents an exit code 2 that no input reaches today, and the clause that would have "proved" it was deleted rather than faked.
  evidence: The first attempt asserted exit 2 by handing `runPanda` a home directory containing a NUL byte. It exited 0, because `readUsageReports` treats a document it cannot read as ABSENCE and every row is derivable from the shipped catalogue with no file at all -- so the report is always producible. The clause was replaced by one that pins the measured truth (an unreadable home still exits 0 with typed absence) and the `catch` was kept as the uncaught-throw guard every other binding in `packages/cli/src/run.ts` carries, with the unreachability said out loud in its comment. Recorded because "do not build a branch that cannot fire" and "an uncaught throw must not escape the binary" pull in opposite directions here, and the resolution was to keep the guard and refuse the fictional test.
- source_spec: `_bmad-output/implementation-artifacts/spec-m15a-panda-reads-the-stream-and-can-finally-say-how-much-is-left.md`
  summary: NEGATIVE RESULT -- a CLI live test cannot authenticate under this suite's home isolation, and the failure looks exactly like a passing skip.
  evidence: `packages/cli/test/isolate-home.ts` repoints HOME and USERPROFILE at an empty temp directory for the whole CLI suite, which is right for every other file and fatal for a live one: claude reads its credentials from the real home and answers "Not logged in / Please run /login" in about 1.5 seconds. The live status check skipped with that as its reason and the file reported green, which is the shape a live check that never runs takes. Fixed inside the one file that needs it: `os.userInfo().homedir` comes from the OS account rather than from the environment -- verified by running it with both variables repointed -- so the child gets the real home while every `runPanda` call is still handed an explicit `homeDir` and panda's own scope stays isolated. Reusable rule: a live suite in a package whose setup isolates HOME must restore it for the CHILD, and its skip reasons must be read, because a skip is not a pass.
- source_spec: `_bmad-output/implementation-artifacts/spec-m15a-panda-reads-the-stream-and-can-finally-say-how-much-is-left.md`
  summary: NEGATIVE RESULT -- the spec's own measurement command carried `--include-hook-events`, and panda does not need it; the flag was dropped after measuring that the quota event arrives without it.
  evidence: M1 was taken with `claude --print --output-format stream-json --include-hook-events --verbose`. Re-run on 2.1.260 without that flag on 2026-09-03, the 14-event stream still carried `rate_limit_event` with `rate_limit_info.unifiedWindows`. Passing a flag the capability does not need would have widened the stream panda parses for no gain, and `--include-hook-events` belongs to story 2-6's lifecycle vocabulary rather than to this one. Also measured in the same pass and recorded because it contradicts the shape the old mode implied: `resetsAt` is a NUMBER, a Unix epoch in seconds, not an ISO string, and `utilization` is a fraction rather than a percentage. Both are reported verbatim; `UsageWindow.resetsAt` is typed `number` on that measurement, and a vendor that switched to a string would be reported as absence rather than silently coerced.
- source_spec: `_bmad-output/implementation-artifacts/spec-m16a-a-worktree-panda-made-is-a-worktree-panda-can-take-back.md`
  summary: `removeWorktree` finishes a tree git no longer registers WITHOUT re-running git's clean check, because it cannot; a `.git/worktrees/<id>` deleted by hand reaches that same state.
  evidence: Marked `ponytail:` at `packages/workspace-git-worktree/src/git-worktree-provider.ts`. Measured on real git 2.49.0 before the branch was written: `git worktree remove` deletes the ADMINISTRATIVE directory after its clean check and the working tree afterwards, so an interruption inside it leaves `git worktree list --porcelain` no longer naming the path while the directory is still on disk, and a second `git worktree remove` then answers `fatal: '<path>' is not a working tree` (rc 128) forever. Panda finishes it: the remainder belongs to a removal git had already approved. The ceiling is that panda cannot tell that state apart from a hand-deleted admin directory, where no clean check ever ran — reachable only by editing git's own admin tree for a worktree panda's ledger claims, and only for an id the user named. Refusing instead would leave a directory nothing in the product can take back, which is the dead end M4.C exists to abolish. Upgrade path: the removal intent record carries the clean verdict git gave before its admin directory went, and that branch proceeds only when the verdict is present.
- source_spec: `_bmad-output/implementation-artifacts/spec-m16a-a-worktree-panda-made-is-a-worktree-panda-can-take-back.md`
  summary: The removal-intent marker is a lockfile with pid-liveness and a 30-minute age fallback and NO cross-host staleness rule, so an intent left by another machine against a state directory on a network share blocks that removal until the age window expires.
  evidence: Marked `ponytail:` at `packages/workspace-git-worktree/src/ledger.ts`. It is the identical ceiling `@panda/registry`'s `lock.ts` accepts, for the identical reason stated there — panda cannot see another machine's processes — and E10 asked for the registry's contention rule rather than a second answer to the same question, so the constants are taken from it (`DEFAULT_MAX_AGE_MS`, `process.kill(pid, 0) === ESRCH`) rather than chosen again here. A corrupt or empty marker (the state a crash between the exclusive create and the write leaves) is dated by the FILE's mtime, so it gets the same grace that lock gives a corrupt lockfile instead of being stolen instantly. Upgrade path: the same one that lock records — a shared leaf lock package, which this file already lists unbuilt three times over.
- source_spec: `_bmad-output/implementation-artifacts/spec-m16a-a-worktree-panda-made-is-a-worktree-panda-can-take-back.md`
  summary: `panda doctor` (machine scope) reports NO worktree leftover, and `panda project doctor` is the only one that does, because worktrees are project state.
  evidence: `runSession` puts every worktree under `worktreeStateDir(projectRoot)` = `<project>/.panda/workspaces` (`packages/session/src/run-session.ts`), so the machine scope has none to report and a machine-scope reading would have to guess at projects panda has never been pointed at — which is exactly what `diagnose`'s own note forbids ("doctor never goes looking for other projects panda has bound"). The leftovers are handed IN through `DiagnoseOptions.worktreeLeftovers` rather than discovered inside `@panda/environment`, because that package's guard test pins its dependencies to four packages and permits exactly four filesystem verbs, neither of which can reach a worktree ledger. Upgrade path if a machine-wide sweep is ever wanted: panda would first need a registry of the projects it has run in, which it deliberately does not keep.
- source_spec: `_bmad-output/implementation-artifacts/spec-m16a-a-worktree-panda-made-is-a-worktree-panda-can-take-back.md`
  summary: NEGATIVE RESULT — the acceptance suite for the crash-safe removal was GREEN with the intent recorded AFTER the tree was removed, which is the exact inversion of D3, and one added observation is what tells the two apart.
  evidence: Measured, not reasoned about. With `ledger.claimRemoval(id)` moved below `git worktree remove`, all six clauses in `packages/cli/test/worktree-remove.test.ts` and all nine in `packages/workspace-git-worktree/test/removal.test.ts` stayed green — because a process killed AFTER the tree is gone leaves a record plus a marker, and the sweep reaches the identical end state from there. Every end-state assertion in the suite was therefore blind to the one property the story is named for. The fix is one synchronous observation taken inside the killed child at the instant the marker appears (`packages/cli/test/interrupted-removal-child.ts`): was the tree still on disk? With the correct order it is; with the inversion it is not, and re-planting the inversion afterwards reddened exactly that clause and nothing else. Reusable rule, and it is this repository's own "a falsification that lands in one shape is worth nothing" pointed at ORDERING: an end state cannot witness the order two writes happened in, so a crash-safety claim needs an observation taken DURING the crash, not after it.
- source_spec: `_bmad-output/implementation-artifacts/spec-m17a-a-malformed-config-is-located-not-quoted.md`
  summary: [EVIDENCE-WRONG 2026-09-04 — see the RE-MEASUREMENT at the end of this file] `document-fault.ts` exists TWICE -- once in `@panda/projection`, once in `@panda/registry` -- because the one rule it carries has to hold in two packages the topology forbids connecting.
  evidence: Marked `ponytail:` in both copies. The rule is "no error panda raises about a document quotes that document's content", and it needs `jsonc-parser`'s offsets to replace the parser message it discards. `@panda/projection` already depended on `jsonc-parser`; `@panda/registry` gained it here. The three ways to share it were all measured and rejected: `@panda/registry` cannot import `@panda/projection` (AD-2 -- projection sits ABOVE registry, and registry only carries it as a devDependency for the gate), `@panda/projection` importing `@panda/registry` at runtime is the wrong direction for a leaf helper, and putting it in `@panda/contracts` would give that package its first third-party dependency, which `pnpm proof:consumer-install` fails by design ("installs @panda/contracts ALONE, and asserts nothing else arrived" -- the third-party promise). Ceiling: two ~20-line copies to keep in step. What stops them drifting is that they are not separately tested -- ONE gate, `packages/projection/test/document-quoting.test.ts`, drives all six documents through both copies, so a change to either that reintroduces the quote reddens it. Upgrade path: a dependency-free leaf package (`@panda/document-fault`, or the shared lock leaf this file already records unbuilt four times over), worth building the first time a THIRD package needs the rule -- at which point the lock leaf and this one are the same package and get built together.
- source_spec: `_bmad-output/implementation-artifacts/spec-m17a-a-malformed-config-is-located-not-quoted.md`
  summary: NEGATIVE RESULT -- the first version of the TOML clause in the one gate was VACUOUS, and it passed.
  evidence: The clause meant to prove `TOML_STRATEGY.listEntries` no longer echoes a raw source line, and it drove `target.claim({ nativeText, entryId: 'ctx' })`. `claim` answers from the id it was HANDED, so its output contained "ctx" for a document it never opened, and the clause's control (`expect(reported).toContain('ctx')`) was satisfied by the argument rather than by the reader. Caught only by probing what `target.read()` actually returns -- `undefined` for every fixture, including the codex ones the clause claimed to cover. Rewritten onto `readNativeMcpEntries`, the reader `panda ingest` really uses, with the control tightened to `expect(read.unreadable.map(e => e.id)).toEqual(['ctx'])` -- which cannot be satisfied by an argument. Reusable rule, and it is this repository's "a zero without a control means I did not look" one level up: a control that can be satisfied by the test's own INPUT is not a control. Assert on something only the code under test could have produced.
- source_spec: `_bmad-output/implementation-artifacts/spec-m18a-the-omission-record-has-nowhere-to-put-the-secret.md`
  summary: The CI secret-detector scan FR-21's own acceptance criterion names has never existed. CORRECTED 2026-09-04: this entry originally claimed it "is the gate that would have caught M18.A's leak two stories earlier". THAT WAS FALSE and nobody had measured it -- see the correction inside the evidence below.
  evidence: `epics.md:411` requires "the artifact contains no secret-detector matches (CI-scanned)" and `prd.md:421` makes it testable as a "secret-detector scan over logs/bundle artifacts in CI". `.github/workflows/ci.yml` runs install, `pnpm check`, and `pnpm build && pnpm proof:consumer-install` — there is no such scan, and M8.A deferred it in those words as "a pipeline concern" (`spec-m8a…:193`). Left out of M18.A deliberately (its D6) because it is a workflow change rather than a code change, and mixing the two would have made the deviation closure unfalsifiable. What stands in for it today, and it is a substitute rather than the thing: `packages/registry/test/bundle.test.ts` drives the "no part of the credential anywhere in the artifact, including the record" assertion over all FIVE arms with three needles each (the whole token, its first eight characters, its last eight) and a per-arm control, and `packages/cli/test/registry-commands.test.ts` drives the same three needles through the binary over all three exits D5 names. That is a scan of the shapes this repository knows to plant, not a scan of the bytes CI produces. The cost of the gap is measured rather than argued: the `field: 'id'` arm carried the credential verbatim from M8.A (`7884e3e`) until M18.A, through a suite that was green the whole time. THE CORRECTION, and it is the reusable half: a CI scan would NOT have caught this, and the claim was a counterfactual asserted without measurement in a ledger built to be measured. Disproved on all three surfaces of the pre-M18.A tree, each with a control. (1) THE BUNDLE: CI never produces one. `.github/workflows/ci.yml` has four `run:` steps -- install, `pnpm check`, `pnpm build`, `pnpm proof:consumer-install` -- and no `panda export` and no `upload-artifact` (control: `pnpm` returns 9 hits in that file, so the query sees). The only bundles that exist during a CI run are written by `packages/registry/test/bundle.test.ts:339,349` into `mkdtemp(join(tmpdir(), 'panda-bundle-'))`, outside the workspace and gone when the job ends. (2) THE JOB LOG: the suite was GREEN for the whole two milestones the leak was live, and a green vitest run prints no assertion diff -- there were no leaked bytes in the log to find. (3) THE SOURCE: the leak was pinned in a test, but `FAKE.githubClassic` is `fixture('ghp', '_Ab3d...')`, assembled from a prefix and a body SPECIFICALLY so scanners do not fire on it (`bundle.test.ts:33-53`, and the reason is written there: push protection rejected the file's first version). A source scan returns green on it by design. So the honest justification for this gate is NOT retrospective. It is that an independent instrument does not depend on panda's own exit-site enumeration being complete -- and that enumeration has been incomplete once per story in the two most recent stories (M17.A found a fourth site by widening; M18.A found `panda import`'s stdout in review round two). SCOPE MATTERS AND THE AC IS THE WEAKER HALF: scored against panda's own three measured incidents, a BUNDLE-ONLY scan catches ONE -- M17.A leaked through `doctor`/`init`/`ingest` stdout, and M18.A round two through `panda import` stdout AND stderr. `prd.md:421` already names the half with teeth by saying "logs/bundle artifacts". BUILD IT TO prd.md:421, NOT TO epics.md:411 ALONE. And price the part nobody has: panda's fixtures plant real token SHAPES on purpose, so a scanner over the job log is red on day one and needs an allowlist -- which is exactly the thing that rots into "the scanner finds nothing because everything is allowed". That allowlist needs a written rule before the step is worth having. Upgrade path: a `gitleaks`/`trufflehog` step over the bundle artifacts and the job log in `.github/workflows/ci.yml`, with a control plant that MUST be caught — a scanner that finds nothing and a scanner that is not running produce the same green.
- source_spec: `_bmad-output/implementation-artifacts/spec-m18a-the-omission-record-has-nowhere-to-put-the-secret.md`
  summary: An omission record's unknown-root-key refusal names the KEY, so a hand-crafted bundle whose key is itself credential-shaped prints that key -- and the sibling array does exactly the same, which is why it was not patched here.
  evidence: `readOmittedEntry` refuses a root key the envelope does not declare and names it (`'note' is not allowed on an omission record`). `registryEntryIssues` refuses an unknown key on the sibling `entries[]` array in the same document and names it the same way (`contracts/src/registry.ts:244-252`, `'<key>' is not allowed at the entry root`). So a credential-shaped KEY reaches stderr through either array. Deliberately NOT fixed in M18.A, and the reason is the rule M17.A shipped and this story inherited: a fix for one path that leaves its sibling leaking makes the surface LOOK closed, which is worse than leaving both open where a reader can see it. Reachability is also strictly smaller than the defect M18.A closed -- that one was produced by panda itself from an ordinary registry on the export path, while this needs a bundle a human hand-crafted with a token as a JSON key. Upgrade path: one rule over BOTH arrays -- refuse an unknown root key by POSITION and count rather than by name (`omitted[0] carries 1 key this envelope does not declare`), which is the same move M17.A made when it stopped quoting a parser's message and started reporting a location. Do not take it for `omitted[]` alone.
- source_spec: `_bmad-output/implementation-artifacts/spec-m18a-the-omission-record-has-nowhere-to-put-the-secret.md`
  summary: The gitleaks CI gate was DESIGNED, measured, and deliberately NOT BUILT -- and the measuring demolished the justification the earlier entry gave it.
  evidence: THREE measurements decided it, none of them an opinion. (1) A SCAN OVER THE JOB LOG IS VACUOUS, PERMANENTLY, and for a structural reason the earlier entry missed. `packages/cli/test/registry-commands.test.ts:22` injects `{ stdout: (line) => out.push(line), stderr: (line) => err.push(line) }`, so every byte panda's verbs print during CI goes into an array and is asserted on -- NONE of it reaches the job log, green or red. Measured on a FAILING run: 0 provider-shape hits over 105 log lines, with the control passing (the same scanner over three planted lines returned 2/3). The earlier entry blamed a green suite; the real cause is that panda's output is captured by construction. (2) GITLEAKS IS 4-FOR-11 AGAINST PANDA'S OWN CORPUS and has NO OpenAI and NO Anthropic rule -- probed in three real-length forms. `bundle.ts`'s `/\bsk-[A-Za-z0-9_-]{20,}/` covers both. So "swap in a real scanner" would be a DOWNGRADE on the two providers panda exists to orchestrate; the honest shape is BOTH detectors, and that is a bigger change than a workflow line. (3) THE INDEPENDENCE CLAIM IS NARROWER THAN THE EARLIER ENTRY STATED, and the designing seat said so against its own design: the gate must drive named verbs (`export`, `import`, `doctor`, `ingest`) because CI emits nothing to scan today, and ANY step that names which verbs to run IS an exit-site enumeration -- just a second one. What is genuinely independent is the SHAPE knowledge, not the SITE coverage, and site coverage is what the gate was justified on. A new verb that prints a credential stays invisible to it until someone adds it to the drive list, exactly as it is invisible to the suite. WHAT THE DESIGN IS, so it is not re-derived: a `scripts/proof-no-secrets.mjs` driving the built binary against a throwaway `HOME`, scanning the exported bundle plus the captured streams of those four verbs plus the `HOME/.panda` tree; a pinned gitleaks binary with its sha256, not the licence-gated action; TWO controls -- the real surface must yield ZERO findings and the same surface plus a runtime-assembled needle must yield EXACTLY ONE, so silence cannot satisfy the pair; and NO ALLOWLIST EVER, because the scan runs only over bytes CI produced at runtime and never over the source tree, where GitHub push protection is already the independent instrument and has already been DEMONSTRATED on this repo (`bundle.test.ts:36-40` records it rejecting that file's first version). One correction the design also produced: `prd.md:421`'s "secret-bearing values never appear in logs" is already unenforceable as literally written, and not because of test fixtures -- `registry-commands.ts:342` and `:482` print the whole entry by CONTRACT, because showing you your own registry is those verbs' job. Build it when a story needs CI to emit an artifact for another reason; the workflow machinery is not worth its allowlist-shaped hazard on its own.

## RE-MEASUREMENT, 2026-09-04 — all 201 entries driven against HEAD

Four independent passes, one per quarter, each required to EXECUTE rather than read and to
carry a control for every zero. **35 entries carry a marker; 23 of those are wrong or closed.**
That is 11% of a ledger this project uses to decide what to build next, and every wrong entry
mis-ranks work in every future session — as one did in this very session, when a false
counterfactual about a CI secret scan made that gate look better justified than it is.

**STALE-FALSE (8) — the claim is not true at HEAD.**
- 12: `panda add` / `list` / `remove` all ship; doctor's `unprojectable` and `removed-by-user`
  exits name `panda remove <type> <id>`, not an SDK call. What survives is only "ingestion is
  additive only", which the summary already said.
- 15: `envelope.data` is `Record<string, unknown>` and already carries the numeric `data.usage`
  the settlement reads. Only `output.metadata` is string-only.
- 24: the "session-only bypass surface is ZERO" clause is wrong — `@panda/session` exports
  `createSessionKernel`, and `PandaKernel.swap` runs a caller-supplied factory. It is ONE.
- 45: `panda run --trace` installs a `createLogSink` and prints records; driven, with the
  no-flag run as its control. The open half is RETENTION, not the absence of a consumer.
- 60: **the most important finding of the pass.** "Nothing reads plugin code from disk" is
  refuted by execution. `resolveMethod` dynamic-imports a user-written specifier from
  `.panda/config.json`, and the module's TOP-LEVEL CODE RUNS BEFORE `validateMethodPlugin`
  refuses it — measured by planting a module whose only statement is a `writeFileSync`, with a
  manifest the contract rejects: the refusal fired AND the file existed. Control: an
  unresolvable specifier leaves no file. AD-3's trust half now has a reachable surface and
  still has no decision behind it. This is NOT fixable by reordering — a module cannot be
  validated without being loaded — so it needs a decision about what an imported method may do,
  not a patch.
- 108: superseded by the correction three entries later; the interpolated-verb ceiling was not
  harmless, and the `FINDING_EXITS` gap it recorded is closed.
- 125: `retired-type` is the FOURTH longest doctor resolution of fifteen, not the longest, and
  the length is the shared composition rather than that kind. Computed over every kind and
  confirmed by driving the binary.
- 164: **the recorded remedy had already expired, and this repository was still bleeding from
  it.** A third live-suite naming style (`live-` as a PREFIX) escapes the glob the entry closed
  itself on and that `AGENTS.md` taught. Fixed and gated in M23.A.

**STALE-FIXED (15).** 3 (build + FR-29 proof), 13 and 43 (`panda ingest` is the production
caller), 16 and 44 (`panda run --executor`), 61 (M7.C applies the configSchema), 75
(`.gitattributes` protects the vendor schemas), 116 (project-scope skills are reported),
136 (5.4 chose the single hook code deliberately), 143 (the worktree plugin was built),
144 (the non-string-id guard, now pinned by a shared clause), 152 (panda can load a
MethodPlugin), 162 (the Standard Schema `path` ships with its renderer), 169 (separator
portability, closed by M8.B and already recorded four lines below it), 170 (the "second
signal" the hex exclusion said did not exist shipped as the flag name, in M21.A).

**EXPIRED REASON (2), and this is the pattern worth naming.** 135's deferral was owned by
Story 5.4; 5.4 shipped, added no parameter, and never raised the question — the deferral now
has no owner. 151's stated trigger ("if a second non-engine caller appears") has FIRED three
times, and one of them, `projection/src/remediate.ts:530`, does NOT code its boundary, so a raw
`EPERM` escapes uncoded from `panda remediate discard --apply`.

**EVIDENCE WRONG (1).** 197 inverts the devDependency direction: `@panda/registry` carries no
panda package at all; it is `@panda/projection` that devDepends on registry for the shared gate.

**DETAIL DRIFT (9).** 56, 63, 85, 94, 111, 114, 117, 127, 128 — each still points at a real gap
while quoting a string, a count or a framing that has moved. A future reader greps for the
quoted sentence and finds nothing.

**THE METHOD NOTE, because it is reusable.** One pass nearly filed an entry STALE-FIXED off the
help text alone: `panda workspace remove` exists, the finding names it — and it is scoped to the
git-worktree provider while the default provider is `local`, whose per-run directories still
have no exit. The callee's name matched; the call site's scope did not. That is this
repository's own rule, applied by an instrument to itself.

Entries are marked in place rather than rewritten, and this block is appended rather than
folded in, because the ledger is append-only and the trail of what an entry once claimed is
half its value.
