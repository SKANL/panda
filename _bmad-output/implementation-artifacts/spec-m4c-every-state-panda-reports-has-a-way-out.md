---
title: 'Every state panda reports has a way out'
type: 'feature'
created: '2026-08-26'
status: 'in-progress'
review_loop_iteration: 2
baseline_commit: '2782fda'
context:
  - '{project-root}/_bmad-output/planning-artifacts/architecture/architecture-panda-2026-08-24/correction-01-native-projection.md'
  - '{project-root}/_bmad-output/implementation-artifacts/spec-m4b-skills-materialise-where-executors-find-them.md'
  - '{project-root}/_bmad-output/implementation-artifacts/spec-2-7b-panda-doctor.md'
  - '{project-root}/_bmad-output/planning-artifacts/ROADMAP-02-the-container-and-the-promise.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** panda can put a user in a state the product cannot get them out of, and it now has four ways to do it. A panda-owned entry or tree the user edited reports `edited` forever. One they deleted reports `removed-by-user` and panda never re-adds it. Anything at panda's location the ledger does not claim is a `foreign-collision` panda refuses to resolve — **including panda's own tree after a crash between the write and the ledger update**, which needs no user action at all. And a ledger record damaged in a way that disqualifies it is carried but never repaired.

Every one of these is correct behaviour. Refusing to touch what panda does not own is the guarantee the whole projection subsystem is built on, and M4.B tightened it precisely because two reviewers deleted files through a forged claim. The defect is not the refusal. **The defect is that the refusal is terminal.**

The ledger has been saying so since Story 2.2. The same gap was recorded there, re-homed to Story 2.7, re-homed again to 2.7b, and is still open — and M4.B added a fourth entrance while closing none. Four stories, five entries, one hole.

**And the only exit that exists today is hand-editing `~/.panda/projection-ledger.json`** — the file whose integrity every safety guarantee in this subsystem depends on. Telling a user to repair panda by editing the ownership record by hand is worse than the state it repairs.

**Approach:** an explicit, per-finding remediation the user asks for by name. Panda never decides to resolve anything; the user does, one finding at a time, having been told exactly what will happen. That is the same asymmetry `panda doctor` already has — doctor reports and writes nothing, and this is the other half it was designed to leave room for.

**Every remediation is described before it runs, by the code that runs it.** A preview computed by a second path is a preview that can disagree with the act, which is the divergence `panda doctor` exists to avoid and the reason its inspection mode is the projection engine rather than a copy of it.

**correction-01 C6 lives here too.** A previous build wrote panda-vocabulary blocks into vendor files at locations no executor reads. Those are panda's own litter and nothing removes them; the remediation vocabulary this story builds is what can.

## Boundaries & Constraints

**Always:** remediation is explicit and per finding — the user names what to resolve and panda resolves nothing else; every remediation states exactly which paths and which bytes it will change BEFORE it changes them, computed by the same code that performs it; a remediation that would touch anything outside the location panda owns is refused coded, and M4.B's containment applies unchanged — resolved paths, links disqualifying, no path a surviving claim still holds; every state panda can REPORT has at least one remediation that leaves it, and a test proves the two sets match; the ledger stays the ownership authority — remediation changes what panda claims, it never guesses; a remediation that fails leaves the state it found, or reports plainly that it did not.

**Ask First:** any automatic or on-by-default remediation; a bulk "fix everything" that does not name each finding; remediating a finding panda did not report in the same run; deleting a user's file as part of adoption [RENEGOTIATED 2026-08-26 by the spec author — permitted for `adopt`, see Spec Change Log 1: `adopt` itself deletes nothing, the consequence is named before the claim is written, and refusing it would remove the only exit from the state a crash produces]; remediation of a scope the caller did not name.

**Never:** no remediation panda cannot describe first; no silent adoption of foreign content; no reach outside what the ledger claims or what the user explicitly named; no new way to lose a file the user wrote; no state left reportable-but-unresolvable once this ships — if a finding has no exit, it is not shipped as a finding.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Every finding has an exit | The full set of reportable states | Each has a remediation, proven by a totality test | Missing ⇒ fails |
| Described before done | Any remediation | Names the paths and the change first, from the same code path | N/A |
| Preview equals act | A described remediation, then run | What happened is what was described | N/A |
| Explicit only | A run with no remediation asked for | Nothing is remediated, exactly as today | N/A |
| Adopt | Content at panda's location the ledger does not claim | Panda claims it, or says why it will not | Coded |
| Restore | A panda-owned thing the user edited | Panda's version, only after the user asks — in two named steps, not one verb [RENEGOTIATED 2026-08-26 by the spec author] | Coded |
| Release | A claim the user wants panda to stop holding | The claim goes, the file stays untouched | Coded |
| The crash state | Panda's own tree with no record | Resolvable — it is panda's litter, not the user's | Coded |
| C6 litter | A block a previous build wrote where nothing reads it | Removable through the same vocabulary | Coded |
| Containment holds | A remediation aimed outside the owned location | Refused coded; nothing touched | Coded |
| A foreign neighbour | Anything panda did not write, beside what it did | Untouched by every remediation | N/A |
| Failure | A remediation that cannot complete | The prior state stands, or the partial result is named | Coded, not silent |
| No hand-editing | Every state reachable in this story | Resolvable without opening the ledger | Proven |

</frozen-after-approval>

## Code Map

- `packages/projection/` -- the remediation vocabulary beside the drift vocabulary that names the states
- `packages/environment/` -- the capability, beside `diagnose` which reports the findings it resolves
- `packages/cli/src/run.ts` -- a thin binding: name the finding, print what will happen, map the result
- tests -- the matrix, the totality proof, and the preview-equals-act proof

## Tasks & Acceptance

- [x] A remediation vocabulary covering every state panda reports
- [x] A totality test tying reportable states to remediations, so a new state without one fails
- [x] Description before action, from the same code path that acts
- [x] Explicit, per-finding invocation; nothing automatic
- [x] M4.B's containment applied unchanged to every path a remediation touches
- [x] The crash state and correction-01's C6 litter both resolvable

**Acceptance Criteria:**
- Given any state `panda doctor` reports, when the user asks for its remediation by name, then panda describes exactly what it will change and then changes exactly that
- And nothing is remediated that the user did not name
- And no remediation touches anything outside what panda owns or what the user named
- And no state reachable in this story requires hand-editing the ledger to leave

## Spec Change Log

**0. RENEGOTIATION of the `Restore` matrix row, by the spec author.** The frozen
row asked for panda's version back as one act. It ships as two named steps —
`adopt`, then `panda init` — and the implementer flagged the departure rather
than quietly satisfying the wording. The argument is right and is recorded here
because it is the kind of reasoning a later reader will want: a verb that
rendered ONE entry into a vendor file would be a second renderer standing beside
`mergeNative`, which is the divergence correction-01 and `panda doctor` both
exist to remove, and scoping `runProjection` to a single entry would converge
entries the user never named. The outcome the row demanded still holds — panda's
version arrives, and only after the user asks. The row is amended, not the
intent.

**1. RENEGOTIATION REQUESTED — adoption of a foreign occupant (HIGH-2 of review round 2). NOT implemented past; the behaviour is unchanged pending an answer.** `adopt` on a `foreign-collision` writes a ledger claim for content the USER wrote, and owning a location is what lets the ordinary `panda init` replace it and, on a skills root, remove it after deregistration. That is against the frozen **Never** (*"no new way to lose a file the user wrote"*) and the frozen **Ask First** (*"deleting a user's file as part of adoption"*), and no renegotiation is recorded — so the behaviour is UNCHANGED and this is the ask, in the shape the `Restore` row already used.

What shipped in this round is the half that needs no permission: the description now states the consequence on both branches and names every path that becomes deletable, before the claim is written. What is on the table is the behaviour, and the three options are not equivalent:

1. **Keep it** (today). `adopt` is the exit from `foreign-collision`, which is the headline state of this story and the one the crash window produces. The safety is consent: explicit, per finding, described first, and never automatic. The cost is that the frozen Never is read narrowly — panda itself never deletes during `adopt`, and the deletion happens later, through `panda init`, which the user runs.
2. **Refuse to adopt content panda cannot attribute to itself.** Honours the Never literally. It also removes the exit from `foreign-collision` entirely — including panda's own crashed tree, which is indistinguishable from a user's file by any evidence panda holds, which is the whole reason the ledger exists.
3. **Split the verb**: adopt only where a record already exists (re-claim), and a separate, differently-named verb for taking foreign content. Same behaviour, a louder name, one more thing to learn.

I recommend (1) with the wording that now ships, and I am not treating that recommendation as permission.

**ANSWERED 2026-08-26 by the spec author: option (1), and the frozen Ask First is
renegotiated to permit it.** The reasoning is not the recommendation's.

I was going to take (3). My own Ask First names adoption-then-deletion as an act
needing separate permission, and a separate verb IS separate permission at the
point of use, where a paragraph is not. The repo has the precedent too: doctor's
own review split `out-of-date` from `not-writable` because one word was making
two different promises.

Frequency decided it the other way. The states this exit exists for are panda's
own tree with no record — a crash, a lost ledger, a build that predates the
document. Those are panda's fault and will be commoner than a user happening to
hand-write a file at exactly the path panda would materialise. And panda cannot
tell the two apart, which is the whole reason the ledger exists. So under (3) the
user would reach for a verb named after taking someone else's content in order to
clean up panda's own litter: the common, safe case wearing the rare, dangerous
case's name. That is a worse error than the one (3) prevents, because it is the
one people meet.

What makes (1) acceptable is not the consent alone — it is the consent plus the
description that now ships: both branches carry the same consequence, and every
path that becomes deletable is named before the claim is written. Informed
consent at the point of use is the thing the Ask First was protecting; the verb
count was never the point.

The Never clause is read as it was meant: `adopt` deletes nothing. The later
deletion happens through `panda init`, a command the user runs, on a location the
user has just said panda owns.



- **The vocabulary is FOUR verbs, and three of them cannot touch a file the user wrote (decision the frozen block left open).** `adopt` — panda claims what is at its own location, exactly as it is now. `release` — panda stops claiming a location; the file is not read, not written, not opened. `repair` — panda rewrites its OWN ledger document to hold exactly the records it can still read. `discard` — panda removes its OWN prior output from a vendor file (correction-01 C6). Justified against AD-6 rather than against convenience: AD-6 says *"ownership is proven by durable metadata records written at creation — never inferred from paths"*, and the four verbs are the four things you can do to such a record — write one for what is there, drop one, repair the document that holds them, and take back output from the era before the document existed. MEASURED consequence of the shape: on the `edited` fixture in `packages/environment/test/remediate.test.ts`, a whole-scope byte snapshot before and after `adopt` reports exactly one changed path, `home/.panda/projection-ledger.json`. The command a user reaches for while something is already wrong cannot lose a byte they wrote, and that is a property of the vocabulary, not a promise about the code.
- **There is deliberately no `restore` verb; "take panda's version" is `adopt` then `panda init` (decision).** The frozen block suggests three shapes and the third is *"take panda's version"*. Panda already HAS a command that takes panda's version — `panda init` — and it refuses only because the ledger disagrees with the disk. So the remediation fixes the disagreement and the existing converge does the rest, pinned by *"re-claims an entry the user edited, so the next projection takes panda's version back"*. MEASURED alternative, and why it was rejected: a verb that rendered one registry entry straight into a vendor file would be a SECOND renderer beside `mergeNative` — the exact divergence correction-01 was written to remove and the reason `panda doctor` is an inspection MODE rather than a copy. Scoping the ordinary projection to one entry is the same second path wearing a different hat, and it would also break *"nothing unnamed is touched"*: running the target's merge converges every OTHER entry of that target in the same call. The residual cost is the intermediate state where panda's ledger claims bytes panda did not write, which the adopt description says out loud; recorded in deferred-work.md.
- **`repair` is the fourth verb because `ledger-damaged` had no other exit (decision).** The frozen Intent names it — *"a ledger record damaged in a way that disqualifies it is carried but never repaired"* — and neither `adopt` nor `release` can reach it: a malformed record has no `entryId` panda can name, and `update` REFUSES to write over an unreadable ledger, which is a load-bearing guarantee Story 2.8's review declared terminal to lose. So `ProjectionLedger` gained exactly one new primitive, `rewriteAll`, whose caller list is asserted in `packages/projection/test/guard.test.ts` to be `['ledger.ts', 'remediate.ts']` and nothing else. MEASURED: adding a second mention of the symbol anywhere in `src/` turns that clause red (mutation M20). CORRECTED after review round 2 — a caller list is a SPELLING check and a reviewer walked past it with `ledger['rewrite' + 'All']([])`; the method now also requires `LEDGER_REPAIR_AUTHORITY`, a module-scope symbol obtainable only by importing it, and refuses coded without one.
- **Description before action is the ENGINE's own mode switch, not a second function (decision).** `runRemediation` takes `mode: 'inspect' | 'apply'`, resolved through `resolveProjectionMode` — the same validator `runProjection` now calls, extracted so the two commands that can write cannot disagree about what "do not touch this machine" means. The `changes` array is computed identically in both modes and IS the description. Proven by execution rather than asserted: `expectPreviewEqualsAct` in `packages/projection/test/remediate.test.ts` runs the remediation under inspection, hashes every file under the sandbox, runs the identical call under apply, hashes again, and asserts that the set of paths whose bytes actually changed equals the set the inspection named — plus that inspection changed nothing at all. It is applied to all four verbs. MEASURED: replacing the validator with `options.mode !== 'inspect'` turns FIVE cases red (mutation M13; the change log first said three, because the driver printed only the first three failures and the log copied what was printed).
- **The remediation DEFAULT is `'inspect'`, the opposite of `runProjection`'s (decision).** A projection converges a machine a user asked panda to manage; a remediation changes who owns what. `panda remediate adopt …` describes and writes nothing; `--apply` performs. MEASURED: flipping the default to `'apply'` turns SIX rows red (mutation M17; first logged as three, same truncated printer), because describing is asserted to leave the finding in place.
- **A remediation runs only for a finding the SAME run reported, resolved to exactly one row (decision).** `remediate` in `@skanl/panda-environment` calls `diagnose` — the same call `panda doctor` prints — and selects from its findings. Zero matches is a coded refusal that says panda never remediates a state it did not just report; more than one is a coded refusal that lists them and asks for `--executor`/`--entry`. That makes two Ask-First items from the frozen block unreachable rather than merely discouraged: *"a bulk fix that does not name each finding"* and *"remediating a finding panda did not report in the same run"*. MEASURED: disabling either branch turns its row red (mutations M15, M16), and the ambiguous case additionally asserts that nothing was written while panda refused.
- **The totality proof derives BOTH sides, and a third assertion runs the states (decision — this is the story's central guarantee).** Side one is `DIAGNOSIS_FINDING_KINDS`, already derived from the total `RESOLUTION` record `panda doctor` phrases its findings from. Side two is `Object.keys(FINDING_EXITS)`, a `Record` over the same union — so a kind added without an exit does not COMPILE (MEASURED: adding one produces `TS2741` in `src/remediate.ts` and `src/doctor.ts`). On top of that, three runtime clauses that a compiler cannot give: every member of the contracts' `DRIFT_KINDS` must have an exit whose `by` is `'remediation'` (a fourth drift verdict upstream turns it red until it has an exit that is not a text editor); every member of `REMEDIATION_KINDS` must be named by at least one exit and every named verb must exist (MEASURED: adding a fifth verb nothing reports turns it red, mutation M19); and the builder table that ENTERS each remediable state must have exactly the keys the exits say are remediable (MEASURED: promoting `not-writable` to a remediation turns it red, mutation M18). The behavioural half then builds each of the five states on a real filesystem, asserts `panda doctor` reports it, remediates it by name, and asserts the kind is gone.
- **An exit is one of three shapes, because they are three different promises (decision).** `by: 'remediation'` — panda performs it. `by: 'command'` — an existing panda command already leaves it (`not-initialised`, `out-of-date` → `panda init`). `by: 'outside-panda'` — panda cannot, and the exit says what can. Collapsing them would have meant naming a remediation for `no-executor` or `not-writable`, which is the same false promise `panda doctor`'s own review found and split into `out-of-date`/`not-writable`: a resolution panda cannot perform is worse than the state it describes. Five kinds are `remediation`, two are `command`, five are `outside-panda`.
- **`adopt` claims the paths the TARGET would write, never a directory listing (decision, USER-DATA).** For a materialisation target the claim comes from `target.plan(...)` and is then hashed as it is on disk. That is what stops adoption from turning a file the user put inside panda's tree into a path a LATER run is authorised to delete. MEASURED end to end: a hand-made `NOTES.md` beside panda's `SKILL.md` is not in the record adoption writes, and survives both the adoption and the removal that follows dropping the entry from the registry.
- **CORRECTED after review — a partially present tree was REFUSED, and that left a state with no exit at all (HIGH, review round 2).** The first shipped shape refused to claim a tree that is only partly there, argued as "claiming it writes a record that reads as `edited` on the very next run". The argument was right about claiming the whole PLANNED set and wrong about the fix: claiming the SUBSET that exists reads `intact`, and the ordinary run writes the missing files back. What refusing produced was a trap rebuilt out of the new vocabulary — three reviewers reached it independently, by `release` on an `edited` tree (the NAMED exit for that state, which moved the user from one refusing exit to two), by a kill inside `land()` between two file writes, and by a user deleting one file from a skill folder. In every one `adopt` refused, `release` was spent or refused, `repair` reported nothing, `discard` did not apply and `panda init` refused; the only escape was `rm -rf`. Now: the subset is claimed, and when the entry has left the REGISTRY the claim falls back to the ledger's own record — the shape reported as `edited` on the removal path, whose only other exit was `release`, which drops the claim and leaves panda's tree on disk forever. That branch's description says the sharpest thing it can, that the next run REMOVES the tree. MEASURED: restoring the refusal turns two rows red; removing the ledger fallback turns the unregistered row red; removing the record-derived containment re-check turns the escaped-path row red (mutations N1, N2, N3). This also falsifies the M4.B crash entry as I closed it — `land()` writes file by file and rolls back only inside a `catch`, which a `SIGKILL` never runs — and that entry is corrected in `deferred-work.md` to say so.
- **M4.B's containment, unchanged, on the one path that CREATES a delete authority (decision, USER-DATA).** Every clause of the removal rule is applied while the claim is built AND again beside the ledger write: paths resolved, containment against the root checked twice, a link anywhere between the root and the file disqualifying, and a path any other surviving record claims refused. Each of the five attacks M4.B's own review used has a case here and each fires when the guard is weakened — a forged claim naming an absolute path outside the root (M1), a RELATIVE path that would resolve against the process working directory (M1), a claim naming a different file from the one the target owns (M2), a junction where panda's tree was (M3), and two ids resolving to one path (M4). A materialisation target that plans outside its own root is refused per entry rather than thrown (M6), and `discard`'s scope containment is checked before the file is even opened (M8).
- **The config target grew a `claim` method rather than the remediation re-deriving a hash (decision, port change).** Only the target knows where an entry lives in its own format and what "the same entry" means there, and `contentHash` has to be computed exactly the way `merge` computes it — `hashOwnedText(strategy.canonical(...))` — or the adopted claim reads as `edited` on the very next run. `claim` is OPTIONAL and its absence is a coded refusal rather than a gap: a target that cannot say what occupies its location gets no adoption, because claiming bytes panda cannot identify is the one thing this vocabulary must not do. It reuses `validate`, `containerConflict`, `entryConflict` and `locate` unchanged, so every shape the merge refuses to write into is a shape adoption refuses to claim (MEASURED: dropping the `entryConflict` half lets panda claim a location the document spells twice, mutation M9).
- **correction-01 C6 ships as a REPORTED state with an exit, not as a hidden command (decision).** `legacy-block` is a new `DiagnosisFindingKind`, severity `problem`. It had to be both or neither: the frozen Never says *"no state left reportable-but-unresolvable"*, and a removal a user is never told about is a command nobody runs. Detection and removal are ONE function — `scanLegacyPandaBlock` in `formats.ts` — reached by doctor through `runRemediation` under inspection, so the sentence reported is the sentence the act carries out. MEASURED locations, from correction-01's own evidence table: `~/.claude/settings.json` (`$.panda`, a file the corrected build does not touch at all, so nothing else would ever clean it), `~/.config/opencode/opencode.json` (`$.panda`, beside the `mcp` entries panda writes today), and `~/.codex/config.toml` (the `# BEGIN panda-managed` block whose sub-keys inside `[tools]`/`[skills]` make the whole file fail to load under `--strict-config`). The JSON removal reuses `jsonRemovalSpan` and the TOML removal reuses `TOML_STRATEGY.remove`'s blank-line rule, so panda takes back exactly the separators panda introduced; the result is re-parsed before it lands, and a removal that would leave the file unparseable is refused (MEASURED as a PAIR: weakening the span to the bare property node leaves `{"a":1,}` and the parse guard catches it — mutations M10 and M12).
- **CORRECTED after review — the JSON side matched on a KEY NAME and asserted a provenance panda cannot know (HIGH, review round 2).** `scanLegacyJson` matched `memberProperties(root, 'panda')` and nothing else: a user's own `panda` key was reported as a `problem`, with a detail saying "a previous panda build wrote" it, and `--apply` deleted 137 bytes of their content as a success. That contradicts AD-6 — the rule this whole vocabulary is justified against, *"never inferred from paths"*, and a bare key name is weaker than the path AD-6 already rules out — as well as the frozen Never and the contracts' own *"the only bytes it can remove are panda's own vocabulary"*. Panda now requires EVIDENCE: the value must be an object whose every member is vocabulary a panda build wrote (`version`, `tools`, `mcpServers`, `skills`, `hooks`, from correction-01's own evidence table). A key holding anything else, or MIXING panda's vocabulary with the user's, is not reported and not removed — silence, not a refusal, because a refusal is still a report and panda has nothing to report about a key it has no reason to think it wrote. MEASURED: dropping the evidence check turns two rows red, and loosening `every` to `some` turns the mixed-key row red (mutations N6, N7).
- **CORRECTED after review — TOML marker matching had no string awareness, and the argument for skipping the TOML parse guard rested on it (HIGH, review round 2).** A `# BEGIN panda-managed` inside a multi-line basic string is three of the USER's own bytes, and `line.trimStart().startsWith(...)` matched it: three lines deleted, `applied: true`, no refusal, and the change detail saying "every other byte of the file is left exactly as it is". My own justification for the missing parse guard — *"the removed region is bounded by panda's own two marker lines"* — was wrong in its premise: it was bounded by text that LOOKS like panda's markers. The scan now walks only the lines outside an open multi-line fence, which is the whole of what can hide a line-initial `#`, and panda still parses no foreign TOML. MEASURED: removing the string awareness turns two rows red (mutation N8).
- **`panda init` neither reports nor removes a legacy block, and the mode is hard-coded (decision).** `legacyFor` passes `mode: 'inspect'` literally rather than threading `runScope`'s own mode, and the scan runs only under inspection. Removal is a decision, and this story's whole rule is that a decision is a user's. MEASURED: threading the caller's mode makes `panda init` silently rewrite a vendor file and turns two rows red (mutation M14). A legacy file panda cannot READ is deliberately not reported at all — its only resolution would be a removal panda could not then perform, which is `panda doctor`'s own Never clause, and two of the three files are already covered by `target-failed`.
- **CORRECTED after review — `discard` HAS the read-write race check, and the argument for omitting it was false on both halves (HIGH, review round 2).** The omission was justified as "the failing case cannot be BUILT, because the read and the write are inside one call and nothing deterministic can interleave". A reviewer built it: `atomicWriteText` performs six further filesystem round-trips after the read (`lstat`, `mkdir`, `stat`, `writeFile`, `chmod`, `rename`), and a competing write anywhere in that window is clobbered — reproduced with `applied: true`, no refusal, and the user's save gone. The scope half was wrong too: Claude Code writes `~/.claude/settings.json` itself, so one of the three IS vendor-rewritten. The guard is back and its case is real: the competing write is chained off a read of the same file so it lands inside the window, and the attempt is repeated because the scheduler owns the interleaving — the LOOP is what makes the assertion deterministic rather than the race. MEASURED: disabling the check turns it red (mutation N9). The standing lesson is kept in `deferred-work.md`, because it is the more valuable half: a declared omission beats a decorative guard, and an omission justified by a premise nobody tried to falsify beats neither.
- **`DriftKind` became a derived type over a new `DRIFT_KINDS` const (decision).** The totality proof has to enumerate the drift verdicts at RUNTIME to assert each has a remediation-backed exit, and a hand-written list in the test would fall behind the union silently — which is the failure mode the whole proof exists to remove. The type is unchanged for every consumer; `packages/contracts/test/projection.test.ts` still pins the three verdicts exhaustively and is untouched.
- **The remediation engine has no filesystem verb that can remove a path, and that is pinned (decision).** `packages/projection/test/guard.test.ts` asserts `remediate.ts` imports exactly `readFile` and `stat` from `node:fs/promises`. Adding `rm` — a reviewer's obvious way to make `discard` "simpler" — is a delete path with no ledger authority behind it, on the one command a user reaches for while something is already broken. MEASURED: adding the import turns the clause red (mutation M21).
- **One pre-existing test file was edited, additively, and it was forced.** `packages/environment/test/doctor.test.ts` — `TARGET_SCOPED` gained `'legacy-block'`. FORCED by the new finding kind: that array feeds a partition asserted TOTAL against the derived `DIAGNOSIS_FINDING_KINDS`, so a new kind lands there as a missing entry, which is exactly what the clause was built to do. One token added, no expectation weakened, removed or changed; the row that judges what each finding NAMES then judges the new kind too. Nothing else in any pre-existing suite was touched.
- **Mutation results, one line each: 21 weakenings, 21 red, 0 green.** `escapingPath` losing its `ownedPaths` containment → *refuses an ABSOLUTE path outside the root* + *refuses a RELATIVE path that would resolve against the cwd*; `escapingPath` losing the same-file check → *refuses a claim that names a different file*; the link check removed → *refuses a tree reached through a junction*; the two-ids-one-path check removed → *refuses to claim a path another registry entry already claims*; a missing file made skippable → *refuses a tree that is only partly there*; `absolutePathOf`'s containment removed → *refuses a target that plans outside its own root*; adopt tolerating an unreadable ledger → *refuses adopt and does not rewrite the ledger*; `discard`'s scope containment removed → *refuses a file outside the scope it was given*; `claimNative` dropping `entryConflict` → *refuses when the vendor document spells the location twice*; the legacy JSON span taken bare → *takes the reserved `$.panda` key out and leaves every other byte*; the unbalanced-marker refusal removed → *refuses markers it cannot bound*; the still-parses guard disabled (paired with the bare span) → the same JSON row; the mode validator replaced by `!== 'inspect'` → three fail-closed rows; `legacyFor` threading the caller's mode → *panda init does not remove it* + *doctor reports it*; the ambiguity refusal disabled → *refuses an ambiguous request*; the not-reported refusal disabled → *refuses a state panda did not report*; the remediation default flipped to apply → three describe-then-act rows; `not-writable` promoted to a remediation → *has a case for exactly the remediable states*; a fifth remediation verb nothing reports → *leaves no remediation unreachable*; a second `rewriteAll` mention → *lets nothing but repair rewrite the ledger*; `rm` imported into `remediate.ts` → *no filesystem verb that can remove a path*. Separately, adding a `DiagnosisFindingKind` with no exit fails to COMPILE (`TS2741`, in both `doctor.ts` and `remediate.ts`). **Two of those counts do not reproduce and are corrected here: M13 is 5 red, not 3, and M17 is 6, not 3.** Both were under-counts, so the property is stronger than the log claimed — the driver printed `failing.slice(0, 3)` and the log copied what was printed. A MEASURED number that does not reproduce is the exact smell this file exists to prevent, whichever direction it errs in.
- **Test counts (after review round 2).** New: `packages/projection/test/remediate.test.ts` 54, `packages/environment/test/remediate.test.ts` 19, `packages/cli/test/remediate.test.ts` 8, `packages/projection/test/guard.test.ts` 8 — 89 new cases, up from 62 in round 1. Package totals after: `@skanl/panda-projection` 246 (243 passed, 3 skipped), `@skanl/panda-environment` 91, `@skanl/panda-cli` 61, `@skanl/panda-contracts` 58. No test in this story touches the developer's real skills roots: both new filesystem suites take the `snapshotRealSkillsRoots` before/after snapshot that M4.B introduced.

### Review round 2 — the claim was over KINDS, the guarantee is over STATES

The round's organising sentence was a reviewer's: *every state panda reports has a way out* is true of every finding KIND and was false of at least four reachable INSTANCES. The totality machinery is non-circular and it holds; its success predicate is "the kind stopped being reported", and one finding satisfied that predicate while leaving the user strictly worse off. Every entry below closes one of those instances or one of the assumptions underneath it.

- **`panda doctor` was still telling users four remediable states were terminal (HIGH).** `FINDING_EXITS` existed, was exported, and was consumed by nothing outside the tests, so the product printed *"panda never overwrites an entry that changed since it wrote it; projecting again leaves your edit exactly as it is"* for `edited`, `removed-by-user`, `foreign-collision` and `ledger-damaged`. My own acceptance criterion says *"when the user asks for its remediation BY NAME"* and the product never printed the name: the trap was closed in the code and left open on the only surface a user reads. `FINDING_EXITS` now lives in `doctor.ts` beside the kinds it is total over, and every `resolution` is COMPOSED from it, so a verb renamed upstream renames itself in the report. Pinned by a clause that derives the expected substring from the exits themselves; MEASURED, dropping the composition turns it red (mutation N18).
- **Two states where every named exit refused, permanently (HIGH).** A `foreign-collision` from a duplicate `mcpServers.ctx` key (plain JSON anyone can produce) or from `[mcp_servers]` written as a table: `adopt` refuses on the ambiguity, `release` refuses because a foreign collision holds no claim. And a `legacy-block` with unbalanced markers or a doubled key, which is reachable by the most likely user action there is — starting to hand-clean the block doctor just named. The exits are real but they are the USER's, in the user's own file, and panda was not saying so. Every one of those refusals now names the concrete action ("resolve the ambiguity in '<file>' itself and re-run", "remove what is left of it by hand"), and the `foreign-collision` and `legacy-block` exit sentences carry the same caveat so it reaches the report as well as the refusal. Not a code change: an honesty one, and it is what the severity comment's promise — *"the exit code is a promise that the light can be got back to green"* — actually rests on.
- **`adopt` hands panda authority to overwrite and then DELETE a file the user wrote, and the description said the opposite (HIGH, USER-DATA, NOT fully closed — see below).** Measured twice end to end: a user's hand-written `SKILL.md` at panda's planned path, adopted, overwritten by the next `panda init`, and deleted outright after deregistration. The wording was INVERTED relative to risk — the re-claim branch (the file was panda's to begin with) carried the warning, while the fresh-claim branch (the occupant is the user's file, strictly higher stakes) said *"no byte of that file is read again, written or removed"* beside `(0 byte(s))`: true of the act, false of its consequence. Both branches now say the same thing, `ProjectionClaim` carries `ownedPaths` and `removedNext`, and the preview NAMES every path that becomes deletable and which of overwrite-or-remove the next run will do. MEASURED: dropping the path list, or restoring the reassuring sentence, each turns a row red (mutations N4, N5). **The BEHAVIOUR is unchanged and needs a decision that is not mine** — see the renegotiation note under Design Notes.
- **`repair` lost a claim that was readable (HIGH).** It read the ledger OUTSIDE the per-file queue and called `rewriteAll` inside it, so a claim written in between was destroyed by the one write in the system that does not merge — deterministic, in-process, first try, and exactly the loss `ledger.ts`'s own class header describes. `adopt` and `release` had the same shape and were saved by `update`'s merge; `rewriteAll` has no backstop, which is what made it the one that lost data. The read that decides the write now happens inside the queue. MEASURED: moving it back out turns the row red (mutation N10).
- **Two ledger writes became entry-granular (MEDIUM, USER-DATA).** `adopt` and `release` handed `update` a whole replacement set for the scope, built from a read they took earlier — which RESURRECTS every claim another writer legitimately dropped in between, and on a materialisation root a resurrected claim is an authority to delete a path panda does not own. `ProjectionLedger.updateEntry` reads inside the queue and moves one entry. The case is deterministic by construction: the competing drop is fired from inside the shipped `claim()` seam, and the ledger's own FIFO queue orders the two writes. MEASURED: restoring the whole-scope replace turns it red (mutation M3).
- **`discard`'s containment was string arithmetic on an unresolved path (MEDIUM, USER-DATA).** The sixth attack, and the five M4.B cases do not cover it: with `~/.claude` a junction into a dotfiles repository, `resolve()` says the file is inside the scope and the write lands outside it — outside the very scope the refusal message promises. M4.B's link rule was applied in `claimMaterialised` and not here. Both the file and the root are resolved through `realpath` now, with the nearest existing ancestor as the fallback for an absent file. MEASURED: reverting to string arithmetic turns it red (mutation N11).
- **The JSON parse guard self-disabled on exactly the files at risk (MEDIUM).** It used `JSON.parse`, so a document with a comment, a trailing comma or a byte-order mark "never originally parsed" and the guard was skipped — and those are the same inputs `jsonRemovalSpan`'s whitespace walk handles worst (a `// note` between the member and its comma leaves a dangling comma the original did not have). It is judged with jsonc-parser now, the way the vendors judge it, and the case asserts the real property: panda refuses and the file is untouched, or panda acts and the file is STILL readable. MEASURED: reverting to `JSON.parse` turns it red (mutation N12).
- **The SDK surface and the command disagreed about the default (MEDIUM).** `runRemediation` — on the FR-29 surface, where untyped callers reach it — defaulted to `'apply'` while `remediate` defaulted to `'inspect'`, so the describe-before-act guarantee was true of the command and false of the layer underneath it. `legacyFor` hard-codes `'inspect'` precisely because of that. Both default to `'inspect'` now, `=== undefined` rather than `??` so a passed `null` still fails closed. MEASURED: restoring the old default turns it red (mutation N13).
- **Every pre-existing usage-error path had lost the `panda --help` line (MEDIUM).** `DEFAULT_USAGE` was `USAGE.split('\n').slice(0, 6)` and this story put two subcommands in front of it, so six call sites printed a truncated synopsis that advertised `panda remediate`, omitted `panda project remediate` and no longer said help existed. Found by a reviewer; also found is that my own first patch of that constant silently never applied. It is derived from the first blank line now, so the block maintains its own boundary. MEASURED: restoring the count turns it red (mutation N21).
- **Both guard tests were mutable to green (MEDIUM).** `import * as fsp from 'node:fs/promises'` left the no-delete-verb clause green, and `ledger['rewrite' + 'All']([])` left the caller list green. The first is closed the way `@skanl/panda-environment`'s own guard closes it — count every MENTION of the filesystem module and require it to equal the named-import count, which covers namespace, default, dynamic and `createRequire` at once. The second is not closable by a scan, so it stopped being one: `rewriteAll` now requires `LEDGER_REPAIR_AUTHORITY`, a module-scope symbol that can only be obtained by importing it, and refuses coded without it. Both scans also strip comments first, removing the false positive the caller list had against its own jsdoc. MEASURED: the namespace import, the missing capability check, and a second module importing the capability each turn a clause red; a comment mentioning the symbol correctly stays green (mutations N14, N15, N24, N16).
- **Three smaller pins the reviewers named, each with its own case.** An exit may name only a command panda actually ships — `startsWith('panda ')` accepted `panda frobnicate` (N19). An exit detail must be more than whitespace (N20). Every SHIPPED config target must implement `claim()`, or a fourth trait record compiles, projects, passes every suite and silently has no `adopt` while doctor tells the user to run it (N17). `panda remediate --help` is answered wherever it appears, like `panda run`, and `panda project remediate` takes the directory positional its two siblings take (N22, N23).
- **Round-2 mutation results: 24 weakenings, 23 red, 1 green BY DESIGN** (the comment-mentions-the-symbol case, which must not fire). Four of them stayed green on the first pass and every one was a real gap rather than a reporting artefact, so each was fixed rather than written up: the record-derived containment check shared its case with the engine's second check and the assertion now distinguishes the two sentences; the entry-granular write had no observable case because the fixture adopted an UNCHANGED entry and `adopt` took its already-claimed early return; the parse guard's case accepted a half-removed member; and the ledger caller list could not see a computed name, which is why it became a capability.

## Design Notes

**Why the refusal was right and the terminality was not.** Panda refuses to touch what it does not own, and M4.B made that refusal stricter for good reason. But a guarantee that can only be escaped by editing the file the guarantee is stored in is a trap, and it has been one since Story 2.2.

**Why one finding at a time.** A bulk fix is where a user loses something they meant to keep. The unit of remediation is the unit of reporting, so the thing the user consents to is the thing they were shown.

**Deliberately not built.** No automatic remediation, no bulk sweep, no interactive prompting, no new drift vocabulary, no bundle export.

## Verification

**Commands:**
- `pnpm check` -- expected: fully green, existing suites unmodified
- `pnpm proof:consumer-install` -- expected: still green
