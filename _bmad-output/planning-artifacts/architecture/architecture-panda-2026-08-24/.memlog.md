---
scope: panda v1 platform
purpose: build substrate for epics/stories/build workflows
altitude: initiative (whole v1)
updated: 2026-08-24T02:41
---

- (version) TypeScript 7.0.2 (native Go port, released 2026-07, verified web): adopt as COMPILER; ecosystem caveat verified - typescript-eslint 8.63 peer caps <6.1.0 and crashes on TS7 (gh#12518, no TS7 API until 7.1); mitigation = Microsoft's official side-by-side pattern: typescript@npm:@typescript/typescript6 alias for lint tooling + tsc7 for build/typecheck
- (version) Node.js 24 'Krypton' Active LTS (24.19.0, EOL 2028-04) = minimum engines + production target; Node 26 Current becomes LTS Oct 2026 - CI canary only. Replaces PRD's '>=22'
- (version) pnpm 11 (11.5.x, pure ESM, requires Node>=22): supply-chain defaults align with panda - minimumReleaseAge 1d, blockExoticSubdeps true, allowBuilds allowlist; .npmrc auth-only, settings in pnpm-workspace.yaml
- (version) Validation: Standard Schema v1 as THE contract-facing schema interface (kernel stays zero-dep); Zod 4.4.x stable allowed in implementations/tests (Zod 3 functionally EOL); Vitest 4.x test runner with native schemaMatching support
- (decision) AD-1 [ADOPTED] Paradigm: microkernel + ports - kernel is a generic service container (lifecycle/DI/event-bus), the 7 contracts are standalone ports, everything else mounts as plugins; adopts model-visible-means-logged invariant from dsh as NFR not backbone; user approved over layered-hexagonal and event-sourced alternatives
- (decision) AD-2 Package topology: role-per-package with strictly-downward dependency rule - kernel depends on NOTHING (not even contracts); contracts standalone (no external deps); adapters/memory/workspace/projection depend ONLY on contracts; cli composes all; third parties implement against @skanl/panda-contracts alone; joint-major versioning per PRD section 11; prevents dependency-rule-by-lint-discipline and forces-contract-consumers-to-install-kernel
- (decision) AD-3 [ADOPTED] Plugin trust model: plugins are trusted code loaded in-process via dynamic ESM import (documented explicitly); real isolation boundaries live elsewhere - executors out-of-process, tools via MCP, secrets never enter projection process; contracts async-friendly so a hostile-plugin worker offload remains possible later; user approved
- (decision) AD-4 [ADOPTED from PRD FR-11..FR-14] State ownership: Registry is THE canonical mutable store (serialized writes); Projections are derived artifacts written exclusively by projection engine via atomic temp-file+rename; vendor configs touched ONLY through ProjectionTarget merges honoring ownership markers/sentinels; observability log is append-only and model-visible-means-logged
- (decision) AD-5 [ADOPTED from PRD FR-1] Injection semantics: hard inject blocks readiness until provided; soft reads return typed-absent and MUST surface explicit not-configured errors at use-site; individual plugin startup failures emit contained events - never crash the kernel (dsh lesson)
- (decision) AD-6 [ADOPTED from gentle-ai/orca lessons] Identity: content-derived IDs (sha256 over canonical identity inputs, e.g. workspace+target); worktree/workspace names retired permanently on release; ownership proven by durable metadata records, never path heuristics
- (decision) AD-7 [ADOPTED] Error model: single typed error hierarchy with stable string codes shared via @skanl/panda-contracts; no naked Error crosses a contract boundary; every contract violation names the violated clause (enables FR-9 contract suites to assert on codes)
- (decision) AD-8 [ADOPTED] Event bus dispatch: synchronous ordered fan-out within scope; handlers may be async and are contained per-listener (one handler rejection never breaks others); no cross-event reentrancy guarantee in v1
- (decision) AD-9 [ADOPTED from PRD FR-5 + gentle-ai lesson] Config layering: defaults -> global -> project -> agent -> invocation overlay, deep-merge with ownership sentinels for panda-owned keys (__replace__-style), each layer inspectable, dump command exposes composed tree with originating layer per key
- (event) Deferred: durability engine choice (v-next, seam defined as four-primitive contract); plugin worker isolation (conditional on dogfooding); SSH WorkspaceProvider impl (contract wide enough per orca study); terminal shell consumer; quota routing policies; secrets vault integrations. Release machinery seed: @changesets/cli for joint-major versioning (verify current version at build bootstrap)
- (decision) Reviewer gate applied: versions PASS (changesets v3 pinned, tsc dual-alias noted); adversarial review found 8 holes H1-H8 - ALL closed by tightening AD-2 (runtime consumption mirrors topology), AD-4 (envelope ownership in contracts + machine-scoped advisory lock + kernel-owned log service + mutation-exclusivity through owning APIs), AD-6 (on-disk record = creation-of-record + same-transaction mirror + recovery sweep), AD-8 (lifecycle events join handlers, no sync re-emit, shutdown drains), AD-9 (contracts own versioned namespaced sentinel grammar, unknown versions = Drift)
- (decision) Reconciliation gaps closed: AD-10 tool-call interception waterfall added (budgets/caps enforced at kernel seam, blocker fix); FR-10 liveness homed as conventions row (hooks only via projection merge path); credential refusal assigned to config-resolution; write-time path normalization + worker-card deferred entry + single-writer/deterministic binding-for-v-next Deferred entries added; PRD engines line updated 22->24 silently-overridden note resolved
- (event) spine finalized
