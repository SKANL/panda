# CLAUDE.md

Agent guidance for panda lives in **[AGENTS.md](./AGENTS.md)** — the single
source of truth for architecture rules, the gates that enforce them, how to run
and check things, and the two tools here that fail as plausible correct answers.

Read it before working here. Do not duplicate guidance into this file: a rule
that lives in two places is a rule that will disagree with itself.

<!-- ponytail: a pointer, not a git symlink. A 120000-mode symlink does not
     survive a Windows clone with core.symlinks off, which is this project's
     primary development platform. Revisit if panda ever needs a third home. -->
