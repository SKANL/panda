{
  "name": "@skanl/panda-contracts",
  "version": "0.1.0",
  "description": "The ports a third party implements to extend panda, and the published suites that prove an implementation conforms.",
  "keywords": [
    "ai-agent",
    "panda",
    "contracts",
    "ports",
    "standard-schema",
    "conformance"
  ],
  "homepage": "https://github.com/SKANL/panda#readme",
  "bugs": {
    "url": "https://github.com/SKANL/panda/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SKANL/panda.git",
    "directory": "packages/contracts"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "panda-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./validation": {
      "panda-source": "./src/validation.ts",
      "types": "./dist/validation.d.ts",
      "default": "./dist/validation.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint .",
    "prepack": "pnpm build",
    "build": "node ../../scripts/clean-dist.mjs && tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@skanl/panda-kernel": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "files": [
    "dist",
    "README.md",
    "METHOD-PLUGIN.md"
  ]
}
