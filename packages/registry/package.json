{
  "name": "@skanl/panda-registry",
  "version": "0.1.0",
  "description": "The canonical environment Registry: scoped storage, portable bundles, and ingest from what is already installed.",
  "keywords": [
    "ai-agent",
    "panda",
    "registry",
    "mcp-server",
    "skills"
  ],
  "homepage": "https://github.com/SKANL/panda#readme",
  "bugs": {
    "url": "https://github.com/SKANL/panda/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SKANL/panda.git",
    "directory": "packages/registry"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "exports": {
    ".": {
      "panda-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint .",
    "prepack": "pnpm build",
    "build": "node ../../scripts/clean-dist.mjs && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@skanl/panda-contracts": "workspace:*",
    "@skanl/panda-kernel": "workspace:*",
    "@skanl/panda-lock": "workspace:*",
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "files": [
    "dist"
  ]
}
