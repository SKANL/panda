{
  "name": "panda",
  "private": true,
  "type": "module",
  "$comment": "The @typescript/typescript6 devDependency is load-bearing: pnpm resolves typescript-eslint's TypeScript peer to 6.0.x through it while workspace packages typecheck on TS 7 natively.",
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "node scripts/check-source-bytes.mjs && pnpm typecheck && pnpm test && pnpm lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "eslint .",
    "build": "pnpm -r build",
    "panda": "node --conditions=panda-source packages/cli/bin/panda.ts",
    "proof:consumer-install": "pnpm --filter @panda/session exec vitest run --config vitest.consumer-install.config.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@panda/cli": "workspace:*",
    "@typescript/typescript6": "^6.0.2",
    "eslint": "^10.9.0",
    "globals": "^17.11.0",
    "typescript-eslint": "^8.67.0"
  }
}
